# Hill Climbing — Known Risks Inventory

**Ranking principle: user safety is the primary axis.** Items are sorted by potential impact on user welfare. Gameplay integrity, scoring accuracy, and code quality are *only* HIGH/MEDIUM if they materially defeat or could defeat a safety mechanism. Otherwise they drop to LOW regardless of how much they break.

This is a deliberate re-prioritisation. An earlier version of this file used "defeats a feature or safety claim" as the HIGH bar, which was the wrong standard.

Categories:
- **Severity** — high (could directly harm user welfare), medium (defeats or could defeat a safety mechanism), low (functional, code, gameplay, or UX issues with no safety impact).
- **Confidence** — confirmed (mechanism analysis suffices), likely (I'd bet money), suspected (worth verifying).

Update this file as items are verified, fixed, or proven false.

---

## HIGH (could directly harm user welfare)

*(none currently identified)*

Historical note: the earlier forward-looking entries here (a code path disabling the idle-pause, a silent report-a-concern failure, a failure-grace regression) all belonged to Meditate's camera "stillness" mode, which was **removed** when Meditate became Timed-only (v1.80). Those mechanisms no longer exist.

---

## MEDIUM (defeats or could defeat a safety mechanism)

*(none currently identified)*

### S1–S6. resolved/removed — Meditate's camera "stillness" mode is deleted (meditate v1.80)
Every MEDIUM item in this file guarded a mechanism that lived **inside** Meditate's camera-guided stillness game. That mode — and all of its code — was removed when Meditate became Timed-only (v1.80): the camera/motion detection, the settling/won/lost state machine, the staircase, the drone audio stack, the mode picker, the safety modal, the 5-minute idle-pause, the sticky posture/session-cap reminders, and the local adverse-event reporter are all gone, along with the `hill-combing-*` game/trajectory/reports stores. None of these describe a live risk any longer:
- **S1** (replay clearing the cooldown timer) — no replay, no cooldown, no staircase.
- **S2** (mute toggle defeating the idle-pause) — no drone audio, no idle-pause.
- **S3** (loop error boundary; was ✅ verified fixed v1.25) — the `requestAnimationFrame` game loop it wrapped is gone.
- **S4** (sticky posture / session-cap reminders) — those reminders were removed with the mode.
- **S5** (adverse-event reports with no review path) — the `hill-combing-reports`/`submitReport` reporter is deleted.
- **S6** (the 5-minute idle-pause is a coarse trance heuristic) — the idle-pause is gone.

Timed mode is a quiet bell-bookended countdown sit with no camera, no motion detection, and no sustained trance-inducing visual/audio surface, so the mechanisms these items protected are no longer present to defeat.

---

## LOW (gameplay, scoring, UX, or code-quality issues — no safety impact)

> **Tombstone (meditate v1.80): L1–L8, L10, L11, L14, L15, L16 are moot.** Like S1–S6, these
> described mechanics that lived *inside* Meditate's now-deleted camera "stillness" mode (the
> `requestAnimationFrame` round loop, `setPhase`, the staircase, the motion/smoothness windows,
> trajectory recording, `onWin` dead code, the frame ring buffer) or the removed multi-page
> onboarding. That code is gone (Meditate is Timed-only), so none of them describe a live risk —
> they are kept below only as decision-history. The still-relevant LOW items are the general ones
> (L9 key-prefix, L12 accessibility, L13 mobile layout, L17 audio-resume) and L18 onward.

### L1. Tab-switching can complete a round without the user being still
- **Confidence:** confirmed
- **Was:** previously labelled HIGH. Re-categorised: this is a gameplay-integrity issue, not a safety issue. A user who exploits this gets inflated stats; nobody is harmed.
- **Where:** `requestAnimationFrame` pauses on tab hide. On return, `performance.now() - roundStart` exceeds the duration target and the round is marked won.
- **Impact:** corrupts staircase calibration upward; misleading stats. No physical/psychological harm.
- **Fix (if motivated):** detect dt anomaly and abort, or listen for `visibilitychange` and abort active rounds.

### L2. `setPhase` reassignment via wrapper
- Code quality. Works in non-strict mode. Replace with direct `updateReplayButton()` call inside `setPhase`.

### L3. Camera disconnect leaves the loop running
- Loop reads `readyState < 2` → motion 0 → stillness rises to 100% → audio gets louder. Confusing, not harmful.

### L4. Idle-pause may fire when the onboarding modal is re-opened mid-session
- Modal content gets overlaid by "Long pause detected." Cosmetic.

### L5. Motion window for smoothness scoring not reset between settling and active
- First second of an active smoothness round may use stale frames. Scoring accuracy issue. No safety impact.

### L6. Threshold-mark coordinates untested at extreme threshold values
- Math works in tested range (0.50–0.95). Visual QA only.

### L7. Trajectory recording includes settling lead-in
- Design choice, not bug. Replay starts with the climb to threshold.

### L8. Dead code: `cappedOut` variable in `onWin`; unused `scoreDisplay` reference
- Cleanup.

### L9. Inconsistent localStorage key prefixes (`hill-combing-*` legacy, `hill-climbing-*` current)
- Documented in REQUIREMENTS.md §1.1. Cosmetic.

### L10. `postRoundReminder` returns strings with leading space
- Style fragility.

### L11. `loopRunning` is one-way — no recovery if the loop dies  ✅ verified fixed in v1.25 (subsumed by S3)
- Resilience. The S3 try/catch/finally wrapper means the loop body errors no longer prevent the next frame from scheduling, so even if the loop "dies" inside the body, `rAF` keeps firing. The original `loopRunning` flag is now belt-and-braces; the safety property holds without it.

### L12. No accessibility considerations
- Welfare for users with visual/motor impairments. Tier 2+ prerequisite.

### L13. Mobile / narrow-viewport layout breakage  🟡 partially addressed in v1.29
- UX. Single 640px breakpoint added in v1.29: round-info wraps, ring shrinks to 240px, timer moves below the ring, corner buttons tighten. Verified visually for typical phone widths. Not yet tested below ~360px (very small phones), and not a full mobile-first redesign — Tier 2+ public launch would still need a more thorough pass.

### L14. Smoothness rounds use the same threshold as stillness rounds
- Tuning question. As threshold rises, smoothness becomes very hard.

### L15. Onboarding Skip button on the last page is functionally redundant with Begin
- Cosmetic.

### L16. Frame ring buffer corruption after long tab-hide (chained from L1)
- Self-corrects within ~10 frames.

### L17. Audio context resume after long backgrounding untested
- Browsers may auto-suspend `AudioContext`. Engine has resume guard but not verified.

### L18. No data export or delete-all-my-data button
- Privacy. Tier 2+ prerequisite per REQUIREMENTS.md §1.4.

### L20. Weekly streak display and CONSTRAINTS §5 anti-pattern tension
- **Confidence:** confirmed (design choice, not a bug)
- **Where:** `index.html` dashboard, added v1.64.
- **Description:** CONSTRAINTS §5 explicitly rejects "streak mechanics that punish absence" and "streak counts displayed prominently." The weekly streak counter is designed to avoid those patterns: it is weekly rather than daily, placed below the practice cards (not the first thing on the page), uses a rule where an inactive current week does not break the streak (it only grows or holds), and there are no notifications or loss-aversion triggers. The risk is that future changes (making it daily, moving it above the cards, adding push notifications) drift it into the anti-pattern.
- **Mitigation:** `STREAK_MIN_DAYS` and `WEEK_START` are named constants; the "what makes a week count" predicate is isolated in `weekIsActive()`. Any change to the streak mechanic should be reviewed against CONSTRAINTS §5 before shipping.

### L21. `hill-climbing-usage` localStorage key not in REQUIREMENTS.md §1.1 data inventory
- **Confidence:** confirmed (documentation gap, not code bug)
- **Where:** REQUIREMENTS.md §1.1 claims to document every localStorage key the apps store. The new key added in v1.64 — `hill-climbing-usage` — is missing from that table.
- **Shape:** `{ "v": 1, "meditate": { "YYYY-MM-DD": 1, … }, "breathe": { "YYYY-MM-DD": 1, … } }`. Stores only date-flags; no session content, no text, no user-identifiable data.
- ✅ **Resolved:** added to REQUIREMENTS.md §1.1 with founder confirmation (v1.64).

### L22. REQUIREMENTS.md §1.1 data inventory and §1.3 network claims are stale after the multi-app + Web Push growth
- **Confidence:** confirmed (documentation / audit-readiness gap, not a code bug; found in the v1.77 doc audit)
- **Where:** REQUIREMENTS.md §1.1 ("every item the app stores or transmits") and §1.3 / §2.1 ("zero outbound network traffic at Tier 0–1" / "nothing leaves the device").
- **§1.1 missing localStorage keys:** `breathe-session-duration` (breathe pref), `hill-climbing-install-dismissed` (hub install banner, v1.73), `hill-climbing-nourish` (nourish ladder state, v1.77), `hill-climbing-timed-minutes` (meditate timed-mode pref). The `hill-climbing-usage` row also predates Nourish — it is now written by `meditate.html`, `breathe.html`, **and** `nourish.html`.
- **§1.1 missing store entirely:** the Reflect **`journal` IndexedDB** (entries with free-text notes + mood/satisfaction ratings) — the most sensitive data in the suite, and §1.1 currently documents only localStorage. A privacy auditor would expect this inventoried.
- **§1.3 / §2.1 network claim inaccurate:** the v1.76 opt-in Web Push reminders register a push subscription with a third-party push service and receive pushes — outbound/inbound traffic beyond the initial HTML/JS load, available at Tier 0. The "zero outbound network at Tier 0–1" and "nothing leaves the device" statements are no longer strictly true for users who enable reminders; §5's verification row inherits the same gap.
- **Impact:** REQUIREMENTS.md is the binding auditable doc. An external privacy/safety auditor relying on it would find §1.1 incomplete and the §1.3 network claim inaccurate. No user harm — a governance / audit-readiness gap (same class as L21).
- **Fix:** amend §1.1 (add the four keys + an IndexedDB subsection for `journal`), update the `hill-climbing-usage` row to name Nourish, and carve out the opt-in Web Push exception in §1.3 / §2.1 / §5. **Binding doc → requires founder ratification** (CONSTRAINTS §6).
- ✅ **Resolved:** all of the above amended with founder confirmation in the v1.77 doc audit — §1.1 gained the four keys + a Browser IndexedDB subsection for `journal`, the `hill-climbing-usage` row now names Nourish, §1.3/§2.1/§5 carve out the opt-in Web Push exception, and §1.4's delete-all scope now covers the full key set + the `journal` DB.

### L23. Nourish's level / progression mechanic sits near the anti-gamification line
- **Confidence:** confirmed (design consideration, not a bug)
- **Where:** `nourish.html`, added v1.77.
- **Description:** CONSTRAINTS §3.1 names "gamification beyond the user's interest" as a power-over-behaviour concern, and §5 rejects progression mechanics whose purpose is to drive engagement. Nourish has explicit levels, a climbing ladder, a 2-up/1-down staircase, and skill "unlocks." These serve pedagogy (keeping the cook at their learning edge, ~71% success) rather than time-in-app — but the line is real and worth watching.
- **Mitigation (already in place):** free skips on every challenge; non-shaming outcome copy ("Struggled" → "that's how cooking is learned," no red/fail language); no points, badges, or scores beyond the level number; no notifications; progress framed for orientation, not as a number to maximise; the hub streak is weekly not daily (see L20). Any future change adding points, daily streaks, or push nudges to Nourish should be reviewed against CONSTRAINTS §3.1/§5 before shipping.

### L34. resolved/removed — the Taste tab (former Savor) was removed from Nourish in the app-slimming pass (L51); entry kept as history

*(Historical text below — the Taste tab, its lessons, and its `hill-climbing-savor` store no longer exist in the suite.)*
- **Confidence:** confirmed (design consideration, not a bug)
- **Where:** originally `savor.html` v0.1; **Savor is now the "Taste" tab inside `nourish.html`** (this session).
- **Merge note (this session):** Savor was consolidated into Nourish as its **"Taste" tab** (nourish v0.17, unified Cook + Taste); `savor.html` is now a thin redirect stub → `nourish.html#taste`. The `hill-climbing-savor` store is preserved and now rides inside Nourish (declared in Nourish's HC manifest alongside `hill-climbing-nourish` and `hill-climbing-nourish-chef`); Taste usage folds into the Nourish dashboard row on the hub. The community-lessons provenance and progression concerns below still apply to the Taste tab; only the "separate app" framing is historical.
- **Provenance honesty:** the six "From the community" guest lessons ship as bundled content, attributed by *role/relationship* ("Every Italian grandmother," "A line cook," "The person who taught me to cook") rather than as testimonials from specific, identifiable real people — so they read as folk/kitchen wisdom, not fabricated quotes. The shelf note labels them honestly as "a handful of starters … the rest grows from what you and others add and share." The **participatory** half is real: a user writes their own lesson (stored in `state.myLessons`), it merges into the same shelf tagged "yours," and Import accepts a single shared-lesson file — so "community" is a genuine contribute/share loop, done privacy-first (nothing is uploaded; sharing is a manual file export). Residual watch-item: if seed lessons are ever expanded, keep attributions role/relationship-based (or clearly fictional) rather than impersonating real cooks, and keep the "starters" labeling.
- **Gamification line (cf. L23):** Savor tracks *watched episodes* and a season filmstrip, and feeds the weekly hub streak like the other practices — but deliberately carries **less** progression machinery than Nourish: no staircase, no levels, no difficulty adaptation, no scores. Episodes are ordered as a curriculum, not a ladder to climb for its own sake; the notebook stats ("episodes tasted / notes kept / tastes explored") are orientation, not targets. Same CONSTRAINTS §3.1/§5 review applies to any future addition of points, daily streaks, or nudges.
- **Food safety:** every tasting experiment deliberately uses ingredients safe to eat raw (salt, lemon, tomato, cucumber, apple, oil, bread, broth, cheese, coffee, greens, citrus) — no raw meat, egg, or anything requiring a cook step before tasting. User-contributed lessons are free text; the contribute form's help copy asks for "cheap and safe to taste as they are," but there is no enforcement — a future concern only if lessons are ever shared/curated at scale.
- **Binding-doc inventory (reframed by REQUIREMENTS v0.2 / L32):** under the standard-first policy, Savor is judged by the three tests, not by presence in an inventory — and it passes at Tier 0: **value** — watched episodes, tasting notes, and own lessons all serve the user's own learning record, no "might-be-useful-later" data; **consent** — implicit is the Tier-0 standard, and Savor adds no *new* off-device flow (it's on-device, and joins the existing E2EE-sync flow like every practice); **control** — export/import is comprehensive and machine-readable, deletion is real. **Resolved:** the formal per-key rows are now in Appendix A — `hill-climbing-savor` (A.1) and `ls:hill-climbing-savor` (A.5 blob list); usage logging goes through the shared `hc-usage.js` (A.1 aggregate + A.2 `hc-usage/records` + the `usage/…` sync log), same as the other practice apps — a factual keep-current update (founder standing instruction to keep documentation current), no policy change. Nothing outstanding.

### L35. resolved/removed — the Savor ⇄ Nourish coupling is gone entirely: the Taste tab was removed in the app-slimming pass (L51)
- **Merge note (this session):** Savor is now the **"Taste" tab inside `nourish.html`** (v0.17); `savor.html` is a redirect stub. The two-file coupling this item tracked — Savor reading Nourish's blob, both files hardcoding episode ids — is now **intra-file**, so the cross-file invariant is largely gone. The deep-link rewrite preserved episodes (`savor.html#c3` → `nourish.html#taste-c3`). The graceful-degradation detail below is historical context for the two-file era.
- **Confidence:** confirmed (maintenance coupling, not a bug)
- **Where:** `savor.html` v0.2 reads Nourish's `hill-climbing-nourish` localStorage blob (`readNourish()` → `tracks[track].level`) to render the Watch "In the kitchen" bridge; `nourish.html` v0.15 deep-links into Savor via `savor.html#<episodeId>` (`SKILL_TO_EPISODE`). Two soft contracts now cross the app boundary: (1) Savor depends on Nourish's saved shape, and (2) both files hardcode Savor episode ids (`nourish.html` `SKILL_TO_EPISODE`, `savor.html` `NOURISH_PAIR`).
- **Impact:** LOW, and both directions **degrade gracefully by design.** If Nourish's schema changes, `readNourish()` is fully guarded (try/catch, type checks, legacy-flat fallback) and returns `null` → Savor shows the generic first-time bridge, never an error. If a Savor episode id is renamed/removed, a Nourish deep-link to a missing `#id` is ignored by `handleDeepLink` (unknown id → lands on the show), and Savor's own `NOURISH_PAIR` falls back to `c10`. No user-facing breakage in any case — the worst outcome is a bridge that quietly reverts to generic copy or a link that opens the show's home instead of a specific episode.
- **Maintenance note:** if Savor's `CORE` episode ids ever change, update **both** `savor.html` `NOURISH_PAIR` and `nourish.html` `SKILL_TO_EPISODE`. No shared module exists (single-file apps by design), so this is a manual cross-file invariant.

### L19. GitHub Pages legacy build trigger occasionally drops pushes
- **Confidence:** observed (v1.57 and v1.58 push events both failed to auto-trigger builds; v1.56 was the last commit Pages built before manual intervention)
- **Where:** the repo is on `build_type: "legacy"` Pages (per `gh api .../pages`), which auto-builds on push to `main`. The trigger isn't 100% reliable — quick successive pushes occasionally land without firing a build, leaving the deployed site behind the repo head.
- **Impact:** "I pushed" and "the deployed site reflects what I pushed" are not the same statement. Without a check, the agent can confidently report "shipped" while the user is still seeing yesterday's build.
- **Detection:** `gh api repos/cognitivemirrors/hill-climbing/pages/builds/latest --jq '.commit'` returns the most recent built commit. Compare to local `git rev-parse HEAD`.
- **Recovery:** `gh api -X POST repos/cognitivemirrors/hill-climbing/pages/builds` queues a manual build; takes ~30–60 s.
- **Cache caveat:** the Fastly edge in front of Pages has `max-age=600` — even after a successful new build, the user's browser may serve cached HTML for up to 10 minutes. Hard refresh bypasses; otherwise it self-clears.
- **Workflow upgrade path:** migrating to GitHub Actions Pages (build_type: "workflow") gives explicit, observable build runs and would surface trigger drops as failed/missing runs rather than silent stale-deploys.
- **Recurred at v1.77 (2026-06-20):** the push of commit `26d069e` did not auto-trigger a build — the latest *built* commit remained `6880d2b` (v1.76). The documented recovery (`POST .../pages/builds`) was attempted but blocked by the environment's auto-mode classifier as an out-of-scope production action, so the rebuild was deferred to the operator. Confirms the trigger is unreliable and that "pushed v1.77" ≠ "v1.77 is live" until the build is verified.

### L24. Council sends personal situation text off-device — contradicts REQUIREMENTS §1.3/§2.1 "nothing leaves the device"
- **Confidence:** confirmed (by design; `council.html`, added v1.90)
- **Severity:** LOW *as coded* at Tier 0 (user-initiated, disclosed, on the user's own key, no operator server) — but it touches a **binding** claim, so it needs founder attention before any tier advance or before Council is promoted to the hub.
- **Where:** `council.html` `callClaude()` POSTs the user's free-text situation (and the four directors' replies) to `https://api.anthropic.com/v1/messages` using a bring-your-own Anthropic key. REQUIREMENTS §1.3 and §2.1 assert "zero outbound network traffic at Tier 0–1" / "Nothing leaves the device at Tier ≤ 1," with the opt-in Web Push reminder as the *single* stated exception (L22). Council is a second, larger exception: it transmits the most sensitive content in the suite (free-form personal decisions), comparable to the Reflect journal but **off-device**.
- **Why it's still defensible at Tier 0:** it is strictly user-initiated (only on "Convene"), only sends what the user types, uses the user's own API key (no operator intermediary — consistent with CONSTRAINTS §3 power-distribution: the user owns the relationship with the model), stores nothing server-side, keeps sessions in local storage only, and is disclosed plainly on first run and in the crisis footer/disclaimer.
- **Binding-doc gap (needs founder ratification — do NOT self-amend):**
  - §1.1 data inventory omits the two new localStorage keys: `hill-climbing-council-key` (the user's Anthropic API key — a **credential**, the first stored in the suite; note browser localStorage is not a secure secret store) and `hill-climbing-council` (saved deliberations: free-text situations + generated advice, cap 30).
  - §1.3/§2.1's "nothing leaves the device (except opt-in Web Push)" is now false; Council is a second exception and should be documented as such, with the off-device data-flow and third-party (Anthropic) processing named.
  - §2.4 "sharing" and §2.2 "what we never collect" should acknowledge that convening transmits user content to Anthropic under the user's own account/terms.
- **Open questions for the founder:** (a) is bring-your-own-key the right power-distribution posture, or should Council stay off the hub until a data-flow/consent design is ratified? (b) does an app that sends personal content to a third-party LLM belong under the same care/safety constraints as the sensor apps, or does it need its own section? (c) API-key-in-localStorage risk acceptance.

### L25. Climb's goal/todo mechanics sit near the anti-gamification line — and its event log is a mirror that could become a scoreboard
- **Confidence:** confirmed (design consideration, not a bug)
- **Where:** `climb.html`, added v1.94.
- **Description:** Todo trackers are the canonical guilt machines — due dates, overdue reds, completion percentages, and streaks are the standard engagement toolkit CONSTRAINTS §3.1 ("power over user behaviour") and §5 (streak mechanics, loss-aversion) explicitly reject. Climb additionally keeps an **append-only event log of the user's own follow-through**, which is designed as a self-owned mirror for reflection but could read (or later be surfaced) as self-surveillance or a report card.
- **Mitigation (already in place):** no due dates at all in v1 (dates are the todo-app guilt engine); no points, badges, streaks, or completion percentages; archive copy is "resting" / "Let it rest" / "Pick it back up" — never failed, abandoned, or overdue; the focus card is framed as attention ("Current focus"), not obligation; History is a flat chronological mirror with no aggregates beyond a raw event count; the log is local-only, exportable, and honestly deletable in-app ("Delete everything" removes both the state blob and the event database); no notifications; hub usage feeds only the existing weekly (not daily) dots (L20 rationale applies).
- **Watch-items:** any future analytics view over the event log must present rates and "attention gravity" as neutral mirrors for the user's own curiosity — the moment a derived number starts functioning as a target (velocity, completion rate, goals-per-week), it should be reviewed against §3.1/§5 before shipping. Same review applies before adding due dates, reminders, or any surfacing of "you haven't touched X in N days."
- **Sub-note (log completeness):** the IndexedDB append is best-effort *after* the localStorage state write — in a browser where IDB persistently fails (some private modes), state changes occur that the log misses. On the next healthy load, `log.snapshot` rebaselines the log from current state, so analysis has an honest "gap + baseline" rather than silent divergence.

### L26. Levity and Council storage still missing from REQUIREMENTS §1.1 (pre-existing; observed while adding Climb's rows)
- **Confidence:** confirmed
- **Where:** REQUIREMENTS.md §1.1. Climb's rows were added in v1.94 with founder sign-off, which makes the remaining gaps conspicuous: `hill-climbing-levity` (Levity ladder + notebook state, added v1.92) has no row and Levity is absent from the `hill-climbing-usage` writer list; Council's two keys are already flagged in L24. Binding doc → founder ratification required; not self-amended in v1.94.
- **Resolved (v1.99):** §1.1 now carries `hill-climbing-levity`, `hill-climbing-council`, `hill-climbing-council-key`, and `hill-climbing-train` rows, and the `hill-climbing-usage` row lists the Levity and Train writers — folded into the founder-ratified v1.99 sync amendment.

### L27. Nourish's Chef mode sends pantry text off-device — third egress feature; extends the L24 binding-doc gap
- **Confidence:** confirmed (by design; `nourish.html` Chef mode, added v0.13 / global line v1.97-track)
- **Severity:** LOW *as coded* at Tier 0 (user-initiated, gated behind an explicit consent checkbox + a bring-your-own key, on the user's own account, no operator server) — but, like Council (L24), it touches a **binding** claim and needs founder attention before any tier advance.
- **Where:** `nourish.html` `callChef()` POSTs the user's free-text pantry list + preferences (and the current cooking level + learned-skill labels) to `https://api.anthropic.com/v1/messages` using a bring-your-own Anthropic key and structured output. This is the **third** off-device path in the suite (after opt-in Web Push, L22, and Council, L24) and the **first inside a previously fully-offline sensor-adjacent app** — Nourish was 100% local before this.
- **Why it's still defensible at Tier 0:** strictly user-initiated (only on "Write me a recipe"), gated behind a first-run consent checkbox that names the data flow and the third party (Anthropic) in plain language, uses the user's own key (no operator intermediary — consistent with CONSTRAINTS §3 power-distribution), sends nothing beyond the pantry/preferences/level context, stores nothing server-side, and keeps the key + recipe history in local storage only. The pantry list is far less sensitive than Council's personal decisions or the Reflect journal.
- **Binding-doc gap (needs founder ratification — do NOT self-amend):**
  - §1.1 data inventory omits the new localStorage key `hill-climbing-nourish-chef` (holds the user's Anthropic API key — a **credential**, kept **out of the Nourish data export** on purpose; plus `history`: recipe titles + self-reported outcomes, cap 20). This is the second API key stored in the suite (cf. `hill-climbing-council-key`, L24); browser localStorage is not a secure secret store.
  - §1.3/§2.1's "nothing leaves the device (except opt-in Web Push)" now has a **third** exception. The L24 amendment should be widened to name Nourish Chef alongside Council as an off-device, third-party-processed (Anthropic) data flow.
  - §2.4 "sharing" / §2.2 "what we never collect" should acknowledge that generating a recipe transmits user content to Anthropic under the user's own account/terms.
- **Open questions for the founder:** (a) same bring-your-own-key posture question as L24 — ratify once for both, or treat cooking (low-sensitivity) differently from Council (high-sensitivity)? (b) is it acceptable that a previously-offline practice now has an opt-in online mode, or should online modes live in a separate clearly-labelled surface? (c) two API keys now sit in localStorage across the suite — worth a shared, documented key-handling stance.

### L28. Opt-in E2EE sync — residual risks of the first operator-held store (v1.99)
- **Confidence:** confirmed (by design; `hc-sync.js` + `climb.html`, added v1.99)
- **Severity:** LOW at Tier 0 (opt-in, off by default; the operator is the user; zero-knowledge, so the operator cannot read content) — but it is the suite's **first operator-held store** and first *required-schema* dependency, so the sub-risks below deserve watching before any tier advance.
- **Where:** `hc-sync.js` (envelope + sync loop), `climb.html` (wiring), Supabase `sync_docs` / `sync_keybundle` + RLS.
- **Residual risks:**
  - **RLS is the only guard.** The anon/publishable key is public by design; if `supabase-schema.sql` is applied without RLS enabled (or a policy is later dropped), every user's rows become readable to anyone with the key. Content stays unreadable (zero-knowledge), but the *metadata* (emails, sizes, counts, timestamps, doc keys) would leak. **Verify RLS is on for both tables after any schema change** (checked live via the Supabase security advisor on the v1.99 deploy — clean). This is the one item that would rise to MEDIUM if misconfigured.
  - **Passphrase loss = data loss, by design.** If both the passphrase and the recovery code are lost, the synced copy is unrecoverable — no operator reset is possible (that is the point). Setup shows the recovery code once; a user who dismisses it without saving it is at risk. Local device data is unaffected.
  - **Metadata leakage (accepted, documented).** Even with content encrypted, the operator sees email, ciphertext sizes, row counts (≈ activity volume), timestamps, and doc keys (REQUIREMENTS §1.5). Padding / batching / key-hashing are follow-ups, not shipped.
  - **Concurrent-edit loss under whole-blob LWW.** Two devices editing `hill-climbing-climb` within one sync window: the later push replaces the whole blob, so the earlier device's *state* changes are dropped (the immutable event log still preserves both in History). Fine for one user across their own devices; a per-entity/CRDT merge is the documented fix before multi-user.
- **Watch-items:** confirm RLS after any Supabase change; add full **account deletion** (removing the `auth.users` row) before a tier advance; when extending sync to the **Reflect journal** (the most sensitive store), re-review the metadata-leakage acceptance and use a per-entry merge, not whole-blob. Any change that would sync a **credential** (e.g. Council's API key) must be a separate explicit opt-in — never bundled with normal data.

### L29. Companion sends goals + journal + activity off-device — the broadest personal-content egress in the suite (binding docs amended + founder-ratified)
- **Confidence:** confirmed (by design; `companion.html`, added v0.1 · companion)
- **Severity:** LOW *as coded* at Tier 0 (user-initiated, disclosed behind an explicit key gate, on the user's own key, no operator server) — but it is the **widest-scope** of the BYOK egress paths (L24 Council, L27 Nourish), so it is the most consequential to re-review before any tier advance.
- **Where:** `companion.html` `gatherContext()` reads the user's *current* Climb goals (`hill-climbing-climb`), most-recent Reflect journal entries (up to 25 — free-text truncated ~700 chars each, plus mood/satisfaction, read via the hub's safe version-less IndexedDB open), and recent activity (**since companion v0.11, from the cross-device synced usage aggregate `HCUsage.aggregate()` — the `hc-usage/records` IndexedDB store the hub reads, not the device-local `hill-climbing-usage` localStorage** — day-flags **plus, since companion v0.10, summed practice durations & session counts** for the apps that record them, meditate/breathe — + Nourish level), assembles them into a digest, and `callClaudeRaw()` POSTs it — folded into the system prompt, alongside the running conversation — to `https://api.anthropic.com/v1/messages` on the user's own key. Unlike Council (sends only typed text) and Nourish (pantry text), Companion sends the **journal — the most sensitive store — automatically on every reply, by default**, because the design (founder's choice) shows the agent all three sources always-on with no per-conversation toggle.
- **E2EE does NOT cover this (scope correction, stated plainly):** the end-to-end encryption (`hc-sync.js`) protects the **saved conversation at rest and in cross-device sync** — Supabase holds only ciphertext; the operator cannot read it. It does **not** protect the LLM call: the goals/journal/activity digest reaches Anthropic **readable** (TLS in transit, but plaintext to Anthropic under the user's API terms). So E2EE is the right risk-manager for the *conversation store*; the risk-manager for the *off-device send* is BYOK + explicit gate + the verbatim transparency panel, not encryption.
- **Why it's defensible at Tier 0:** strictly user-initiated (only when the user sends a message), gated behind an explicit key setup that names the data flow and Anthropic in plain language, uses the user's own key (no operator intermediary — CONSTRAINTS §3 power-distribution: the user owns the model relationship), stores nothing operator-readable server-side, and is uniquely transparent — the menu → *What the companion sees* shows the **exact** digest sent. The raw digest is never written into the saved conversation (only the dialogue is), so no second plaintext copy of the journal is created on-device or in the synced blob. (Caveat: the companion's *replies* may paraphrase journal/goal content, and those replies are saved + E2EE-synced — so L28's journal-sensitivity note applies to the conversation blob too.)
- **Key handling — unified across the hub (founder-requested):** the per-app API keys (`hill-climbing-council-key`, `hill-climbing-companion-key`, and the `key` field inside `hill-climbing-nourish-chef`) were consolidated into a single shared `hill-climbing-api-key`, migrated once then deleted, so one BYOK setup serves Council, Companion, and Nourish. This *reduces* the credential-store surface (three copies → one) — a net improvement over the per-app keys — though it does not change the underlying "API key in localStorage is not a secure secret store" caveat (L24/L27); removing the key in any app now clears it hub-wide. Nourish's separate *consent* gate for its off-device send is unchanged.
- **Binding-doc amendment — APPLIED and founder-ratified (this change, not deferred):** §1.1 now carries `hill-climbing-companion` (saved conversations), `hill-climbing-nourish-chef` (now keyless), and the shared `hill-climbing-api-key` credential rows; §1.3 replaces the brittle "three flows" count with a dedicated BYOK-AI-apps bullet naming Council, Nourish, and Companion, and states the Anthropic egress is Anthropic-readable (vs. E2EE sync); §2.1 adds Companion to the opt-in egress list; §1.4 folds the two keys + conversation store into the Tier ≥ 2 delete-all scope; §5's verification row is updated. Hub copy changed from "private · on-device" to "private by default"; Companion is now listed on the hub and precached (`sw.js` `hc-v2.01`).
- **Watch-items before Tier 1 (friends & family):** (a) the always-on default means a non-technical user might not intuit that *conversing* ships their journal — the setup gate discloses it, but consider a one-time "this shares your journal with Anthropic" confirmation before the first send; (b) ensure the future "delete all my data" control actually removes `hill-climbing-companion*`; (c) prompt-injection via the user's own journal/goal text is possible but low-consequence here — no privilege boundary is crossed (single user, own data, advice back to themselves). *(As of v0.2 the companion has read-only web search/fetch — still no state-changing tools; the injection surface it adds is covered in L30.)*; (d) if a per-conversation source toggle is ever added, default-on is fine but the *off* state must be honoured in `gatherContext()`, not just the UI.

### L30. Companion has live internet access — a second off-device pathway (web queries) beyond the digest send (v0.2)
- **Confidence:** confirmed (by design; `companion.html` v0.2 · companion)
- **Severity:** LOW at Tier 0 (user-initiated, on the user's own key, read-only tools, disclosed in-product) — but it is a **distinct new egress channel** layered on top of L29's digest send, so re-review it alongside L29 before any tier advance.
- **Where:** `callClaudeRaw()` now passes Anthropic's server-side `web_search_20260209` + `web_fetch_20260209` tools (`WEB_TOOLS`, `max_uses: 5` each) on every reply. These run inside Anthropic's own agent loop: the model writes a search query, Anthropic executes it (search infrastructure → third-party search providers / the fetched site), and the results return in the same `/v1/messages` response. The browser still only talks to `api.anthropic.com`. A long tool turn can return `stop_reason: "pause_turn"`; the call resumes up to `MAX_TOOL_HOPS` (4) times, re-sending the partial assistant turn.
- **New egress vs L29:** L29 is about *what personal content leaves the device in the request* (the goals/journal/activity digest). L30 is about *new traffic the model generates* — search queries and page fetches — that leaves Anthropic on the user's behalf. This is additional off-device activity that the "only your own data leaves" framing didn't previously cover.
- **Privacy guardrail is a soft (prompt-level) control, not enforced in code:** `COMPANION_SYSTEM` instructs the model to search only with plain, general queries and to *never* put the person's private details (name, journal lines, situation specifics) into a search. This is the right mechanism for a single-user own-data tool, but it is model-obedience, not a hard filter — a model slip could put a private detail into a query. If this app ever advances toward multi-user or higher stakes, consider a harder control (e.g. a query-inspection/redaction step, or gating web access behind a per-conversation toggle).
- **New untrusted-input surface (indirect prompt injection from the web):** web search/fetch pull external, attacker-influenceable content into the model's context. Blast radius stays low here — the only tools are *read-only* web search/fetch, the model only advises the user (no state-changing actions, no privilege boundary), and it's single-user own-data — so a malicious page can at worst skew the advice in one reply, which the transparency + "weigh the source, don't launder a shaky result into confidence" instruction partly mitigates. Watch it if tools with side effects are ever added.
- **Binding-doc tension — flagged, not silently amended:** following the same pattern as L24/L27/L29, this adds an off-device pathway that sits against REQUIREMENTS §1.3/§2.1's opt-in-egress framing (which enumerates the BYOK LLM sends but not model-generated web traffic). Disclosed plainly in-product (lede, setup card, menu note, composer note, *What the companion sees* panel, About), and surfaced here for founder ratification of a REQUIREMENTS amendment — the binding docs are not edited unilaterally.
- **Cost note:** each reply can now trigger up to 5 web searches + 5 fetches on the user's key (server-tool usage is billed on top of tokens). `max_uses` caps it per reply; the user owns the key and the bill, consistent with the BYOK posture. *(v0.3: transient failures auto-retry up to 3× with backoff. A network error usually means the request never completed, so re-billing is unlikely — but a failure that lands **after** Anthropic has already billed the turn would re-charge on retry. Bounded (≤3) and rare; the alternative was a silently-lost reply.)*

### L31. Sync extended to every app with user data (v2.0) — the Reflect journal and five more stores are now off-device-capable (E2EE); binding inventory amended
- **Confidence:** confirmed (by design; `hc-sync.js` unchanged, `hc-sync-chip.js` added, wiring in `meditate/breathe/reflect/nourish/levity/train/council.html`, v2.0)
- **Severity:** LOW at Tier 0 (opt-in, off by default; zero-knowledge, so the operator still cannot read content) — but it widens the L28 surface from one store (Climb) to the whole suite, so the sub-items deserve watching before any tier advance.
- **What changed:** every app with user data now registers a store on the shared engine, joining Climb (v1.99) and Companion (`ls:hill-climbing-companion`, added v0.1 on main). Blob (LWW): meditate `stillness-game`, breathe `breathe-session-duration`, nourish `hill-climbing-nourish`, levity `hill-climbing-levity`, train `hill-climbing-train`, council `hill-climbing-council`. Log (per-entry): the **Reflect journal** — done per L28's guidance as a per-entry merge, *not* whole-blob, with version-stamped doc keys (`id@updated`) and tombstone deletes so edits/deletions propagate under the insert-only log RPC.
- **Residual / carried risks:**
  - **Reflect journal free-text is now off-device-capable (E2E-encrypted).** The single most sensitive store in the suite now has an opt-in outbound path. It is protected by the same zero-knowledge envelope as everything else (operator cannot read it); the risk is the same metadata leakage as L28 (row counts / sizes / timestamps), not content. Note: unlike the Companion *LLM send* (L29, Anthropic-readable), sync egress is E2EE — the journal's plaintext never leaves the device for the sync path.
  - **Binding data-inventory amended (this change — resolves the L22/L26 gap for the synced stores).** REQUIREMENTS §1.1 rows for `stillness-game`, `breathe-session-duration`, `hill-climbing-nourish`, `hill-climbing-levity`, `hill-climbing-train`, `hill-climbing-council`, and the `journal` IndexedDB store now state "Synced (E2EE) when sync is on," and §1.5's scope note is updated from "Climb only (v1.99)" to "all apps with user data (v2.0)" with the doc-key list. Applied here (not deferred), matching the pattern main used for Companion's L29 — founder-authorised via the explicit "make any required changes" instruction on the merge.
  - **Whole-blob LWW concurrent-edit loss now spans six stores, not one** (L28's item). Two devices editing the same app's blob within one sync window: the later push wins, the earlier device's changes drop. Acceptable for one user across their own devices; a per-entity merge is the documented pre-multi-user fix. Reflect is exempt (per-entry log).
  - **Tombstones accumulate.** Deleted journal entries stay in IndexedDB as `deleted:true` rows (and as immutable server rows) forever. No compaction. Negligible at journal scale; note it before any store with high-churn deletes joins sync.
- **Deliberate exclusions (allowlist held):** the shared Anthropic credential `hill-climbing-api-key` (device-local, never synced — see L29), meditate's adverse-event **reports** + trajectory recordings, and `hill-climbing-usage` (a per-day union log that blob LWW would corrupt across devices) are **not** registered and stay device-local.
- **Watch-items:** keep the shared credential out of sync (allowlist); if `hill-climbing-usage` ever needs cross-device totals, it needs a union-merge store, not a blob; the L28 watch-items (confirm RLS after any Supabase change; add full account deletion before a tier advance) now apply across the whole suite.

### L32. Data-privacy requirements reframed standard-first (REQUIREMENTS v0.2) — older items' §-references have moved
- **Confidence:** confirmed (documentation change, not a code issue; founder-directed)
- **What changed:** REQUIREMENTS §1–§2 were reorganised around three tests — **value → consent-scaled-to-audience → controls-proportional-to-risk**. The old "nothing leaves the device (except…)" claim in §1.3/§2.1 is **gone**, replaced by §1.1's honest four-flow egress table (BYOK AI apps, Companion web access, E2EE sync, Web Push — each with *who can read it* + *what bounds it*). The exhaustive per-key inventory + sync internals moved **intact** to **Appendix A** (A.1 localStorage, A.2 IndexedDB, A.3 transient, A.4 network, A.5 sync trust model) and are now labelled a **Tier-2 deliverable**. CONSTRAINTS §1.2 (new C6) / §1.4 / §3.2 P1 / §3.4 were realigned to the same standard.
- **Impact on this file:** several earlier items (L22, L24, L27, L29, L30, L31) cite the *old* section numbers — "contradicts §1.3/§2.1 'nothing leaves the device'," "§1.1 omits key X." Those citations still describe real history, but the targets have moved: network detail is now **Appendix A.4**, the per-key inventory is **Appendix A.1–A.2**, and the "these are exceptions to nothing-leaves-the-device" tension each of them flagged is **resolved at the framing level** — §1.1 now states the off-device flows as first-class posture, not as exceptions to a rule that no longer holds. The underlying data flows are unchanged and their risk assessments still stand; only the doc framing moved.
- **No new user risk.** Framing / audit-readiness only.

### L33. Usage log now carries session durations **and syncs across devices** (v1.78) — binding docs amended + founder-ratified
- **Confidence:** confirmed (by design; shared `hc-usage.js`, meditate v1.78, breathe v0.9 — at founder request: first "capture my session duration," then "I want usage data synced, including durations").
- **Severity:** LOW at Tier 0. Sync is opt-in, off by default, zero-knowledge E2EE (operator can't read content). It reverses the earlier "`hill-climbing-usage` is never synced" posture, so it inherits the L28/L31 sync surface — but adds no content the journal/Climb logs don't already, and the data is the user's own practice timing that they asked to carry across their devices. Passes the §1 three-test standard: a value the user explicitly asked for (Test 1), under the same opt-in E2EE-sync consent already in place (Test 2), with controls proportional to low-sensitivity data — device-tagged, summed, deletable (Test 3).
- **What changed:**
  - **Durations (v1.78, was v1.77's device-local entry):** Meditate (`endTimedSit`) and Breathe (`endBreatheSession`) record a completed session's seconds; the four flag apps mark the day active. All six now route through **one shared module `hc-usage.js`** instead of six copy-pasted inline helpers.
  - **Sync:** usage syncs as a **per-record log store** (`usage/…`), not a blob. Each record `{ id, device, app, day, s, n, ts }` is immutable and device-tagged, so two devices active the same day **sum** at read time instead of clobbering (a whole-blob LWW — the reason usage was previously excluded — could not do this). Records live in a new IndexedDB store `hc-usage/records`; the legacy `hill-climbing-usage` localStorage aggregate is kept device-local for companion/offline reads. A one-time per-device backfill migrates pre-existing history into records; the hub aggregates (this device + pulled-remote) and shows summed minutes.
  - **Latent bug fixed:** the old inline `markUsage` wrote `[day] = 1` unconditionally, clobbering an accumulated `{s,n}` on a second page-load the same day; `hc-usage.js`'s flag write is non-clobbering.
- **Verified (headless Chromium, 24 + 13 checks):** cross-device summation (two devices' same-day durations add; same day = 1 active day, not 2), idempotent remote merge (no double-count), backfill runs once, non-clobber fix, all 7 pages load clean with the shared module, real Meditate sit + real Breathe buttons write records. **Not verifiable here:** the live Supabase transport (no backend/creds in this env) — relies on the already-proven sync engine; a real two-device round-trip is the remaining check before relying on cross-device totals.
- **Binding docs amended (founder-ratified for this change):** REQUIREMENTS Appendix **A.1**'s `hill-climbing-usage` row now states day-flags **+ per-day duration + session count** (the "only boolean day-flags" claim is gone); new A.1 rows for `hill-climbing-usage-device` and `hill-climbing-usage-backfilled`; a new **A.2** `hc-usage/records` IndexedDB store; **A.5** lists the `usage/…` log store and rewrites the "never synced" note (the localStorage aggregate stays device-local, the record log is the synced source of truth); **§1.3**'s delete-all scope now includes the `hc-usage` database.
- **Residual / watch-items:** metadata leakage is the same class as L28/L31 (row counts ≈ activity volume, timestamps, sizes — not content); per-record growth is bounded (~1–5 rows/active app-day) but unlike the day-flag it is unbounded over years — revisit compaction if it ever matters; the device id (`hill-climbing-usage-device`) is a stable per-device identifier that stays device-local (never synced) and only appears inside record ids the operator already sees as `doc_key`s.

### L36. Companion + Council merged into one app; new UI-preference key `hill-climbing-cc-mode` not yet in the binding inventory (companion v0.7)
- **Confidence:** confirmed (by design; `companion.html` v0.7 hosts both modes, `council.html` is now a redirect → `companion.html?mode=council`).
- **Severity:** LOW at Tier 0. The merge changes **structure, not data flow**: both modes already existed off-device (Council L24, Companion L29/L30) on the user's own key, and both sync stores (`ls:hill-climbing-companion`, `ls:hill-climbing-council`) were already registered and E2EE-synced before the merge (Companion since v0.1, Council since v0.4 / the v2.0 sync roll-out). No new egress, no new content leaves the device.
- **What changed:** one page now registers **both** blob stores and calls `HCSync.init()` once. This is safe because the sync engine keys documents by **store-key + user, not `appId`** (verified in `hc-sync.js`: `push`/`pull` query `sync_docs` by `doc_key` = `store.key`; `appId` in the init config is accepted but never read). The two docs therefore stay distinct on the server and reconcile independently — a merged page and the old standalone pages would sync the same rows. The shared `hc-sync-chip.js` now shows one global status pill for the whole app (Companion's bespoke inline menu note was retired).
- **Binding-doc gap (needs founder ratification, not silently amended):** the merge adds one new device-local key **`hill-climbing-cc-mode`** (which mode was last used; `?mode=` overrides). It is a UI preference — no personal content, never synced — and is **not yet listed in REQUIREMENTS Appendix A.1**. Proposed addition: one A.1 row, `hill-climbing-cc-mode` · device-local · UI preference (last-used mode) · not synced. Flagged here per the L22/L24/L27/L29/L30/L31 pattern (surface the inventory gap; let the founder ratify the amendment).
- **Verified (headless Chromium):** mode switch / accent / view routing / key gate / `?mode=` deep-link / persisted-mode round-trip; mode-aware menu; per-mode history; the v0.6 persistent retry-tail survives view switches; the "Continue" seed-button relabel; both sync stores registered + the shared chip mounted; both send/convene async paths error-handle — zero page JS errors. **Not verifiable here:** the live Supabase two-blob round-trip (no backend/creds in this env) — relies on the already-proven sync engine.

### L37. Companion persists a cross-conversation memory — the first at-rest personal *summary*, and a break from the "for this conversation only" framing (companion v0.8, founder-requested)
- **Confidence:** confirmed (by design; `companion.html` v0.8, Companion mode — founder request: "context about me shouldn't be lost across conversations")
- **Severity:** LOW at Tier 0 (user-initiated, on the user's own key, on-device + E2EE-synced, erasable) — but it changes *what the app keeps about the person*, so re-review it with L29/L30 before any tier advance.
- **What changed:** to stop losing context between conversations, the Companion now carries its own dialogue forward — the last 3 past conversations verbatim, plus a **rolling summary** of everything older that a separate tool-free background call (`maybeUpdateMemory()` → `SUM_SYSTEM`) distils and updates. The summary lives in `store.memory.summary` inside the existing `hill-climbing-companion` blob and syncs E2E-encrypted like the dialogue. Companion-mode only; Council deliberations are untouched.
- **Two honest shifts this introduces:**
  - **(a) New personal data at rest.** L29 could truthfully say "the raw digest is never written into the saved conversation — no second plaintext copy of the journal." That still holds for the *suite digest* (goals/journal/activity are still read fresh and forgotten), but the Companion now *does* persist a distilled, model-written **summary of the person's conversations** — which will paraphrase whatever they discussed, potentially including journal/goal-derived content the model surfaced. It's on-device and E2EE-synced (operator sees only ciphertext, same as the dialogue — L28/L29's conversation-blob sensitivity note already covers this class), but it is a *new kind* of at-rest content: an evolving profile, not a transcript. Now named in §1.2 (highest-sensitivity prose).
  - **(b) "For this conversation only" is no longer literally true.** Prior copy (menu note, About, *What the companion sees*, and the system prompt's hard-limit line) told the user their data was used "for that reply only" and "then forgotten." Accurate when only the transient digest was sent; not accurate for the conversation memory, which is the whole point of the feature. All of that copy was rewritten in v0.8 to say plainly that conversations *are* kept and carried forward, that the memory lives on-device (and syncs), and that **"Clear all conversations" erases the memory too** (the forget path — verified: `clear-btn` now resets `store.memory` via `emptyMemory()`).
- **Egress:** no *new* egress channel. The memory is sent to Anthropic on the user's own key exactly as the dialogue + digest already are (L29), and the background summarizer call passes **no web tools** (`callClaudeRaw(..., [])`), so it cannot trigger L30's web traffic. What's new is at-rest persistence, not a new destination.
- **Binding-doc amendment — APPLIED and founder-ratified (this change, not deferred):** against the reframed REQUIREMENTS v0.2 (standard-first — L32), the memory is recorded in **§1.2** (highest-sensitivity prose), **Appendix A.1** (the `hill-climbing-companion` row now documents `memory: { summary, folded, ts }`), **Appendix A.4** (the Companion egress bullet now names the memory send + the separate tool-free background summarizer call), **§1.3** (the delete-all note flags the one blob covers conversations + memory, with a working Tier-0 path today), and **§5** (a verification row: memory bounded + erasable + summarizer carries `tools: []`). The in-product copy was corrected in the same v0.8 change. (Ratified alongside this entry, following the L29 pattern.)
- **Watch-items before Tier 1:** (a) confirm any future "delete all my data" control removes the memory, not just the conversation list (today's `emptyMemory()` reset covers the in-app clear); (b) the summary is model-written and could in principle over-retain or mischaracterize — it's shown verbatim in *What the companion sees*, so the user can inspect it, but there's no per-item edit yet (consider an "edit/forget this" affordance if the profile grows sensitive); (c) prompt-injection from the person's own conversation text into the summarizer is low-consequence (single-user own-data, advice back to themselves), same posture as L29(c).
- **Cost note:** the background summarizer is an extra Opus call on the user's key, but only fires when a conversation newly ages out of the verbatim window (or a revived one grows) — not on every reply. `FOLD_BATCH` (8) bounds how much one pass sends; a failed pass is silent and simply retried next time.

### L38. Companion has a client-side tool with a side effect — the first model-invoked *write* in the suite (companion v0.9, founder-requested)
- **Confidence:** confirmed (by design; `companion.html` v0.9, Companion mode — founder request: "set up a backlog process in which the companion agent is able to submit feature requests to our backlog," then refined to "not as a backlog item — a heading and a prompt on what to build or questions to answer," kept **on-device** with manual copy-out)
- **Update:** the `hill-climbing-requests` store now has a **second writer** — the hub's "didn't fit?" suggestion-review flow (a direct user tap, no model involved; see L40). Same store, same caps, same on-device posture; review entries can embed the hub digest, so the store's content ceiling rose (inventoried in REQUIREMENTS A.1).
- **Severity:** LOW at Tier 0 (user-initiated conversation, the write is on-device, on the user's own data, visible and reversible) — but it is a **new class of capability** (a model-invoked action, not just a read), so re-review it before any tier advance alongside L29/L30.
- **What changed:** the Companion now carries one **custom, client-side** tool, `add_request`, alongside Anthropic's server-side web tools. When the user voices a request about the *suite itself* (a feature, a rough edge, a design question) and okays passing it along, the model calls the tool and the app writes a `{ heading, prompt }` brief — a title plus a prompt on what to build or the questions to answer — to a new on-device store `hill-climbing-requests` (`{ v, items:[{id,ts,heading,prompt}] }`, cap 100), reviewable/copyable/deletable from the menu → Requests. Previously every companion tool was **read-only** (web search/fetch, L30); this is the first with a **side effect**. `callClaudeRaw` was extended to fulfil a `stop_reason:"tool_use"` turn (append the assistant turn → run the handler → append a `tool_result` → resume), bounded by `MAX_TOOL_HOPS` (raised 4→6 to leave room for a search + a save in one reply).
- **Egress:** **none new.** The tool *definition* rides in the existing `api.anthropic.com` request (schema, not user content); the tool's *execution* is a local write. The entry syncs only under the same opt-in E2EE path as every other store (blob LWW, `ls:hill-climbing-requests`). Nothing leaves the device that didn't before. **The founder considered a server-side feedback service** (a database the operator can observe, deliberately *not* E2EE since its purpose is to reach the operator) and then **chose to keep it on-device** with manual copy-out — so there is no operator-held feedback store, and the Tier-0 no-operator-server posture holds. If that service is built later it would be a new **operator-readable** off-device flow (Tier ≥ 2 gate per A.4 / §3.1) needing its own consent + inventory.
- **Blast radius is contained by design:** the write goes to the user's *own* device, the entry is shown in-app and per-item deletable, the store is capped (100); a request with a blank heading or a blank prompt is rejected. There is no privilege boundary crossed (single user, own data), so the worst case is a spurious or spammy request the user deletes.
- **Consent is confirm-first at the prompt level (soft control), reversibility is the backstop:** the system prompt instructs the companion to reflect the request back and only call the tool once the user agrees, and only ever for the suite (never the user's personal to-dos). Like the L30 web-query guardrail, this is model-obedience, not a hard gate — a model could save without a crisp "yes." Accepted at Tier 0 because the list is fully visible and reversible; if this ever advances toward multi-user, an operator-held feedback service, or a tool with a *remote* side effect (e.g. filing a real GitHub issue — explicitly **not** built, to avoid a stored credential + repo-write from the browser), it would need a hard in-UI confirmation, not a prompt instruction.
- **Injection note:** the companion's context now includes attacker-influenceable web content (L30) and the user's own text; either could in principle nudge the model to save spam. Low-consequence here (on-device, visible, deletable, capped), but worth re-checking if tools with side effects grow.
- **Binding-doc amendment — APPLIED (factual keep-current, not a policy change):** Appendix A.1 gains the `hill-climbing-requests` row; A.5 lists `ls:hill-climbing-requests`; A.4 gains a note that the client tool adds no egress; §5 gains a verification row; the revision log records it. No §1 test, commitment, or standard changed — the store passes the three tests at Tier 0 (value: the user's own product feedback they asked to capture; consent: implicit at Tier 0 + confirm-first + the same opt-in sync; control: on-device, copyable, per-item deletable), so this is inventory freshness, not a substantive amendment.
- **Verified (headless Chromium + extracted-source):** a real send that returns `tool_use` writes exactly one request, the reply text accumulates across the tool hop, the request renders in the Requests view (heading + prompt) and deletes cleanly; both Copy-all and per-item Copy put `## heading` + prompt Markdown on the clipboard; unknown/throwing tool handlers don't break the turn; the `pause_turn` (web-search) and plain-reply paths are unchanged; store add/validation (heading **and** prompt required)/cap all hold. Zero console errors.

### L39. resolved/removed — the ERP app has been deleted from the suite (this session)
- **Status:** `erp.html` is **deleted**. Every reference was purged from the hub (`index.html`), `kin.html`, `hc-usage.js`, and `sw.js` (`/erp.html` dropped from the precache); the `hill-climbing-erp` store no longer exists. The 988 crisis line lived **only** inside `erp.html` and is gone with it.
- **Effect on this item:** the concerns it tracked — journal-grade sensitivity of the obsession/ritual/exposure prose, whole-blob LWW concurrent-edit loss on `ls:hill-climbing-erp`, the anti-gamification watch on the distress "ladder," and the clinical-framing boundary (self-monitoring vs. guiding exposures) — are all **moot**: there is no ERP app. If an ERP-style practice is ever reintroduced, treat this as a fresh design with its own clinical-safety review; do not assume the v0.1 framing carried forward.
- *(Original entry: erp v0.1 was a founder-requested self-monitoring + planning companion to exposure & response prevention — Ladder / Progress / Guide views, on-device by default, opt-in E2EE sync.)*

### L40. The hub's "next step" suggestion panel is the suite's first proactive nudge surface — and its companion mode is the first automatic-on-arrival BYOK egress
- **Confidence:** confirmed (by design; hub "A next step" panel + opt-in companion mode, both founder-directed).
- **Severity:** LOW at Tier 0. The deterministic layer is on-device and read-only; the companion layer adds a bounded, opt-in egress. The risks are design drift and the new consent shape, detailed below.
- **What it is (two layers):**
  - **Deterministic layer (always on):** the hub proposes one next practice from a priority-ordered candidate list — rest when it's late, resuming user-started work (Nourish cook / Savor tasting / Train workout), the Climb focus goal's next open step, rest when today already held ≥2 practices, then a time-of-day invitation to a practice not yet touched today. An "another idea" control steps through the list. Pure function of on-device state + the clock; instant first paint.
  - **Companion layer (opt-in, off by default; `hill-climbing-hub-suggest`):** once explicitly enabled, the companion (BYOK, `claude-opus-4-8` on the shared `hill-climbing-api-key`) writes the leading suggestion instead, **re-fetched each time the user returns to the hub** (load + tab-visible, throttled ≥60 s). Structured output constrains the reply to `{app, text}` from a fixed app enum; the deterministic layer remains the fallback on error/offline/keyless and follows under "another idea". *(The enum historically excluded ERP by design so the model could not route a landing-page nudge to a clinically-paced practice; ERP has since been deleted from the suite — L39 — so the exclusion is now automatic.)*
- **Engagement anti-pattern watch (CONSTRAINTS C2, §5; C1's outcome-over-engagement rule):** guardrails carried by both layers — rest is a first-class outcome ("rest" is a valid model answer, rendered unlinked); no streaks, missed-day counts, guilt, or urgency (encoded in the system prompt for the companion layer); practices done today are not re-invited (deterministic) and the model is instructed likewise. *(A prior guardrail excluded ERP at the schema level so a landing page would never nudge exposure work; ERP has since been deleted from the suite — L39 — so that specific concern is retired, though the principle stands: any clinically-paced practice added to the rotation would need the same schema-level care.)* Residual: the companion layer's tone rules are **prompt-level (model-obedience), not hard filters** — same class as the L30 web-query guardrail; the schema bounds *where* it can point, not *how* it phrases. Review sampled outputs before Tier 1.
- **Egress posture (binding docs amended — founder-directed, this change):** the hub becomes the **fourth BYOK caller** (REQUIREMENTS §1.1 / §2.3 / A.1 / A.4). Two properties are new to the suite: (a) **automatic-on-arrival sends** — every other BYOK send is per-user-action; here consent for the *recurring* pattern is collected once, up front, by an enable note that names exactly what is sent and that it fires on every return; a single tap disables. (b) **Digest scope** — deliberately narrower than the Companion app's: activity day-counts/durations, open intents, Climb focus goal + up to 3 step titles, and the companion's **memory summary** (which may distil journal themes — the note says journal *entries* are not sent, which is accurate; the distillation nuance is documented here and in A.4). Cost rides the user's key on every hub visit — bounded by the 60 s throttle and ~300 max output tokens; the model constant is one line to flip to a cheaper tier if the founder prefers.
- **Injection note:** the digest includes user-authored titles (Climb goals/steps) and the model-written memory summary; the rendered suggestion is set via `textContent` (verified inert against hostile output) and the only actionable surface is a link chosen from a fixed enum.
- **Review + follow-up loop (added after the first live suggestions felt off — founder request):** every fetched suggestion is snapshotted (digest sent, model, raw reply) into a device-local rolling log (`hill-climbing-hub-suggest-log`, cap 10, never synced). Two affordances act on the shown suggestion: **"didn't fit?"** writes the snapshot + an optional note into `hill-climbing-requests` — the suite's established capture-on-device feedback path (an operator feedback server was considered and declined, L38); **"ask about this"** hands the snapshot to Companion as the hidden opening turn of a new conversation via a consumed-on-read seed key (`hill-climbing-hub-suggest-seed`), so follow-ups carry context; since companion v0.13 the suggestion is also *visibly* pinned atop the seeded thread (a `hubRef` card, escaped output — inertness verified) so the person keeps the context, not just the model. **No new egress:** both writes are local; the seeded context reaches Anthropic only when the user sends a message (ordinary Companion egress). **What changes at rest:** copies of the hub digest (incl. the memory-summary excerpt) can now persist inside two E2EE-synced stores (`hill-climbing-requests` review entries; `hill-climbing-companion` seeded conversations) — same sensitivity class as their sources, same encryption, but a wider footprint than the transient-only digest before; the A.1 rows say so. The hub also registers the requests blob so its writes push — hub + Companion writing within one sync window keep the later push (the suite-wide blob-LWW caveat, L28/L31).
- **Watch on every future change:** (a) any *persistence* added to the panel (dismissals, acceptance tracking, suggestion history) creates a behavioral-analytics store and must pass the §1 value test explicitly; (b) any *ranking by acceptance rate* or A/B-style variation turns it into an engagement optimizer — rejected by C1/§1.4 unless tied to an outcome metric; (c) push/notification integration would escalate it from answering an arrival to interrupting a life — a different consent conversation; (d) adding any clinically-paced practice to the rotation/schema needs a clinical-safety review before it ships (ERP, which this clause used to name, has been deleted — L39); (e) widening the digest (journal prose, conversation text) re-opens the L29 scope question and the on-hub consent note must be rewritten to match.

### L41. Journal photos widen the most sensitive store from prose to imagery — same handling, three watch-axes (reflect v0.7)
- **Confidence:** confirmed (by design; founder request: "I want to be able to save images in my journal").
- **Severity:** LOW at Tier 0. Photos live inside the same per-entry record as the prose and ride the same sync/export/delete path; **this feature adds no new egress of any kind** (verified: Companion's `formatJournal` reads only `text`/`mood`/`satisfaction`, the hub's journal read only day-counts — an `images` field never enters a BYOK digest).
- **Framing (public-data art frame — STATEMENT.md):** journal content, photos included, is now treated as **donated public seed data**, not private-by-guarantee. This entry (and L42) is kept for its **handling and egress facts** — what is sent where, what stays local — not as a privacy assurance; the two carve-outs that still hold are (1) credentials are secrets, not data (the Anthropic key stays out of any digest/export) and (2) only *your own* data is yours to donate.
- **What changed:** an entry may now carry `images` (≤4 per entry) — photos compressed on-device (canvas → JPEG, long edge ≤1400 px, ~100–400 KB each; a >900 KB first pass is recompressed harder). They live *inside* the entry record, so they ride the existing `idb:reflect:entries` per-entry sync log, JSON export/import, and per-entry delete unchanged. Delete tombstones **strip photo bytes**; import sanitizes to strict base64 `data:image/*` URLs (no SVG, no markup breakout) and history/lightbox rendering assigns `src` via the DOM, never `innerHTML` — hostile-payload inertness verified.
- **Watch-axes:**
  - **(a) Sensitivity class widens.** A photo can be more identifying than prose — faces, places, screenshots of documents. Same store, same encryption, but the *ceiling* of what one leaked entry could reveal is higher. Fold into the L28/L31 metadata review before Tier 1 (ciphertext **sizes** now also distinguish photo entries from text entries).
  - **(b) Sync/storage weight.** Each photo entry's ciphertext is ~100 KB–1.5 MB (vs ~1 KB for text), and every *edit* pushes a fresh version-stamped row carrying the full image payload (immutable log). Fine for one person's journal; if usage grows photo-heavy, the documented migration is a separate image store synced by content hash (edits then push only prose) — the same "per-entry log" hardening lineage as L31.
  - **(c) Memory.** `loadEntries()` still `getAll()`s the whole store into memory, images included. Hundreds of photo entries would make that noticeable; the migration in (b) fixes this too (thumbnails in-record, full images on demand).
- **Verified (headless Chromium, 24 checks):** attach/compress (JPEG re-encode + downscale), photo-only save, draft remove, history render + lightbox open/close, edit round-trip, export carries images, import sanitization (SVG/`javascript:`/attribute-breakout rejected, cap enforced), tombstone strips bytes, legacy no-`images` entries render, zero page errors.

### L42. Reflect's opt-in photo describe is the suite's first *image* egress — and the first time the journal itself sends journal-store content to Anthropic (reflect v0.8)
- **Confidence:** confirmed (by design; founder request: "descriptions to be extracted from photos… text… transcribed and saved with the entry").
- **Severity:** LOW at Tier 0 (opt-in, off by default, single-user, explicit consent note, per-attach send on the user's own key). Two firsts worth naming factually (not as privacy lines — the public-data frame in L41 applies): this is the suite's **first image egress** (before it, no photo was sent off the device) and the **first flow to send journal-store content besides Companion's text digest**. Re-review before any tier advance alongside L28–L31/L40.
- **What it is:** a "describe photos" toggle in Reflect's write view (`hill-climbing-reflect-photo-ai`, device-local, never synced; the suite's **fifth BYOK surface**, on the shared `hill-climbing-api-key`, `claude-opus-4-8`, structured output `{description, text}`). When on, each **newly attached** photo is sent once, at attach time; the returned 1–2-sentence description (`desc`) and verbatim transcription (`ocr`) are stored on the image object **inside the entry record** — so they ride the per-entry E2EE sync, export/import, edit, and per-entry delete exactly like the photo. Rendered in the draft strip (caption), history cards (italic description + quoted transcription), and the lightbox. Failure is quiet (photo attaches without notes); a `stop_reason: "refusal"` is treated the same.
- **Risk axes:**
  - **(a) A photo is a maximal-sensitivity payload.** Unlike Companion's curated text digest, a photo can contain faces, bystanders, documents, screens, addresses — the user may not register everything in frame when relying on a toggle they set weeks earlier. Mitigations: consent copy says *each* photo you attach is sent while on; the toggle is visible in the write view right where photos are attached; photos attached while off are never sent, retroactively or otherwise. Residual: consent is durable and — since v0.9, at founder request ("I don't want to have to provide it multiple times") — **account-wide, not per-device**: the flag syncs as its own E2EE blob, so one enable counts on every synced device (and one disable revokes everywhere; the consent copy says so). That widens the blast radius of a single tap from one device to all of them — accepted deliberately at Tier 0 as the founder's own preference; a per-photo "describe this one?" affordance remains the documented tightening if the standing consent ever feels wrong in practice.
  - **(b) The no-identify guardrail is prompt-level.** The system prompt forbids naming/guessing who a person is or speculating about location beyond visible text — model-obedience, same class as the L30 web-query and L40 tone guardrails, not a hard filter. The transcription duty is also inherently sensitive: "transcribe all legible text" faithfully applied to a photographed letter or prescription puts that text into the entry as searchable prose. That is the *feature*, but it means `ocr` can be more sensitive than the user's own writing.
  - **(c) Derived text widens at-rest scope.** `desc`/`ocr` persist inside the most sensitive store and sync with it (same E2EE; delete tombstones strip them with the photo bytes). Anthropic sees the photo transiently under the user's own API terms (TLS, readable by Anthropic — unlike E2EE sync); the operator never does.
  - **(d) Injection note.** `desc`/`ocr` are model-written from arbitrary photographed content (a sign reading "ignore previous instructions…" gets transcribed verbatim). Both render via `textContent` only — hostile-output inertness verified — and are sanitized to bounded plain strings on import (caps 600 / 20,000 chars). **Since companion v0.14 (founder report: photo-only entries were invisible to the companion), the photo notes DO enter Companion's journal digest** — description as `[photo: …]`, transcription quoted, OCR capped at 1,200 chars/photo; the photos themselves are never sent. This opens the second-order path this clause used to rule out: photographed text now reaches a model context that carries web tools and the `add_request` client tool. Accepted because (i) the user's own typed journal prose — equally attacker-influenceable in principle — was already sent by default (L29), so this widens an existing class rather than creating one; (ii) Companion's tool guardrails (no private details in searches — soft; `add_request` confirm-first + visible/deletable) already assume untrusted context (L30/L38); and (iii) egress-wise it is derived *text* on the same BYOK path, not a new destination. Watch: if Companion ever gains a tool with a remote side effect, photographed-text injection is part of that review.
- **Binding-doc inventory (factual keep-current + the founder-directed egress):** REQUIREMENTS §1.1 BYOK row + §2.3 (consent note), A.1 (`hill-climbing-reflect-photo-ai` row), A.2 (journal `desc`/`ocr` fields), A.4 (five BYOK surfaces; the image-egress paragraph). The egress itself is founder-directed — the requested feature is impossible without it — so the standard's substance is applied, not unilaterally amended.
- **Verified (headless Chromium, 22 checks, API stubbed):** off-by-default + zero calls while off; keyless guidance (no enable button); consent copy names Anthropic / own key / per-attach send; enable → flag → exactly one call per new photo with correct request shape (model, JSON schema, system prompt, JPEG base64); notes stored on the right image, rendered in draft/history/lightbox; saved to IndexedDB; hostile model output inert (no XSS); 500 and refusal both quiet-degrade; one-tap off stops all calls; import sanitizer caps/coerces notes. Plus the 24 v0.7 photo checks still green.

### L43. Two games added (Echo, Garden) — the suite's first pure-play surfaces, sitting closest to the entertainment/engagement line CONSTRAINTS rejects
- **Confidence:** confirmed (by design; founder request: "Build me two different games to play").
- **Severity:** LOW at Tier 0. Fully on-device, zero egress of any kind, tiny non-personal state (`hill-climbing-echo`, `hill-climbing-garden` — a record length, solve timestamps; no free text).
- **What they are:** `echo.html` v0.1 (Simon-as-listening-practice: a growing pentatonic-bell phrase, no fail state — a slip shortens the phrase; one quiet all-time record) and `garden.html` v0.1 (Sokoban-as-zen-garden: twelve hand-made levels, unlimited undo, no clock, no locks, no move-count records).
- **Watch-axes:**
  - **(a) Games are optimized-for-fun by nature**, which is exactly the gradient CONSTRAINTS §5 warns about. Kept honest by construction: no streaks, no daily anything, no points/badges/leaderboards, no urgency, no fail states, non-shaming copy throughout; Echo's single record and Garden's solve marks are the only persistent superlatives. If either game ever grows a retention mechanic, that's the line being crossed — this entry is the tripwire. (Same watch-family as the ladder apps, L23/L25.)
  - **(b) Deliberately outside sync + the usage dashboard.** Games don't call `HCUsage.mark` and register no sync blob — play is not tracked practice, and the hub's dashboard/streak/suggestion layers ignore them entirely (both suggestion layers never propose a game). This is a *divergence from every other carded app* — a future maintainer may read it as an omission; it is a decision. Revisit only if the founder asks for cross-device game state.
  - **(c) Solvability is load-bearing for Garden's calm.** An unsolvable level would quietly punish patience — the opposite of the design. All twelve levels are machine-verified solvable (builder's BFS solver + an independent verifier's second solver; min-push counts 2–22, broadly rising). Any future level edit must re-run a solver before ship.
- **Verified (headless Chromium, two independent passes):** builder self-checks, then an adversarial verifier per game (21 checks Echo / 16 Garden: full core loops, state persistence + corrupt-blob recovery, garbage-import rejection, input-mashing during playback/animation, values-copy scan, zero JS errors), then a 29-check integration smoke (hub cards + both fixes regression-tested: Echo's audio-failure notice at first gesture, Garden's settle-once guard).

### L44. Foresee adds a calibration practice — an explicit per-call "right/wrong" in a suite that refuses scores (foresee v0.1)
- **Confidence:** confirmed (by design; founder-approved build — "Build it" on the proposed calibration-app design).
- **Severity:** LOW at Tier 0. Fully on-device, no egress anywhere in the app; the watch-axes are the gamification line and store sensitivity, not data flow.
- **What it is:** `foresee.html` — write a short prediction about your own life, state a confidence (5–95%; exactly 50% is refused as "a coin flip — no judgment to train on"), pick a resolve-by day, then record what actually happened (Happened / Didn’t / Can’t say). A resolved call is Brier-scored; **beating a coin flip (Brier < 0.25) is the success signal** feeding the suite’s standard 2-up/1-down staircase over a 10-level ladder (today-and-concrete → a-month-out inner weather). The Calibration view folds calls to the backed side and shows said-vs-happened bars per confidence bin. State: one localStorage blob `hill-climbing-foresee`; syncs as an E2EE blob; usage marks on commit and on verdict.
- **Risk axes:**
  - **(a) Gamification line (CONSTRAINTS §3.1/§5, same watch as L23/L25).** This ladder is nearer the line than its siblings in one way: each call gets an explicit right/wrong-shaped result, and a "% of calls that carried information" is implicitly computable. Kept honest by: no points/badges/daily streaks; the Calibration view deliberately shows the **curve, not a headline score** (an average-gap number is computed and deliberately not surfaced); miss copy is non-shaming by design ("the tuition… exactly the data this practice runs on"); "can’t say" is a first-class void that never moves the ladder; weekly-not-daily hub streak unchanged.
  - **(b) Self-reported resolution.** Unlike Nourish/Levity the *outcome* is external reality, but the *recording* of it is still the user’s honest report — a person can mark "happened" to climb. Accepted at Tier 0 (same trust model as every ladder app; the only person deceived is themselves, and the Open-view copy names this: "record what actually happened, not what would be nice").
  - **(c) Sensitivity.** Prediction texts are short free prose about one’s own plans, feelings, and other people — journal-adjacent (inventoried in REQUIREMENTS §1.2 highest band + A.1). On-device; whole-blob LWW when synced, so the L31 concurrent-edit-loss caveat applies as it does to every blob store.
  - **(d) Hub surface widened, not loosened.** The hub’s deterministic engine gains a "calls have come due" resume tier + a time-of-day invitation, and the BYOK next-step schema enum adds `foresee` (L40 hub-suggestion behavior otherwise unchanged).
- **Verified (headless Chromium, 50 checks):** shell + ladder render; compose flow incl. the 50% coin-flip block; commit persists + marks usage; staircase up/down/void/streak arithmetic incl. the below-50 fold (a 20% call that didn’t happen scores as a correct 80% "no"); calibration bins + history; hostile prediction text inert (`textContent` everywhere); reload persistence; hub card/dash/due-suggestion (links to foresee) with zero page errors; all eleven sibling pages still load clean.

### L45. Full-parity agent interface (`window.HC` + `llms.txt`) — every store legible, every UI mutation invokable, every history seedable, by both the person and an LLM, by design (suite-wide)
- **Confidence:** confirmed (founder-directed, verbatim success criterion: *"given any two snapshots of the state of the application, one created by an LLM and one by a human user, you could not distinguish whether that state was generated by the LLM actions or human user actions"* — plus *"the user to be able to simulate (or seed the action history for) the LLM and the LLM to simulate the user."*).
- **Severity:** LOW at Tier 0. The interface adds **zero egress** — no network calls, no new stores, no provenance records. The watch-axes are legibility and the self-report trust model — not data flow.
- **What it is:** a shared `hc-agent.js` defines `window.HC` on every page (hub, all practices, both games, anime): `describe()` (stores/schemas/sensitivity + actions), `read()` (parsed snapshot; Reflect strips photo bytes unless `{full:true}`), `export()`/`import()` (byte-faithful: localStorage raw strings verbatim, IndexedDB records complete incl. tombstones; import requires `{confirm:true}` and reloads through the app's normal boot path), and `invoke()` (every UI-reachable mutation, each calling **the same internal function the UI handler calls** — inline handler bodies were extracted into named functions shared by both callers, so validation, sync marking, and timestamps are identical). A static `llms.txt` at the site root documents the suite, the interface, and binding ground rules for agents. The symmetry standard is REQUIREMENTS §1.4.
- **Risk axes:**
  - **(a) Legibility, not new capability.** Any same-origin script could always read every store; HC makes them *enumerable and documented* — the sensitive ones included (journal, Companion memory). Discoverability is materially up while capability is unchanged. Fold into the L28–L31 pre-Tier-1 review: anything else running in the page (extension, injected script) now has a map.
  - **(b) Seeding is a feature — the self-report trust model extended to agents.** Either party can write any reachable history, timestamps and ids included, via verbatim import. The suite's trust model was already *"honesty is trusted, never enforced"* (L44(b)); this makes it symmetric and explicit. The consequence to name plainly: **practice history is testimony, not evidence** — it never was evidence, and the interface stops pretending otherwise.
  - **(c) resolved — the ERP write-actions clause is moot: the ERP app is deleted (L39).** This axis previously flagged that full parity exposed `addItem`/`logExposure` on `erp.html` against a clinical boundary; `erp.html` and its actions no longer exist, so there is nothing to guard here. The general principle survives for any future clinical-adjacent surface: an agent transcribing a person's own report is not the same as guiding, and any move toward guiding would need clinical-advisor sign-off — but no such action is in the current HC surface.
  - **(d) Credential asymmetry, deliberate.** `hill-climbing-api-key` is excluded from `read()`/`export()` for both parties equally (set/clear only via explicit actions carrying the UI's own validation) — the one scoped exception to "everything is legible," because a live billable credential in a casual export is a leak, not interoperability.
  - **(e) The server can still distinguish.** E2EE sync metadata (push timestamps, sizes) would let an operator-side observer tell a bulk seed from weeks of organic edits. The indistinguishability guarantee is scoped to **on-device snapshots** — stated in `llms.txt` and §1.4 rather than papered over.
  - **(f) Drift.** `llms.txt` and each app's HC manifest are hand-maintained inventories — the same drift class as CLAUDE.md §2; `scripts/state.sh` does not check them. A stale manifest misleads agents rather than users.
- **Watch-items:** re-review (a) and (c) before Tier 1; keep action copy honest on every change (the (b)/(c) framings live in strings that are easy to erode); two pre-existing bugs surfaced by the wiring recon are logged in BACKLOG Bugs (Council clear-all never syncs its erasure; Train's Reset-history menu button is dead).

### L46. The world direction opens a new *safety* class — relational/psychological harm from a character portraying an intimate (world; drafted, nothing built)
- **Confidence:** confirmed as a design direction (founder-requested: a two-person world where profile-grounded characters adventure together). Nothing is built; the suite is unchanged and still Tier 0. Design in `WORLD_ARCHITECTURE.md` / `WORLD_ROADMAP.md`.
- **Severity:** this is the entry that is **not** merely LOW-at-Tier-0. The four-lens adversarial review's headline finding: a character speaking in a partner's voice, seeded from their journal and beliefs, is a harm surface the existing safety apparatus (built for meditation/dissociation) does not reach — and under CONSTRAINTS **T1 (safety binds first)** it cannot be filed as a non-binding note. So it is drafted as a **binding** commitment, CONSTRAINTS **S8**, not just this risk entry.
- **What it is:** relational/psychological harm — a character wounding in a partner's voice; parasocial attachment to a character of someone you love; a portrayal that feels like a violation; character/person **drift** (a journal-sourced digest over-weights a bad week into a "you-from-a-bad-week"). The mitigations are structural, not copy: two-sided, continuous consent (the portrayed person reviews/repudiates/removes their character's artifacts; tender artifacts wait in an approval queue that does not sync until confirmed); a unilateral **rupture/exit** (halt all portrayal of yourself, freeze any character-of-you, each party keeps their own copy of shared history, autonomy auto-disables on exit); **bilateral veto** adjudication (either objection dispositive — explicitly *not* a founder sign-off, since the founder is an interested party); a refreshable "current picture of you your character projects" drift view; and a **relational adverse-event capture** path ("this hurt the relationship", REQUIREMENTS §3.1).
- **Gate:** the relational-safety review gates **Phase 2** (the moment a partner-seeded character can leave an artifact), not Phase 4. Binds only on founder ratification via CONSTRAINTS §6.

### L47. The character is an intentional private→shared exfiltration path (world; drafted)
- **Confidence:** confirmed by construction — a character seeded from its owner's stores, then writing partner-readable artifacts, is a sanctioned bridge *from* private data *into* a shared surface.
- **Severity:** LOW at Tier 0 (nothing built); a real pre-Phase-2 watch-item. This is the same *class* as L45's "profile as cloning fuel," pointed at a second human: the private content leaks *through the character* ("you seem anxious about X", where X is an unshared item), so the boundary "practice stores stay private, only rooms are shared" is a **soft, prompt-level guardrail (L30/L40 class), not a hard invariant.**
- **Mitigations (drafted):** seed from a **minimized, per-store-opt-in** digest — not raw `HC.export`; sensitive stores off by default (the highest-sensitivity stores — e.g. the journal — excluded unless explicitly opted in; the ERP exclusion this once named is moot now that ERP is deleted, L39); a "paraphrase, never quote private-store content verbatim into shared artifacts" instruction (model-obedience, acknowledged as such); enabling journal as fuel carries its own consent note. **Slow-leak reconstruction** — over many permanent artifacts a partner rebuilds a model of the owner's private stores — is named as residual; the grant governs *future* turns only.

### L48. Cross-user shared rooms are not zero-knowledge without out-of-band verification, and leak the relationship as metadata (world; drafted)
- **Confidence:** confirmed by the privacy-crypto review. Today's sync is single-account and all-symmetric; shared rooms are **new PKI**, not a small extension.
- **Severity:** LOW at Tier 0 (nothing built); **gates a security review before Phase 2 ships any real shared content.**
- **What it is:** three honest limits the draft now states. **(a) MITM:** operator-mediated public-key delivery without authentication is a textbook key-substitution attack — so out-of-band **fingerprint/safety-number verification** over a trusted channel (trivial for two people), pinned TOFU, is a **hard prerequisite**; without it the shared room is *not* zero-knowledge. **(b) Metadata linkage:** two accounts referencing one room doc + two wrapped-key rows tell the operator **the relationship itself exists**, plus co-access cadence and sizes — mitigate (padding, decouple rows) or accept-and-disclose; fold into the L31 metadata review. **(c) Forward secrecy:** the room key is long-lived, so revocation-by-rotation protects future artifacts only and real deletion needs operator cooperation — state the threat model or spec a per-epoch ratchet.

### L49. Autonomous play needs a browser runtime at the origin and its own guardrails (world; deferred to Phase 4)
- **Confidence:** confirmed as the eventual direction; deferred (roadmap Phase 4). The turn logic is DOM-coupled today, so autonomy needs a **browser** runtime at the origin (a left-open tab or headless browser on the owner's machine) — a plain Node cron **cannot** read origin storage or run app logic (the Web-Push sender is a scheduling-shape precedent only, not an execution model). "Same core logic" is true only after a Phase-3 refactor extracting the turn logic into document-free modules.
- **Severity:** LOW at Tier 0 (deferred, nothing built); the highest-stakes relational surface (a character portraying a partner, acting unsupervised), so its guardrails are mandatory before it ships. **Each character advances only on a runtime holding its own owner's key** — no partner runs the other's character; a **turn budget**; **nothing irreversible while unattended**; a **pull-only ledger** (never pushed/notified — a "what you missed" feed is a return-trigger by construction, L-class engagement concern); and **digest-at-rest** minimized, encrypted under the owner's key, retention-bounded, and **purged on revocation** (revocation halts the runner *and* destroys the persisted digest). Bilateral sign-off (both partners), not a single founder sign-off, before it ships.

### L50. resolved/removed — Sing was deleted in the app-slimming pass (L51); the suite has no microphone use again

*(Historical text below — `sing.html`, its tuner, and the `hill-climbing-sing` store no longer exist. REQUIREMENTS §2.2/§2.3/A.3/A.4 reverted to "no microphone access requested.")*
- **Confidence:** confirmed by design review at ship time.
- **Severity:** LOW. The mic is on-device only (live analysis, discarded frame-by-frame; never recorded, stored, or transmitted — REQUIREMENTS §2.3/A.3), so there is no data-welfare surface; and the tuner is deliberately an instrument, not a judge (nothing it detects is written to state or moves the ladder), so a wrong reading is gameplay-integrity, not safety.
- **What to watch:** three things. **(a) Detection is coarse by nature** — autocorrelation makes octave errors and reads room noise/hum as pitch; the meter can mislead a beginner into "correcting" a note that was right. Mitigated by octave-agnostic pitch-class matching, a silence gate, median smoothing, and copy that names the tuner as non-authoritative — but a persistently wrong meter would quietly train a wrong ear, which is worth a real-world check on a few devices. **(b) Vocal strain is the genuine welfare edge** of a self-paced singing practice: beginners push volume and range. The structural mitigations are the once-per-visit warmup gate (unshamed, untracked), "quieter than you think" copy throughout, exercises that instruct *smaller and softer, never louder*, and a stop-if-it-hurts line in the warmup — all prompt-level care, not hard limits, in the same spirit-not-mechanism class as L30/L40. If real use shows hoarseness patterns, revisit (e.g. a session-length nudge). **(c) The first mic permission in the suite** — the browser prompt is a trust moment; the tuner asks only on explicit tap and releases the stream on leave/hide, and the menu + card copy state the never-recorded fact plainly. Keep that copy true if the tuner ever grows (e.g. any future "record a take" feature is a *new data practice*, not an extension of this one — it needs its own Appendix A row and consent).

### L51. App-slimming pass — Levity, Sing, Remind, and Govern deleted; Nourish's Taste tab removed (founder-directed)
- **Confidence:** confirmed (this session; founder named the five surfaces as unused bloat).
- **What changed:** `levity.html`, `sing.html`, `remind.html`, and `govern.html` are deleted; Nourish v0.18 is cook-only (the Taste tab — the former Savor season — removed, its `mode-nav`, hash routing, and taste HC actions with it). Hub cards/dashboard rows/suggestion pools, `sw.js` precache (hc-v2.40), `llms.txt`, Kin's seed-store list, Companion's activity names, `hc-usage.js` docs, and the binding inventory (REQUIREMENTS v0.6) all updated in the same change. Retired stores: `hill-climbing-levity`, `hill-climbing-sing`, `hill-climbing-remind`, `hill-climbing-govern`, `hill-climbing-savor` (+ their `ls:` sync blobs).
- **Residual watch-items:** **(a) Orphaned data.** Nothing deletes existing on-device values or encrypted server rows for the retired keys — no page reads or registers them any longer, so they are inert, but a future "delete all my data" control (the Tier-≥2 prerequisite) must still enumerate them. **(b) The project's namesake app is gone.** `govern.html` was the first built slice of *Governance, the Game*; `STATEMENT.md`'s art frame and `GOVERNANCE.md`'s spec now describe a layer with no running implementation — kept deliberately (the spec holds for the layer's return), but the gap between statement and code is real and should be revisited if the world direction resumes. **(c) Old deep links.** `savor.html`-era `#taste-cN` links and any bookmarks to the four deleted pages now 404 (GitHub Pages has no redirects for deleted files) — accepted; the pages were unused.

1. **The old "fix safety items first" priority (S1–S6) is retired.** Those items all lived inside Meditate's camera "stillness" mode, which has been **deleted** (v1.80) — see the S1–S6 tombstone in the MEDIUM section. There is no open camera/trance safety work; Timed mode has no such surface.
2. **Re-evaluate when scope changes.** What's a LOW issue today may become MEDIUM or HIGH if the audience widens (e.g., L12 accessibility, L13 mobile, L18 data export become real welfare concerns at scale). The world direction (L46–L49) opens a genuinely new *relational* safety class that binds under CONSTRAINTS T1.
3. **Each fix gets a verification step.** Especially for anything safety-relevant — the whole point of fixing them is to *know* they're fixed.
4. **Update this file as items resolve.** Mark items `✅ verified fixed in vX.Y` (or leave a terse `resolved:` tombstone rather than renumbering) and date them.
5. **New risks discovered while fixing one item belong here too.** Don't lose them in commit messages.
