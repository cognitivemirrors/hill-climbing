# Hill Climbing — Observation Backlog

A flat list of observations, bugs, ideas, and open decisions. Add an item the moment you notice it; triage at session boundaries. Cheaper to keep than to recover from "I had a thought during a round and now I can't remember it."

Sections are categories, not status. Status is a leading marker on each item:
- `[ ]` — open
- `[~]` — in progress
- `[x]` — done (kept for history; archive periodically)
- `[?]` — needs more thought / observation before action
- `[!]` — concern reported via in-app modal, awaits triage

Move items between sections freely. The order within a section is rough priority — top is more urgent.

---

## Inbox

Things noticed but not yet sorted into a category. Empty this regularly.

- [x] **Companion + Council merged into two modes of one app (`companion.html`, v0.5).** Council was a standalone, hub-unlisted file; it now lives inside the Companion app as a second mode, chosen by a top-centre segmented switch (Companion · Council). The switch re-themes the whole page (teal ↔ indigo) by flipping `html[data-mode]`, which drives every accent through the `--accent*` CSS variables; the mode is set in a pre-paint head script (from `?mode=` → `hill-climbing-cc-mode` → default companion) so a Council deep-link never flashes teal. Each mode keeps its own DOM subtree (only one shown), storage (`hill-climbing-companion` synced / `hill-climbing-council` device-local), history, system prompts, and a per-mode secondary nav (Talk·History / Convene·History) + corner menu. Shared: the one Anthropic key, the browser→Anthropic call layer (Companion passes `WEB_TOOLS`, Council none), the key gate, and the crisis footer. `council.html` is now a redirect → `companion.html?mode=council` (hash preserved) so old bookmarks/home-screen shortcuts land in Council mode; added to the `sw.js` precache (**hc-v2.05**) — closes the council-precache half of the APP_SHELL gap below. Verified headless (Chromium/Playwright): mode switch, accent, view routing, key gate, deep-link, persistence, mode-aware menu, and both send/convene async paths render + error-handle with zero page JS errors. Off-device posture unchanged (still flagged vs REQUIREMENTS §1.3/§2.1). Version line unified at v0.5 (was companion v0.4 / council v0.3).
- [x] **Companion: regenerate a response (`companion.html`, v0.4).** A quiet "↻ Regenerate" control on the **last** companion reply drops that reply and re-answers the same preceding turn — for a different take. Restricted to the last reply on purpose (regenerating a mid-thread one would silently orphan everything after it); a mid-thread reply carries no control. Reuses the v0.3 machinery: `regenerate()` pops the trailing assistant turn, persists, re-renders, and calls `respond()` — so the same call + retry path answers the now-trailing turn (works on the hidden "Let it open" seed too). Rendering: `refreshRegenerate()` attaches the control to the last real assistant turn after every render and when busy settles, and self-clears while a reply is in flight, on error turns, or when the last visible turn is the user's (use "Try again" there). The control is DOM-only — never written into `active.thread`, so it never enters the saved/synced conversation. Verified: extracted `regenerate()` passes drop-last-only / hidden-seed / no-op-on-trailing-user / no-op-while-busy tests; v0.3 retry + v0.2 multi-block/pause-turn still pass. sw.js → hc-v2.04.
- [x] **Companion: retry mechanism on failed replies (`companion.html`, v0.3).** A dropped connection surfaced as a bare "*Load failed*" turn and the reply vanished — more likely now that web-search turns take longer to come back (reported from device). Two layers: (a) **automatic** — `callClaudeRaw`'s single fetch is split into `postOnce` / `postWithRetry`, which retries **transient** failures (network drop / 429 / 5xx·529) up to `MAX_RETRIES` (3) with exponential backoff (0.9s/1.8s/3.6s), while **non-transient** errors (401 bad key, other 4xx) fail fast; (b) **manual** — a persistent failure renders an error turn with a **"Try again"** button. `send()` was split into `send()` (pushes the user turn) + `respond()` (does the call); the failed user turn is already stored, so retry re-runs `respond()` and never re-appends it. Empty replies are also treated as retryable-via-button rather than storing a blank bubble. Verified: real extracted functions pass auto-retry / fail-fast-on-401·400 / bounded-429 / 5xx-recovery tests, and the v0.2 multi-block + pause-turn logic still passes after the refactor. Cost note added to KNOWN_RISKS L30. sw.js → hc-v2.03.
- [x] **Companion given internet access (`companion.html`, v0.2).** `callClaudeRaw()` now sends Anthropic's server-side web tools (`web_search_20260209` + `web_fetch_20260209`, `max_uses: 5` each) on every reply, so the companion can look things up — current events, facts, references, checking a claim — on the user's own key. Two correctness changes came with it: the response handler now **concatenates all text blocks** (web-search replies write text before *and* after the search — the old first-text-block grab would have dropped the answer) and **resumes on `stop_reason: "pause_turn"`** (up to `MAX_TOOL_HOPS`, re-sending the partial assistant turn). System prompt gains a "You can look things up" section with a hard privacy guardrail — search with general queries only, never the person's private details / journal lines / situation specifics — and the "send it anywhere" line now explicitly covers web searches. Disclosure updated honestly across lede, setup card, menu note, composer note, *What the companion sees* panel, and About. Verified: real `callClaudeRaw` extracted from the file passes multi-block/pause-turn/plain-reply behavior tests; both inline scripts syntax-clean. New egress pathway + soft (prompt-level) privacy guardrail + web-content injection surface recorded in KNOWN_RISKS **L30** (binding-doc tension with REQUIREMENTS §1.3/§2.1 flagged for founder ratification, not silently amended, per the L24/L27/L29 pattern).
- [x] **Companion added (`companion.html`, v0.1) — a conversational agent with transient access to goals/journal/activity.** New standalone app: a single companion you converse with that reads your *current* Climb goals, Reflect journal, and usage-log activity fresh at each reply, folds them into the system prompt for that one call, then forgets them. Raw data is never written into the saved conversation — only the dialogue is (so the synced blob carries no second copy of the journal; verified). BYOK direct-to-Anthropic on `claude-opus-4-8`, reusing Council's call layer; conversations persist to `hill-climbing-companion` and sync E2E-encrypted via hc-sync (`ls:hill-climbing-companion`, appId `companion`). API key device-local (`hill-climbing-companion-key`, defaults to Council's key), never synced. Transparency: menu → *What the companion sees* shows the verbatim digest. **Surfaced + documented (founder-approved):** listed on the hub with a teal card; added to `sw.js` precache (`hc-v2.01`); hub footer changed "private · on-device" → "private by default". REQUIREMENTS amended + founder-ratified — §1.1 rows (`hill-climbing-companion`, `hill-climbing-companion-key`), §1.3 BYOK-AI bullet, §2.1, §1.4 delete-all scope, §5 verification row. Risk assessment recorded as KNOWN_RISKS L29 (key point: E2EE covers the saved conversation, **not** the LLM send — that's managed by BYOK + explicit gate + verbatim transparency). Tier-1 watch-items live in L29.
- [ ] **Companion: feed the *entire* journal history, aggregated — not just the last 25 entries.** (Founder request.) Today `gatherContext()` sends the most-recent 25 entries verbatim (each truncated ~700 chars); everything older is invisible, so the companion can't see long-arc patterns — a theme recurring across months, a mood trend over a year, "you said this every winter." Want: use the whole history, held to a bounded token cost by an aggregation step. Sketch: keep the most-recent N entries verbatim (recency carries the most signal) and roll up everything older into a compact summary — e.g. per-month mood/satisfaction averages + entry counts, recurring themes/keywords, and a few notable extracts — so the prompt size stays ~flat as the journal grows. Two implementation paths to choose between: (a) **local/statistical** — deterministic, cheap, no extra egress: date-bucketed averages + frequency terms computed in-browser; (b) **LLM rollup** — richer but costs a call and sends more history: a periodic "journal digest" summarization pass, cached (e.g. `hill-climbing-companion-digest`) and refreshed incrementally as new entries land, so old entries are summarized once not every reply. If (b), it widens the L29 privacy scope (more of the journal leaves the device) and must be reflected in the transparency panel and REQUIREMENTS. Keep the *What the companion sees* panel showing the exact aggregate that's sent, whichever path.
- [x] **Single Anthropic API key across the hub (founder request).** Consolidated the three separate BYOK stores (`hill-climbing-council-key`, `hill-climbing-companion-key`, and the `key` inside `hill-climbing-nourish-chef`) into one shared `hill-climbing-api-key`. Each of Council / Companion / Nourish now reads/writes the canonical key via an identical inline helper; legacy keys migrate once (copied to canonical, then deleted) so nobody re-enters; removing the key in any app clears it hub-wide. Nourish's chef blob is now keyless (key scrubbed on migration); its separate consent gate is unchanged. Docs updated: REQUIREMENTS §1.1 (single `hill-climbing-api-key` row + keyless `hill-climbing-nourish-chef` row) and §1.4 delete-all; KNOWN_RISKS L29 (net reduction in credential surface, 3→1). Copy updated in all three apps.
- [ ] **Onboarding copy is stale post-v1.52.** The six-page modal still describes the old game ("hold above a moving threshold," "drop below for half a second or longer and the round ends," "amber tick marks the floor"). Currently invisible because `FEATURES.showOnboardingOnFirstRun` is `false`, but anyone re-enabling it sees a wrong description of the practice. Rewrite to match the gradient-accumulator paradigm before re-enabling.
- [~] **manifest shortcuts are missing levity, council, and climb** (noticed while wiring Train; levity's precache gap was fixed by Climb v1.94). ~~sw.js APP_SHELL is missing council.html~~ — council.html added to APP_SHELL in v0.5 (now a redirect into the merged Companion app). Remaining: decide on the manifest `shortcuts` list — related inventory gaps flagged in KNOWN_RISKS L26.
- [x] **Ratify REQUIREMENTS.md §1.1/§1.3 amendments (v1.77 doc audit).** Added the four un-inventoried localStorage keys (`breathe-session-duration`, `hill-climbing-install-dismissed`, `hill-climbing-nourish`, `hill-climbing-timed-minutes`) plus a `journal` IndexedDB subsection to §1.1; updated the `hill-climbing-usage` row to include Nourish; carved out the opt-in Web Push exception in §1.3/§2.1/§5; widened §1.4's delete-all scope. **Founder-approved** in the v1.77 audit. Detail in KNOWN_RISKS L22.

---

## Bugs

Things that don't work as intended.

- [ ] **Nourish: result copy over-promises at the top of the ladder (L10).** At level 10 with one clear banked, the result screen still reads "One more clean cook at this level and you'll climb a rung" — but L10 is the top, so a second clear only resets the streak with no climb. Cosmetic; reachable only after clearing most of the ladder. Fix: branch the message when `level === LVL_MAX`. (Found in the v1.77 doc audit.)
- [x] Report modal shown on page load (missing default `display: none`) — fixed v1.17
- [x] Cancel button in report modal did nothing (no `.show` to remove) — fixed v1.17
- [x] Report link invisible at Tier 0 (over-gated) — fixed v1.18
- [x] Detection always read 0 (hidden video frame-pause optimization) — fixed v1.4
- [x] Stillness climbed to 100% and stayed (consecutive-frame comparison too sensitive to noise) — fixed v1.3

---

## Features / Enhancements

Things to build.

### High-leverage
- [ ] Improve and validate the framing passthrough on the positioning screen — verify head and shoulders actually fall within the silhouette guide; surface corrective feedback ("step back," "centre yourself," "lower the camera," "more light"). Approaches to evaluate: browser `FaceDetector` API (limited support but free), a small WASM/MediaPipe face-and-pose model, or a simple bounding-box heuristic over high-contrast regions in the analysis canvas. Tradeoff is dependency weight vs. accuracy. Also: tune the silhouette overlay itself — current circle-and-curve may be optically wrong for a 4:3 frame at typical laptop distance.
- [ ] Backend submission for adverse-event reports (Tier ≥ 2) — currently localStorage only
- [ ] Pre-screening questionnaire on first run (Tier ≥ 2)
- [ ] Trajectory history list — currently only the last round is saved
- [ ] Session summary view (today's rounds, longest hold, total practice) accessible from re-frame screen
- [ ] Camera framing auto-check during positioning (luma sample, warn on too-dark / too-bright)

### Polish
- [ ] Audio test step during positioning (let user adjust system volume before round 1)
- [ ] Differentiated visuals for smoothness vs. stillness rounds (warmer color palette during smoothness?)
- [ ] Reflection prompts after long rounds (gentle, opt-out-able)
- [x] Streak tracking — **shipped as weekly streak in v1.64.** Weekly not daily (avoids daily-streak shame mechanics per CONSTRAINTS §5). Rule: ≥1 active day/week counts; current-week inactivity never breaks the streak. Displayed below the cards (not prominent). Three 7-dot weekday strips (one per app, Mon→Sun) + week-streak count. Backfills journal history retroactively from IndexedDB; meditate and breathe show 0 until next session (no prior history to reconstruct).
- [ ] Welcome screen hint about the `d` key for the debug overlay (or remove it now that detection works)

### Tier-gated future work
- [ ] Age gate (Tier 3)
- [ ] Geo-localized crisis-resources lookup (Tier 3)
- [ ] Daily session count cap (Tier 3)
- [ ] Cool-down between sessions (Tier 3)
- [ ] Onboarding ramp — first 14 days have lower duration cap (Tier 2)

---

## Tuning / Calibration

Numbers that need empirical adjustment based on actual practice.

- [?] Smoothness target band `[0.15, 0.55]` — verify after a few smoothness rounds
- [?] Smoothness max std-dev `0.12` — same
- [?] `MIN_BREAK_SECS = 10` — might want proportional to round duration (e.g. `max(10, 0.15 × duration)`)
- [?] `FAIL_GRACE_SECS = 0.5` — does this feel forgiving enough on a long round?
- [?] `INITIAL_THRESHOLD = 0.60` — initial value of `game.threshold`. The threshold is now adaptive (v1.22) and staircases ±0.02 per win/loss step; this is just the starting point. Try 0.55 or 0.65 and feel the difference for new users.
- [?] `THRESHOLD_STEP = 0.02` — bigger steps adapt faster but feel jumpier; smaller steps are smoother but slower to find the user's edge.
- [?] `MOTION_SCALE = 0.018` — tightened from 0.03 in v1.28. Detection-sensitivity ceiling. If false positives appear (camera noise reads as motion), nudge upward.
- [?] `PIXEL_THRESHOLD = 8` — luma-diff floor that counts as "moved" per pixel. Lower = more sensitive (more false positives from noise).
- [?] Anchor-layer fade-out range `0.85–1.0` — currently sub-bass and fundamental fade between these in v1.32. Widening (e.g. 0.80–1.0) makes the silent zone start earlier; narrowing (0.92–1.0) keeps the drone present longer.
- [?] Ambient bell interval `6000–14000ms` — feels right at first listen but unverified over longer sessions. Could be too sparse (eerie silence) or too dense (chime spam) at extremes.
- [?] Ambient bell volume `0.35` — relative to milestone bells (1.0). If they feel too quiet vs. milestones, raise toward 0.5.
- [?] Ambient bell scale (currently A · C · E · G · A · C · E across two octaves). Try a different scale (Eastern pentatonic, Pythagorean) for different mood.
- [?] Initial round duration of 30 s — too long for first-time users? Try 20 s.
- [?] Step-up of +15 s — too aggressive? Maybe scale: +10 s below 60 s, +20 s above.
- [?] Audio fade-in of 2.5 s — too long? Too short?
- [?] Idle-pause `IDLE_PAUSE_SECS = 300` (5 min). Trance-prevention timing. Coarse heuristic; flagged in KNOWN_RISKS S6.
- [?] Settling timeout `SETTLING_TIMEOUT = 90` — too short for users with anxiety who take longer to settle?
- [?] `BREAK_AFTER_WINS = 3` — interlude every 3 wins. Could try 2 (more frequent breaks) or 4 (less interrupted flow).

---

## Experiments — multiplayer / complex-adaptive-system framing

Treating the app as two coupled complex adaptive systems (user + engine) and as a multiplayer game (user vs. past self, user with others). These are *experiments*, not features — each one has a hypothesis worth testing, a measurement, and a reason it might generate insight beyond what we'd see by just adding capability. Run them because the answer is interesting, not because we already know what to build.

### Category A — Engine ↔ user coupling (mutual modeling)

- [ ] **A1. Personalised difficulty curve beyond 2-up/1-down.** Hypothesis: each user has a characteristic failure pattern (minute-3 fade, post-meal slump, first-round-of-day jitter). Engine builds a per-user model of when failures happen and adjusts the staircase shape to find their *specific* sweet spot rather than a population sweet spot. Measure: success-rate stability over 4 weeks vs. baseline staircase. Risk: model overfits to noise; user gets a confused experience.
- [ ] **A2. Plateau detection and probing.** Hypothesis: a user whose `game.threshold` has been static for 7+ days is either truly plateaued or has adapted to game-the-detection. Engine introduces a controlled variation (slightly different mode, varied detection sensitivity, different starting duration) and observes whether performance survives. Probes whether progress is real or local-maximum.
- [ ] **A3. Calibration-reconciliation dialog.** Periodically the engine asks: *"I think you're working at the right level. Agree?"* User responds yes/no/explain. Engine's internal model of the user updates. Generates a long-running record of user-vs-engine model alignment.
- [ ] **A4. User builds an explicit engine-model.** *"Predict whether the next round will succeed."* The user makes a calibrated forecast; the engine records actual outcome. Brier-score the user's calibration over time. Hypothesis: better self-model → better practice; the metric is internally meaningful.

### Category B — User vs. past self (asynchronous self-multiplayer)

- [ ] **B1. Ghost runner.** Replay the user's best round as a visible faint trace on the ring + audio overlay during a current round. Are they stiller than yesterday? Hypothesis: visible self-comparison improves engagement *without* the shame mechanics of streak counters, because the comparator is just past-you. Risk: introduces competition into a non-competitive practice.
- [ ] **B2. Aggregate-trajectory comparison.** End-of-week reflection shows this week's stillness curves overlaid on previous weeks. The shape — not the score — is the artefact. Hypothesis: shape changes (faster settle-in, smaller dips, longer plateaus at high stillness) are more meaningful than peak scores.
- [ ] **B3. "Today vs. last Tuesday."** Specific time-of-day comparison. Tests whether circadian / weekly rhythm dominates.

### Category C — Inter-user multiplayer (real other users)

*All Tier ≥ 2; involves data leaving the device. Sketch only — implementation gated by user-council ratification per CONSTRAINTS.md §3.*

- [ ] **C1. Anonymous aggregate currently-practising.** *"Right now, 7 people are practising. Here's the median stillness curve."* Faint overlay during your own practice. Tests whether community presence affects sustained-attention quality.
- [ ] **C2. Asynchronous dyadic.** Two consenting users practise independently; their stillness curves are visible to each other after the fact. Tests whether knowing-someone-else-saw-mine affects honesty/effort.
- [ ] **C3. Synchronised dyadic.** Two users in real time, each seeing the other's score. Hypothesis: co-regulation is real and measurable as cross-correlation of stillness traces. If true, this is a meaningful add for therapy-adjacent contexts.
- [ ] **C4. Cooperative group session.** N users, shared aggregate score. The group succeeds together or fails together. Tests whether collective accountability improves or degrades individual practice.

### Category D — Emergent CAS dynamics

- [ ] **D1. Identify attractors.** Across many users / many sessions, are there stable points the system gravitates toward (specific threshold + duration combinations)? Are they distributed naturally or clustered around the staircase parameters? Tells us whether the staircase is finding equilibria or imposing them.
- [ ] **D2. Detect phase transitions.** Is there a stillness-threshold above which the practice changes character? (e.g., after 0.85 the user reports qualitatively different experience.) Hypothesis: there's a non-linear regime change, not a smooth gradient.
- [ ] **D3. Anti-Goodhart probes.** Periodically vary detection sensitivity, threshold, or audio behaviour by ±5%. If performance drops, the user was overfit to the specific implementation. If it holds, calibration generalises. Tests whether they're cultivating real stillness or game-specific behaviour.
- [ ] **D4. Compulsion early-warning.** Engine watches for patterns associated with compulsive use: rapid re-attempts after losses, ignoring break prompts, late-night sessions, escalating session counts. Triggers a gentler intervention than the existing soft cap.
- [ ] **D5. Fatigue and recovery modelling.** Within-session degradation curves, between-session recovery times. Does the user have a measurable "stillness budget" per day? If so, surface it.

### Category E — Mutual information flow

- [ ] **E1. Predictive failure cue.** From the last 200 ms of motion samples, predict whether the user is *about* to drop below threshold. Issue an audio cue — a single soft tone — *before* failure. Hypothesis: predictive feedback shortens the closed-loop, lets the user catch drift before it becomes a loss. Risk: cue creates anxiety; users learn to dread the tone.
- [ ] **E2. Pre/post-session mood correlation.** Optional 1-question rating before settling and after the round. Longitudinal pattern is the signal: pre→post lift indicates immediate value; absent lift means habit but not benefit. (Already discussed; this entry tracks the experiment rather than the implementation.)
- [ ] **E3. Reflective pattern surfacing.** *"You tend to do best in the morning. Your worst rounds follow long sitting periods. Your stillness is correlated with sleep quality."* Engine generates these from the data and surfaces them at long intervals. Hypothesis: reflection beats reactive feedback for skill development.
- [ ] **E4. Open-ended journal coupling.** Free-text note on entry/exit; engine looks for language-outcome correlations over weeks. Hypothesis: the things users write about emotionally have measurable effect on practice quality.

### Category F — Safety as a CAS property

- [ ] **F1. Trance-detection probes.** Periodically during long idle/won states, introduce a brief stimulus (a click, a faint flash) and measure user response — they should react within X seconds. Dampened or absent response is a signal of altered state. Engine reduces stimulus intensity and surfaces a check-in.
- [ ] **F2. Dissociation-risk model from interaction patterns.** Reaction-time distributions, eye-blink rate (if camera permits), motion-stillness derivative. Build a probabilistic estimator and validate against self-reports.
- [ ] **F3. Resilience testing.** Adversarial inputs: simulated camera failure, user reporting acute distress, extreme network latency, extended session that approaches limits. Does the system degrade gracefully? Does the safety surface stay intact under all of these?

### Category G — User-as-system (long-horizon)

- [ ] **G1. Skill curve characterisation.** Over 12 weeks, what does a successful user's trajectory look like? Linear improvement, step-function, plateau-and-jump? Compare to a non-engaged user. The shape distinguishes "this practice worked for them" from "they got better at the game."
- [ ] **G2. Self-similarity analysis.** Are individual rounds fractal? Does a 30-second round look like a scaled 5-minute round? If yes, micro-practice is meaningfully equivalent to macro-practice and short sessions are valuable.
- [ ] **G3. Off-app spill-over.** Self-reported measures of attention/calm/well-being in everyday life vs. in-app metrics. Hypothesis: in-app skill is necessary but not sufficient; some users transfer, some don't. What predicts transfer?

### Notes on running these experiments

- **Most are Tier ≥ 2.** They require either backend infrastructure (C-series), tracking that exceeds Tier 0 ethics (D, E, G), or population data (D1).
- **Pre-register hypotheses.** Without it, post-hoc analysis becomes pattern-matching on noise. Write down the prediction and the kill criterion before instrumenting.
- **Most experiments will fail.** That's the point. The goal is generating real evidence, not confirming designs.
- **Some experiments are themselves safety-relevant.** F-series in particular needs clinical-advisor sign-off before instrumenting.

---

## Design Questions

Open decisions, not yet resolved. Many flagged in `CONSTRAINTS.md` §7 already; tracking the most live ones here.

- [?] Co-founder requirement: solo at Tier 0 OK; what's the trigger to require disciplinary diversity (clinician/ethicist) on the team?
- [?] Tier transition triggers — at what user count, revenue level, or duration does TIER 0 → 1 → 2 → 3 advance? Pre-commit before reaching them.
- [?] Pre-screening questionnaire content — which validated items, what false-positive vs. false-negative tradeoff
- [?] User council mechanism — how are members selected? Self-nomination, randomized, by tenure?
- [?] Crisis pathway specifics — which hotlines, which warm-handoff partners, what consent flow
- [?] Revenue model — direct subscription, donation, sliding scale; each has different power-distribution implications
- [?] Should consecutive-loss tracking exist anywhere? (Currently no. Risk of shame-mechanic if surfaced.)
- [?] Smoothness rounds use the same staircase as stillness rounds — should they?
- [?] Should re-framing reset the cooldown if mid-cooldown?

---

## Reported Concerns

Items captured via the in-app `this didn't feel right` link. Pull from `localStorage.hill-combing-reports` periodically and triage here. Each entry should keep its timestamp and the round-state snapshot from when it was reported.

- [!] (none yet)

---

## Done

Items completed in recent versions, kept for context. Archive to a CHANGELOG when this gets noisy.

- [x] **v1.99 (climb v0.4) — cross-device sync foundation + Climb pilot (opt-in, end-to-end encrypted).** First break from device-only storage, at founder request ("log in and have my data sync across devices"). New shared classic script `hc-sync.js` (loaded before each app's script so its adapters can close over the app's globals; Supabase JS pulled via lazy dynamic `import()` so first paint stays offline/no-build) — an app-agnostic sync layer over **Supabase** (Auth + Postgres with Row-Level Security). **Zero-knowledge:** a random DEK encrypts every doc (AES-GCM); the DEK is wrapped by a passphrase-derived key and a one-time recovery-code key (PBKDF2-SHA256, 600k); the server holds only ciphertext + the two wrapped-DEK blobs, and cannot decrypt. Working DEK is imported non-extractable and cached in a device-local `hc-sync` IDB. Per-store merge: **blob LWW** for `hill-climbing-climb` (device-local logical clock; server RPC re-checks), **union-by-id** for the immutable `climb`/`events` log; startup renders local first but **arms pushing only after the initial pull reconciles** (render-then-clobber guard, three-layered). Climb wiring is one push hook at the `apply()` choke point + two `registerStore` adapters reusing `load/save/render/idbAll/idbPutMany`; account/passphrase/recovery/sign-out/**Delete-my-synced-data** UI lives in the existing corner menu with a status dot. Council's API key and device-local UI flags are **never** synced (allowlist model). **Binding-doc amendments (founder-ratified via plan approval):** REQUIREMENTS §1.1 (new `sync_docs`/`sync_keybundle`/email inventory as §1.5 + folded in the missing L26 keys `levity`/`council`/`council-key` and `train`), §1.3/§2.1 (opt-in E2EE sync as a third off-device flow + Tier-0 carve-out on the "operator can't read it" principle), §2.2 (optional account), §2.3/§1.4 (consent + deletion); CONSTRAINTS §3.1/P1/P4 honored by design, not amended. `sw.js` precaches `/hc-sync.js` (cache → hc-v1.99). Verified in headless Chromium: not-configured path stays dormant + local app works with no console errors (7 checks); E2EE envelope round-trips end-to-end (non-extractable DEK, passphrase + recovery unlock, wrong-pass + AAD-tamper both rejected, log round-trip). **Founder provisions** the Supabase project (URL + anon key into `climb.html`, run `supabase-schema.sql`) before cross-device verification; schema + steps shipped in-repo.
- [x] **nourish v0.13 — Chef mode: an LLM writes a recipe from your pantry and teaches the craft.** New fourth tab (Cook · **Chef** · Shop · Progress). You list what's in your kitchen (one tap fills it from your shopping have-marks + extras), optionally pick a time posture (Quick / No rush / A project) and add a free-text preference, and Claude returns **one recipe you can cook mostly from what you have**, rendered in the app's own challenge-card vocabulary: a skill-focus tag, the dish, pantry pills (additions flagged in accent), **"The skills you're building"** — each competency with *why it matters* and a concrete **"How to check —"** you can observe, then **"How to cook it"** where every numbered step carries the *technique* it teaches and the *sensory cue* that it's working, a "You've nailed it when" tell, and a rescue note. The request is tailored to the cook's current ladder level + already-learned skills. **Bring-your-own Anthropic key** (default model **Opus 4.8**, Sonnet 5 selectable), browser→Anthropic direct (`anthropic-dangerous-direct-browser-access`) with **structured output** (`output_config.format`) so the recipe comes back in a fixed shape — mirrors council.html's pattern. **First off-device path inside a previously fully-offline app**, gated behind an explicit **consent checkbox** that names the data flow (pantry list → Anthropic) in plain language before the first call. Key + recipe history live in their **own** localStorage blob `hill-climbing-nourish-chef`, deliberately kept **out of the Nourish data export**. "COOK THIS" logs the day on the hub (`markUsage`, the commit-to-cook moment) and offers the same three honest self-reports (Nailed it / Came together / Struggled) written to a chef history — **off-ladder, the 2-up/1-down staircase is untouched**. Egress + the un-inventoried key flagged in KNOWN_RISKS **L27** against REQUIREMENTS §1.1/§1.3/§2.1 for founder ratification — **not** self-amended (same posture as Council L24). Verified end-to-end in headless Chromium with the API stubbed: 32 checks — consent gate, key+consent enforcement, structured request body (output_config/json_schema/model/pantry/time), full card render (competencies + eval, taught steps, tell), COOK THIS→usage→self-report, ladder-untouched, key-absent-from-export, consent-persists-across-reload, 401 error path; zero console/page errors.
- [x] **climb v1.97 (v0.3) — one editable root; leaves shown under their branch; Overview replaced by a Steps tab.** (Renumbered from v1.96 to clear a collision — Train landed on `main` as v1.96 concurrently.) Reframed navigation around a **single editable root goal** ("The hill", renamable — logged like any rename): every goal now hangs under it, so the goal screen is the only tree browser and there's no separate flat-goals view. On any goal's screen you see its **parent** (breadcrumb), its **direct children**, and **all descendant leaves grouped beneath the direct child they belong to** (a direct child that is itself a leaf shows nothing under it; a leaf deeper than a grandchild carries a dim relative path). The depth-grouped **Overview tab (v0.2) is removed**; its flat all-steps view becomes a dedicated **Steps** tab (one flat list, no depth grouping, goal path per row, live checkboxes, dim done group). Nav is now **Goals · Steps · History**. The root can be renamed but never moved, rested, or focused (its ⋯ menu is Rename-only; no focus toggle). Drag now reorders **branch blocks** (child + its leaves move together); leaf rows aren't draggable. **Migration:** existing saves gain a root and their top-level goals are reparented under it, with one `log.snapshot` baseline appended (the per-event log didn't record the structural change); a fresh install just seeds an empty root and logs nothing until the first goal. `hill-climbing-climb` blob gains `rootId` (REQUIREMENTS §1.1 updated); `sw.js` cache → `hc-v1.97`. Verified in headless Chromium (39 checks): root creation/protection/rename, leaves-under-children with relative paths, leaf navigation, branch-block drag (mouse), flat Steps tab round-trip, one-time migration + no-duplicate-on-reload, 375px, zero outbound, clean console.
- [x] **v1.96 — Train, a new practice (log your workouts).** (Renumbered twice to clear concurrent collisions on `main` — Climb landed as v1.94, then Climb v0.2 as v1.95; Train is the **seventh** practice.) New single-file app `train.html` (in-product `v0.1 · train`, ember accent 20° between Reflect's amber and Levity's rose). Create a workout, add exercises — pick from your own catalog or define a new one choosing which of **reps / weight / timer** it tracks. Every set carries a **goal and an actual** per tracked field; pulling a known exercise copies its sets from the last workout that included it with each goal = max(previous goal, previous actual) — quiet progressive overload as an editable default, no streaks/badges/PRs per CONSTRAINTS. Per-set count-up stopwatch persists its start timestamp (survives reloads, never drifts); stopping opens a confirm step — elapsed prefilled, −10s/−5s/+5s and free editing — before the actual time is logged, since you're often late stopping the clock. The done-check copies goal→actual for one-tap "as planned" sets. lb/kg toggle in the corner menu (remembered; stored canonically in lbs so cross-workout comparisons stay valid). State in one `hill-climbing-train` blob with export/import/reset; an in-progress workout persists and resumes across reloads. `markUsage('train')` at workout start (the commit moment). Hub: seventh card + dash-row + streak predicate (masthead already count-free); sw.js precaches `/train.html` (cache → hc-v1.96); manifest Train shortcut.
- [x] **climb v1.95 (v0.2) — Overview tab: every goal and step in one flat, bottom-up view.** Third tab (Goals · **Overview** · History) with a Goals | Steps segmented toggle. **Goals mode:** two sections — **Leaves** (effectively-active goals with no active subgoals; "the concrete edges") then **Parents** — each grouped by depth with small labels ("2 levels deep" → "top level"), deepest first, DFS-stable within a group; every nested row carries a dim ancestor path ("Get strong › Sleep well"); tap jumps into the tree at that goal (full breadcrumb). Global collapsed "resting · N" disclosure at the bottom (explicitly-rested subtree roots, with path + Pick-it-back-up). **Steps mode:** all open steps of active goals in the same deepest-first depth groups, each with its goal path; checkboxes are live (normal `task.complete`/`task.uncomplete` events via `apply()`); dim collapsed "done · N" group below; steps under resting goals stay parked with their goal. Pure derived view — no new storage, no new event types, viewing logs nothing. New helpers `depthOf`/`dfsActiveGoals`/`groupByDepth` beside the existing derived reads. Per L25: depth groups and counts are neutral structure — no percentages or scores. `sw.js` cache → `hc-v1.95`. Verified in headless Chromium (24 checks): section split, depth grouping/labels, paths, jump-in navigation, restore from the global disclosure, live checkbox round-trip from the overview, zero events from mere viewing, 375px fit, clean console.
- [x] **v1.94 — Climb, a sixth practice (goals + steps, with an honest memory).** New single-file app `climb.html` (in-product `v0.1 · climb`, night-sky indigo accent 230° — fills the arc between Breathe's blue and Meditate's violet). Hierarchical goals at arbitrary depth (drill-down navigation + breadcrumbs, not an outline — phone-width friendly), sibling re-ordering via a pointer-drag grip handle (mouse + touch, `touch-action:none` on the handle only), explicit **Move…** picker for reparenting (own subtree excluded), one featured **current focus** goal surfaced in an accent card at the root, archive-as-**resting** (goals never delete; non-shaming "Let it rest" / "Pick it back up" copy), and day-sized **steps** (tasks) per goal with complete/reopen/two-tap-delete. **Every mutation flows through a single `apply()` choke point** — reduce onto the `hill-climbing-climb` localStorage blob (source of truth), then append to an append-only IndexedDB event log (`climb`/`events`, ts-indexed): 14 event types with before/after payloads (renames carry from/to, moves carry both parents+indices) so add/remove rates, goal evolution, and attention gravity are computable from the log alone. V1 ships the raw **History** browser (day-grouped, human-phrased) + full JSON export (state + events) + in-app two-tap **Delete everything** (blob + event DB) — no analytics dashboard, no timers, and deliberately **no due dates, points, streaks, or completion percentages** (anti-gamification review in KNOWN_RISKS L25). Usage logs `markUsage('climb')` on the **first mutating event of a page load**, not page-open. Hub wiring: sixth card (mountain-and-flag glyph), dash row + accents, `climb` in APPS/weekIsActive/sets, masthead now count-free ("practices for coming back to yourself" — the count broke on every new app; dropped at user request). `sw.js`: cache → `hc-v1.94`, precache `/climb.html` **and `/levity.html`** (the latter was missing since v1.92 — pre-existing gap, one-line fix). REQUIREMENTS §1.1/§1.4 rows added with founder sign-off via plan approval; adjacent Levity/Council inventory gaps flagged in KNOWN_RISKS L26, not silently amended.
- [x] **council v1.93 (v0.3) — the meeting is now interactive (Chair-facilitated continuation).** (Renumbered from v1.92 to clear a collision — Levity landed on `main` as v1.92 concurrently.) First testing observation: one-shot deliberation is ineffective — you can't reach a conclusion in a single pass. Fixed the core interaction model. After the opening round (4 directors + Chair, unchanged), a reply composer appears; you push back / add what they missed / ask to go deeper, and the **Chair responds as a facilitator**, carrying the full thread, bringing a specific director's voice back in when a lens helps, and driving toward a decision you own. A distinct `FACILITATOR_SYSTEM` prompt (separate from the one-shot `CHAIR_SYSTEM`) encodes the ethos per-turn: take pushback seriously (update, don't defend), never flatter or decide for you, and — as an explicit anti-dependence guard — **converge, don't prolong** (a good chair makes the meeting *end*). Crisis/medical/legal hard limit applies every turn. Multi-turn is a proper `messages` array (situation → opening-as-assistant-turn → back-and-forth); `callClaude` refactored into `callClaudeRaw(system, messages, maxTokens)` + a single-turn wrapper. Thread persists onto the saved session; **reopening a past meeting from History shows the thread and lets you continue it.** Note: this sends a growing context per turn (still off-device, same key — no new binding-doc issue beyond L24); cost scales with conversation length. Verified end-to-end (opening 5 calls, reply grows context 3→5→7 msgs, persistence, reopen-and-continue) in headless Chromium.
- [x] **v1.92 — Levity, a new practice (learn to be funny).** New single-file app `levity.html` (in-product `v0.1 · levity`) built on the same 2-up/1-down staircase as Nourish: a 10-level ladder of 25 comedy-craft challenges (observation → rule of three → specifics → exaggeration → misdirection → callbacks → self-deprecation → wordplay → act-outs → building a set), each with steps, a worked example, the theory, and a "you've nailed it when" tell. Self-reported outcomes (Landed it / Got a smile / Fell flat) drive the staircase. Middle view is a **Notebook** (bit file) — capture lines from the report screen or add them by hand, star the ones that landed. Rose accent (348°) fills the warm end of the wheel past Reflect's amber. Wired into the hub as the **fifth card** (Council is not hub-carded): dashboard row, `hill-climbing-levity` state key, `markUsage('levity')` into the shared usage log (keyed by the canonical `hcDayKey`), subtitle now "five practices." Export/import + corner data menu carried over. (Was authored as v1.90 but rebased over the merged Council v1.90/v1.91 line.)
- [x] **council v1.91 (v0.2) — per-session tester note (POC evidence + adverse-event capture).** After the Chair, each session captures a clarity rating (Off / Meh / Somewhat / Clarifying), an open note, and a prominent **"something felt off"** flag — the pre-test instrumentation from the risk plan. Doubles as the adverse-event signal (meditate's "this didn't feel right"). Written onto the saved session; flagged/noted sessions marked in History and prefill on reopen.
- [x] **council v1.90 (v0.1) — new practice: an LLM board of directors.** New single-file app `council.html` (fifth practice; not yet wired into the hub dashboard). You bring a real decision/situation; four directors respond in parallel from distinct lenses — **Steward** (future self), **Strategist** (real objective + leverage), **Skeptic** (loyal opposition), **Humanist** (the people, and you) — then a **Chair** synthesises the tension and hands the choice back without voting. Design thesis: safety and effectiveness are bidirectionally aligned when the board optimises for *your judgment*, not your compliance — so every seat is instructed to resist flattery, disagree (with you and each other), stay in its lane, and never decide for you; a hard crisis/medical/legal limit is baked into each system prompt; an always-visible 988/crisis line + not-therapy disclaimer sit in the UI. **Bring-your-own Anthropic API key** (localStorage `hill-climbing-council-key`), calls go browser→Anthropic directly (`anthropic-dangerous-direct-browser-access`), model `claude-opus-4-8`, 5 short non-streaming calls per session. Sessions saved locally (`hill-climbing-council`, cap 30) with History + Clear. **This is the first app in the suite that sends data off-device** — disclosed plainly, gated on an explicit key. Contradicts REQUIREMENTS §1.3/§2.1 ("nothing leaves the device") — flagged in KNOWN_RISKS L24 for founder ratification, **not** silently amended. Verified end-to-end (key gate, parallel deliberation, Chair, history persist/reopen, error paths) in headless Chromium with the API stubbed.
- [x] **nourish v1.89 (v0.12) — remove the home-screen how-it-works instruction.** Dropped the "Cook the dish in your own kitchen, then tell me how it went. It adapts — clear two and you climb, struggle and it eases back." hint under the home button (static markup emptied + the `renderHome` else-branch set to `''`). The separate mid-cook "…is waiting for your verdict" message on the same `home-hint` element is kept.
- [x] **nourish v1.88 (v0.11) — two learning tracks: General + a new Sushi track (beginner → aburi).** Introduced a `track` concept. The original 24 challenges are the **General** track, untouched (they default to `track:'general'`); added a **Sushi** track of 8 challenges across 4 levels: cook sushi rice → season into shari → temaki & maki rolls → slicing & nigiri → spicy mayo/sweet glaze → **aburi (blow-torched) salmon nigiri** as the final destination. Each sushi challenge has the full treatment — brief/teaches/tell, "How to do it" steps, shopping ingredients + quantities, and a scalable "Make it a meal" recipe. A segmented **General / Sushi** selector on the Cook home switches tracks. **Per-track progress** (level/streak/cleared/history/active/recent in `state.tracks[id]`, mirrored into the working fields the staircase reads); pantry/extras/servings stay shared. `LVL_MAX`→`lvlMax()`, `LEVEL_THEMES`→per-track + `levelTheme()`; pickChallenge / upcoming / aheadSkill / Progress-skills are all track-scoped. **Existing pre-track saves migrate into the General track** (no progress lost). New skills: Sushi rice, Rolling, Nigiri, Aburi (torch). Verified: migration, per-track isolation, the sushi 2-up staircase, sushi recipes/steps/quantities, import, and no console errors.
- [x] **nourish v1.87 (v0.10) — full, scalable "Make it a meal" recipes.** Each challenge's "Make it a meal" is now a complete beginner recipe: a meal name, a bulleted ingredients list with quantities, and a numbered method that builds on the challenge technique ("…steps above"). A **Feeds 1 / 2 / 4** selector on the card scales the ingredient amounts (e.g. Butter & parmesan pasta: 180g→360g pasta) and shares `state.servings` with the Shop selector — change it in either place and both update. Recipes live in a `MEAL_RECIPE` map (per-person quantities); the quantity formatter was refactored (`fmtQty` → `fmtQtyItem` core + `mealIngLine`) so counts/fractions/plurals all read right: "2 onions (finely diced)", "½ can chopped tomatoes", "8 eggs", "4 chicken thighs", "400ml stock". New `renderMeal()`; the old one-line `meal` prose is replaced by the recipe. Verified all 24 recipes render and scale, the card↔shop selectors stay in sync and persist, and no console errors.
- [x] **nourish v1.86 (v0.9) — explicit "How to do it" steps on every challenge card.** Added a beginner-friendly numbered step list to all 24 challenges (kept in a separate `CHALLENGE_STEPS` map, like the ingredients/quantities), rendered as a **"How to do it"** block between "The challenge" and "Make it a meal". The steps are explicit enough to follow with no prior cooking knowledge (e.g. fried egg: "Crack the egg into a cup first, then gently slide it into the pan… when the white is nearly set, put a lid on for ~30 seconds to set the top"). Numbered `<ol>` with accent-coloured `::marker`s; the block hides if a challenge has no steps (none currently). Verified all 24 challenges have steps, the script parses (curly-apostrophe strings), and no console errors.
- [x] **nourish v1.85 (v0.8) — "Finished cooking" relabel + back-to-instructions on the record screen.** The challenge card's primary button is now **FINISHED COOKING** (was "START COOKING") — it leads to the record-result screen, so the label now matches what it does. The record screen's copy follows: kicker "Cooking" → "Finished", prompt "When you're done — how did it go?" → "How did it go?". Added a **"‹ back to instructions"** action that returns to the recipe card (resolves the dish from `served`, or from `state.active` when resuming a cook after an app reopen), so you can re-read the brief / "nailed it when" before recording — pressing FINISHED COOKING again returns to the record screen. Renamed `startCooking`→`finishCooking` and the button id to match. Verified the instructions⇄record loop, the resumed-cook path, and no console errors.
- [x] **nourish v1.84 (v0.7) — ingredient quantities + a "Feeds 1 / 2 / 4" selector on the shopping list.** Each recommended ingredient now shows an indicative quantity that scales with a serving-size selector (default 2, persisted in `state.servings`): per-person base amounts in `INGREDIENT_QTY` × the serving count, e.g. pasta 90g/person → 90 / 180 / 360g. A `fmtQty()` formatter handles weights/volumes (180g, 200ml), bare counts (eggs → 4), measure words with pluralisation (1 onion / 2 cloves / 4 handfuls; fractions stay singular — ½ lemon, ½ can), and fixed notes for things that don't scale (salt "to taste", olive oil "a few glugs", whole chicken "1 (serves ~4)", mayo's oil "~150 ml"). Deliberately a *sense* of how much to buy, not precise recipe maths. Applies only to the recommended list — manual extras are unchanged. Verified scaling / pluralisation / halves / notes across several levels + persistence; no console errors.
- [x] **nourish v1.83 (v0.6) — stop the iOS zoom when focusing the "Add an item" input.** The add-item input was `font-size: 14px`; iOS Safari auto-zooms on focus of any input below 16px, jolting the layout. Bumped it to `16px` (the reliable fix — Safari won't zoom at ≥16px). Also pinned the viewport meta with `maximum-scale=1.0, user-scalable=no` to keep the screen width consistent / disable pinch-zoom (note: effective on Android; iOS may still honour accessibility zoom — but the 16px change alone fixes the reported focus-zoom). Nourish-only (its input was the only sub-16px field; Reflect's textarea is already 16px). Verified computed font-size 16px + viewport string.
- [x] **reflect v1.82 (v0.5) — corner data menu (ported from nourish).** Replaced Reflect's top-right `export`/`import` text buttons with the same single `⋯` corner icon → "Your data" dropdown (Export "Download your journal" / Import "Restore from a backup file" + an on-device note); opens on click, closes on outside-click / Esc / re-toggle. Inherits Reflect's amber accent. Brings the suite's data-management UI to consistency — Nourish got this in v1.79, and Nourish + Reflect are the only two apps with export/import (Meditate/Breathe have none, so they're unaffected). Verified at 375px: no nav overlap (63px gap), menu mechanics + amber tint confirmed via DOM assertions + screenshot, no console errors.
- [x] **nourish v1.81 (v0.5) — fix the Add-an-item input to the bottom of the screen.** Moved the manual-add composer out of the top of the Additional ingredients list to a `position: fixed` bottom bar (centered inner row, `env(safe-area-inset-bottom)`-aware, grounded with a gradient). It lives inside `#shop-view`, so it shows only on the Shop tab (`display:none` hides it on Cook/Progress) and `#shop-view` gained `padding-bottom: 84px` so the list never hides behind it. The "Additional ingredients" label now sits directly above the list. Verified at 375px: composer fixed at viewport bottom, 87px clearance when scrolled fully, add/submit + dedupe intact, hidden on Cook/Progress, no console errors.
- [x] **nourish v1.80 (v0.4) — left-align the Additional ingredients rows.** The manual `.extra-row` lacked `width: 100%`, so each row shrank to its content width and the `margin: 0 auto` then centered it — short items rendered as ragged, centered rows (e.g. "tin foil" ~142px wide, offset right, name starting at x=165) instead of matching the full-width, left-aligned recommended rows. Gave `.extra-row` `width: 100%` and dropped the now-redundant `max-width`/`margin: auto`; the rows now fill the column with names flush-left. Verified at 375px: extra rows render rowWidth 347 / nameLeft 62 — identical to the recommended list (was 188/142 and 142/165 before).
- [x] **nourish v1.79 (v0.3) — Shop tab: a recommendation-driven shopping list + a persistent manual list, and a corner data menu.** New **Shop** tab in the mode-nav. **Recommended** section aggregates the (deduped) ingredients for your next `UPCOMING_COUNT` (3) uncleared challenges from the current rung up, shows which dishes they cover, and a "X to buy · Y on hand" count; tap any item to mark it on-hand (persists by ingredient name in `state.pantry`, so staples stay checked as you climb). The list refreshes automatically as the level advances (clears / "too easy"). **Additional ingredients** section is a persistent, user-managed list: type to add (case-insensitive dedupe, blank-guard, input clears), each row has a check-off (have) and a × remove; items persist across level changes and reloads (`state.extras = [{name, have}]`) — they never auto-churn. Empty states for both; `pantry` + `extras` included in export/import. **Data menu:** the two top-right `export`/`import` text buttons (which crowded the centre mode-nav once it grew to three tabs) are replaced by a single `⋯` corner icon opening a small "Your data" dropdown (Export / Import + an on-device note); opens on click, closes on outside-click / Esc / re-toggle. Verified at 375px: nav now has 27px clearance each side (no overlap); full Shop flow, persistence across a real reload, and the menu mechanics confirmed via DOM/state assertions + screenshot. (Preview's screenshot tool intermittently hung this session; verified primarily via accessibility snapshot + eval.)
- [x] **nourish v1.78 (v0.2) — split the single "skip" into "show another" + "too easy ↑".** The challenge card's lone skip became two distinct actions: **show another** (neutral reshuffle — a different dish at the same rung) and **too easy ↑** (calibration — the dish is beneath you). "too easy" steps the ladder level up by one (capped at 10, resets the clear-streak for fresh footing) and immediately serves a harder challenge, so a confident cook self-calibrates upward instead of tapping through dishes below them — it complements the after-cook 2-up/1-down, which handles the "too hard" direction via Struggled. Hidden at level 10 (nothing harder to climb to). `card-actions` now wraps on narrow screens. Verified in-browser: level 1→2 with a harder dish served, streak reset, max-level guard, no console errors.
- [x] **suite v1.77 — Nourish: a fourth practice that teaches cooking via the staircase.** New single-file app `nourish.html` (`v0.1 · nourish`, herb-green accent hsl150 — fills the empty arc between Breathe's blue and Reflect's amber). Carries the suite's signature 2-up/1-down staircase (Levitt 1971, same as Meditate) into cooking: a 10-level ladder of 25 real, transferable cooking challenges (seasoning → searing → pan sauces → braising → roasting → emulsions → plating a whole meal). No sensor for cooking, so success is **honestly self-reported** (Nailed it / Came together / Struggled) — two clears step you up a rung, one struggle eases you back, converging on ~71% success. Every challenge card carries a **"Make it a meal"** section whose ambition scales with difficulty — a one-line toss-together at L1 ("pile the dressed leaves next to bread and cheese"), a composed plate that references earlier skills by L8 ("make a pan sauce from the roasting juices — your L5 skill"). State is one small `hill-climbing-nourish` localStorage blob (level, streak, cleared dishes, history, in-progress cook); export/import included (data-ownership per CONSTRAINTS). Two views: **Cook** (home ladder → challenge card → cook-and-report → adaptive result) and **Progress** (skills earned + history). Usage logs `markUsage('nourish')` at the moment you commit to cook (the Meditate-analog of "the sit begins"). Hub wiring: 4th card (bowl-of-steam glyph), 2×2 card grid, dashboard row + accent, and `nourish` added to the weekly-streak predicate. `sw.js` precaches `/nourish.html` (cache → `hc-v1.77`); manifest gains a Nourish shortcut; hub masthead now reads "four practices". **Note for next session:** REQUIREMENTS.md §1.1 data inventory should gain the `hill-climbing-nourish` key (binding doc — needs founder sign-off; see KNOWN_RISKS L21 precedent for the usage key).
- [x] **hub v1.76 — Opt-in Web Push practice reminders (PWA, self-owned, no third party).** Morning + evening prompts delivered to the user's own devices via the Web Push protocol. **Receiving half** in the PWA: `sw.js` gains `push` + `notificationclick` handlers (cache → `hc-v1.76`); the hub gains a quiet "Daily reminders" opt-in panel (`Notification.requestPermission` → `pushManager.subscribe` with the app's VAPID public key → Copy the subscription), hidden where unsupported, with an iOS "install to Home Screen first" guard. **Sending half** in `push/` (Node + `web-push`): `send.js` signs with VAPID and posts a morning/evening prompt drawn from a hand-authored rotation (`prompts.js`, 16+16, non-striving / no-streak-shame voice per CONSTRAINTS); `.github/workflows/notify.yml` runs it on cron (`7 12 * * *` / `7 0 * * *` UTC ≈ 8:07am/pm America/Toronto) + manual `workflow_dispatch`. VAPID **private** key and the push subscription live in **GitHub Actions secrets** — never committed (repo is public); only the public key ships in the client. `send.js` no-ops cleanly (exit 0) until `PUSH_SUBSCRIPTION` is set, so pre-setup scheduled runs don't email failures. Verified end-to-end against a real Chrome/FCM subscription: send returns 201 and the banner displays (morning + evening). Rough edges (documented): GitHub cron is approximate; browser subscriptions can rotate (silent until re-subscribe + re-paste); duplicates the Claude-app reminder routines until those are switched off.
- [x] **hub v1.75 — Install banner now shows on all iOS browsers, not just Safari.** The iOS-hint branch was gated on `isIOS && isSafari`, so Chrome/Edge/Firefox for iOS (which carry `CriOS`/`EdgiOS`/`FxiOS` in the UA) showed nothing — and since no iOS browser fires `beforeinstallprompt`, those users saw no banner at all. Every iOS browser is WebKit underneath and all offer "Add to Home Screen" in the share sheet, so the gate is now just `isIOS`. Dropped the now-unused `isSafari` check. Verified the predicate against Chrome/Safari/Edge iOS + iPadOS UAs (fires) and desktop Chrome (does not — uses beforeinstallprompt).
- [x] **hub v1.74 — Install banner re-shows after 5 days.** Dismissal now stores a timestamp (`Date.now()`) instead of a permanent `'1'`; on load the banner stays hidden only while `now - dismissedAt < RESHOW_AFTER_MS` (5 days), then re-prompts. Legacy `'1'` values parse to epoch ~0 → treated as long-expired → re-show (harmless migration). Window is the one `RESHOW_AFTER_MS` constant. Verified: 2-days-ago stays hidden, 6-days-ago re-shows.
- [x] **hub v1.73 — Install-prompt banner.** Dismissible bottom banner on `index.html` that prompts to install the PWA. Chrome/Edge/Android: captures `beforeinstallprompt`, shows a working **Install** button that fires the native prompt. iOS Safari (no programmatic install): falls back to a "Share → Add to Home Screen" hint with the Install button hidden (`.ios` class). Never shown when already installed (`display-mode: standalone` / `navigator.standalone`); dismissal (× or after installing) persists via `hill-climbing-install-dismissed` so it never nags — deliberately non-pushy per CONSTRAINTS. Styled with the shared tokens (lavender accent, blurred dark panel). Verified in-browser: show/hide paths, prompt() call, dismissal persistence, no console errors.
- [x] **hub v1.72 — Name the installed PWA "Hill Climbing"** (was "Stillness" via manifest `short_name` + hub `apple-mobile-web-app-title`).
- [x] **suite v1.71 — Progressive Web App (installable + offline).** Added a single suite-level PWA: `manifest.webmanifest` (name "Hill Climbing — Stillness", start_url `/`, scope `/`, standalone, dark theme/background, 3 app shortcuts → Meditate/Breathe/Reflect), a `sw.js` service worker (precaches all 4 app shells + icons on install; network-first for navigations so version bumps reach users online; cache-first for static assets; offline-falls-back to the cached hub), and generated icons from `icon.svg` (the stillness ring on the deep-lavender base) → `icon-192/512.png` (512 also maskable), `apple-touch-icon.png` (180), `favicon.ico`. Each of the 4 HTML heads gained `<link rel="manifest">`, icon links, and apple-mobile-web-app meta; each registers the SW on load. Fully offline-capable (apps are static; reflect uses IndexedDB, meditate the local webcam — no network needed). Verified: SW registers at root scope, 12 shell assets cached, no console errors. **Bump `CACHE_VERSION` in `sw.js` on every future deploy** so clients pull fresh HTML.
- [x] **meditate v1.70 — Actually fix the white iOS Safari toolbar (solid `background-color`).** v1.69's `theme-color` didn't fix it on iOS Safari, which tints its toolbar from the body's *computed* `background-color`, not `theme-color` or rendered pixels. meditate's `background:` shorthand (gradient only) left `background-color: transparent` → Safari fell back to white; breathe/reflect were dark only because their solid `background` value set a real `background-color`. Added `background-color: var(--bg-base)` (#05050f) under the opaque gradient — visually identical, gives Safari a colour to sample. `theme-color` (v1.69) stays for Chrome Android.

- [x] **suite — Consistent dark browser-chrome tint (`theme-color`).** meditate's mobile toolbar rendered white while the others were dark: only `index.html` declared `<meta name="theme-color">`. breathe/reflect happened to look right because iOS Safari samples their solid `html/body` background — but meditate paints its background as a `radial-gradient` (no solid layer Safari can sample), so it fell back to white. Added an explicit `theme-color` to meditate (`#05050f`), breathe (`#0a0b12`), and reflect (`#0a0b12`), each matching its own `--bg-base`. This also fixes Chrome Android, which ignores background-sampling entirely. Versions bumped: meditate v1.69, breathe v0.6, reflect v0.4.

- [x] **hub — Equal-height practice cards on mobile.** On the stacked mobile layout (≤600px), cards sized to their own content, so a 3-line subheading made one card taller than its 2-line neighbours. Switched the mobile `nav` from flex-column to CSS grid with `grid-auto-rows: 1fr`, forcing every card to the tallest card's height. Desktop (flex row) was already equal via `align-items: stretch`; unchanged. CSS only, `index.html`.

- [x] **meditate v1.68 — Collapse the start screen to the timed flow; fix hub copy.** With Timed the only practice, the mode picker (a single card you had to click before the duration row appeared) is now redundant: the whole `#mode-picker` is hidden (`display:none`) and `#timed-options` (duration pills + Begin) shows directly on the start screen. `meditationMode` defaults to `'timed'`, and `endTimedSit()` returns to the visible duration row instead of re-hiding it / showing the picker. Both picker buttons stay in the DOM (handlers reference them). Also fixed the last user-facing "camera-guided" reference: the hub (`index.html`) Meditate card now reads "Settle into stillness with a quiet timed sit." Restoring the picker is documented inline in `meditate.html`.

- [x] **meditate v1.67 — Temporarily remove the camera-guided Stillness mode.** The mode picker's **Stillness** card is hidden (`display:none`, inline, clearly commented) while its technical issues are worked out, leaving **Timed** as the only reachable practice. The button element stays in the DOM on purpose — `endTimedSit()` and the timed-mode handler reference `modeStillnessBtn`, so removing it would throw at load; restoring the mode is deleting one inline style. All camera/stillness/staircase code is untouched. User-facing copy that described the camera game was revised to timed-only: the first-run safety modal, the Tier 2+ responsibility-forward variant, and the onboarding tour (now 5 pages, was 6 — dropped the score/floor/smoothness/dissonance pages that no longer apply). The pre-v1.67 stillness onboarding copy lives in git history for whenever the mode returns.

- [x] **meditate v1.66 — Timed-mode audio rework: bookend bells + ambient noise bed.** Replaces the per-frame ambient pentatonic bells (which rang every 6–14 s throughout a timed sit) with a single opening bell (528 Hz) and the existing single closing bell (220 Hz). The continuous sound is now a soft ambient noise bed: looping brown noise through a lowpass (~520 Hz cutoff) with a slow ~14 s LFO swell so it breathes like distant waves — textural, not the sustained sine-drone the user found fatiguing. New `AudioEngine.startAmbientNoise()` / `stopAmbientNoise()` (fade in 2.5 s on begin, fade out 1.5 s on end/abort), routed through dry+reverb→master so mute and the round gate still apply. Removed the `timedNextBell` scheduler and its `AMBIENT_BELL_*` use in `timedLoop()`. State copy updated to "Rest. A bell opens and closes the sit."

- [x] **meditate v1.65 — Timed meditation mode + mode picker.** The "Get started" screen is now a two-card picker: **Stillness** (camera-guided, the original game, unchanged) and **Timed** (quiet countdown, no camera). Timed sits pick a duration (5/10/20/30/45 min, default 10, persisted to `hill-climbing-timed-minutes`) and run a dedicated rAF loop independent of the camera loop — motion detection never starts. The existing ring fills empty→full over the duration with a countdown; audio is the existing ambient pentatonic temple bells at a fixed gentle level (no stillness-reactive drone — `AudioEngine.silenceDrone()` zeros the drone/dissonance baselines, `update()` is never called) plus a single closing bell (220 Hz) at the end. Esc / stop button end the sit cleanly with no win/loss recorded. Reuses `markUsage`, `fmtSecs`, `triggerBell`, the ambient-bell palette constants, wake lock, and the ring dashoffset math.
- [x] **breathe v0.5 — Timed breathwork sessions.** Breathwork now stops after a chosen length. Inline duration pills (3/5/10/15/20 min, default 5) sit under the protocol row, reusing `.proto-btn` styling + `.sel` convention; the choice persists to a new `breathe-session-duration` localStorage key. `tick()` ends the session at the limit (breathwork mode only — training/recovery untouched) with a "Complete / Rest a moment." message and one soft closing tone. Pills disable during a session; manual STOP and Space-to-stop still work.
- [x] **v1.64 — Weekly usage dashboard on the hub.** `index.html` gains a "This week" panel below the three cards: a 7-dot Monday-first weekday strip per app in its accent colour, an `N / 7` count, and a week-streak number (≥1 active day/week; current week never breaks the streak). `meditate.html` and `breathe.html` each gain a `markUsage()` hook writing daily flags to a shared `hill-climbing-usage` localStorage key. Journal usage is read retroactively from IndexedDB, so past reflect entries count immediately. Meditate and breathe show 0 until next use (no prior history to reconstruct). Also bumps the version label to v1.64 (v1.63 commit missed this).
- [x] **v1.63 — Wake lock: prevent screen sleep during sessions.** Acquires the Wake Lock API on session start in `meditate.html`; releases on stop/abort/page-hide. Prevents phone/tablet screen from sleeping mid-practice.
- [x] **v1.62 — Meditate landing = practice screen at rest + audio deferred to rounds.** Landing redesign: the start screen's "Meditate" title (redundant now the hub introduces the app) is replaced by a static replica of the practice ring (`#start-ring`, the `.ring-dots` + `.ring-track` circles only — the resting look), so the landing reads as the practice screen paused. Below it: a serif-italic prompt ("Find a comfortable position.") and a labeled "Get started" pill (replacing the bare ▶ icon — clearer and more inviting; matches the suite's button language). The button runs the existing permission flow → straight into practice (skips framing, per v1.57). **Audio:** the master gain is now phase-gated — silent through the idle "Be still to begin" wait, fades in only when a round begins, fades out when it ends (the pre-round drone was intrusive). Centralized via `AudioEngine._applyGain()` (single source of truth); `setMuted` + `setRoundAudible` (called from `setPhase`) + idle-pause resume all route through it. On-entry `start()` ramp removed; iOS audio unlock stays in the constructor on the Begin gesture. Verified via preview: gain 0 (idle) → 0.62 (active) → 0 (idle), and mute composes (un-muting while idle stays silent).
- [x] **v1.61 — Unified visual identity across the suite (design tokens + serif signature).** Shared token vocabulary now in all four files: `--font-display` (Georgia serif) / `--font-ui` (system sans), `--bg-base`, per-practice `--accent` (+`-soft`/`-glow`), radius/motion. The signature is serif-display / sans-UI with dark backgrounds + a subtle accent-tinted radial glow. **Breathe (v0.4):** serif phase labels + countdown/timer numerals (lining figures), blue accent-tinted bg, accent-driven states. **Reflect (v0.3):** serif italic prompt, serif day-headers, serif journal entries (notebook feel), amber accent-tinted bg, amber focus ring. **Meditate (v1.61):** tokenized into the system — purple/serif/immersive preserved (it was the anchor the others were tuned toward); zero visible change. **Hub:** serif wordmark + italic tagline + serif card titles. Functionally untouched everywhere — CSS only. Family-resemblance model (each practice keeps its accent + mood) chosen over full uniformity. Next: rework Meditate's landing screen to match the practice screen at rest + defer pre-session audio.
- [x] **v1.60 — Multi-app integration: three practices behind a hub.** The repo is now a suite called **Hill Climbing**. New static hub at `index.html` (Meditate · Breathe · Reflect cards, dependency-free). Renames (git-tracked; localStorage/IndexedDB are origin-scoped so state survives): `index.html` → `meditate.html` (the meditation app, v1.60), `nervous-system.html` → `breathe.html` (v0.3), `journal.html` → `reflect.html` (v0.2). Each app gains a top-left `#home-link` (house glyph) back to the hub. The meditation app's visible title changed "Hill Climbing" → "Meditate"; "Hill Climbing" is now the *suite* name (hub wordmark; browser tabs read `<App> · Hill Climbing`). Onboarding/safety copy that named "Hill Climbing" reworded (incl. the Welcome card's name-as-metaphor line). README + CLAUDE.md §1–2 updated for the new layout. Next: unify the visual identity across the three apps (currently meditate = purple/serif/immersive, breathe & reflect = flat-dark/sans).
- [x] v1.59 — Hide the numeric score in the centre of the ring. The ring fill is the sole readout now — no integer, no percent sign. JS still writes `textContent` and `color` to the spans, so the data path is intact; only `#score-display { display: none; }` gates the visibility. Re-enable by removing the override.
- [x] v1.58 — Aesthetic refinements to the meditation screen. (1) Camera glyph swapped from emoji `📷︎` to inline SVG — iOS Safari was rendering the codepoint in colour despite the U+FE0E variation selector and `font-variant-emoji: text` rule, which only an SVG sidesteps. SVG strokes use `currentColor` so hover theming still works. (2) Score number typography: 4rem → 4.5rem, lighter weight, slightly cooler tint. Percent sign tighter, smaller, less shouty letter-spacing — reads as part of the value rather than a label. (3) Round timer: dropped the "REMAINING" pseudo-element label; the number stands alone, lighter weight, gentler colour. (4) State text: dropped "· Esc to stop" from active/settling phases (the visible stop button is the primary affordance; the keyboard hint was clutter on every screen and dead text on mobile). "Fill the bucket…" softened to "Hold steady…" — the accumulator is mechanism, not poetry.
- [x] v1.57 — Initial flow skips the positioning screen. Begin → camera permission → straight into meditation. The 📷︎ corner button (reframe-btn) is now the only way into positioning. Refactor: extracted `enterMeditation()` helper shared by the Begin path and the position-ready path. iOS audio handling: `ctx.resume()` stays inside the Begin gesture (synchronous, in-gesture is required for iOS unlock); `start()` (the 2.5s gain ramp) deferred to after camera-permission resolves so the fade-in doesn't run while a permission dialog is up.
- [x] v1.56 — Camera preview properly removed from layout (2×2 invisible speck instead of full-size with opacity:0), and reframe button anchored to bottom-right corner so it no longer floats where the thumbnail used to be. Glyph swapped from ↺ to 📷 (camera, with U+FE0E to force text-style monochrome on iOS Safari). Mobile breakpoint cleaned up — the prior `width: 100px` was leftover from when this was a text button. The camera-preview `<video>` stays in the DOM as a "playing" rendered element so the motion-detection loop's `drawImage` keeps producing frames (the v1.4 frame-pause constraint).
- [x] v1.55 — Hide the camera preview thumbnail during practice (initial attempt; superseded by v1.56). Set `opacity: 0` while keeping full size, which left a ghost layout that the reframe button was still positioned around — visually the button looked floating.
- [x] v1.54 — Inverted the sensitivity ramp from v1.53. Sensitivity now goes DOWN with progress: 1.5× at progress=0 to 0.5× at progress=1.0. Multiplier is `1 + SENS_SPAN * (0.5 - progress)`. User feedback on v1.53: gauge filling faster at the end didn't match the desired feel — the late-round should be deliberate, not accelerating. Symmetric still — both fill and drain are damped at high progress. Effect: gauge moves quickly early (you can climb out of motion), slowly late (the last slice takes patience).
- [x] v1.53 — Progress-modulated sensitivity (initial direction; superseded by v1.54). Fill/drain rate multiplied by `1 + SENS_SPAN * (progress - 0.5)`. Sensitivity ramped 0.5× → 1.5× from start to end.
- [x] v1.52 — **Paradigm shift: gradient accumulator.** Score is now an integral, not an instant. Per frame during active rounds: `A += dt * (stillness - 0.5)`, clamped to `[0, capacity]`. Win when A reaches capacity; lose when timeLimit elapses. Two staircase knobs (timeLimit, requiredRate) replace (duration, threshold); capacity = timeLimit × requiredRate. The 2-up/1-down cadence (Levitt 1971) is preserved. Removed: failure-grace window, threshold-tick visual, old asymmetric-fail logic — every frame counts proportionally, drain is the grace. Visuals: ring fill and score number show accumulator/capacity during active/won/lost; live stillness during idle/settling/interlude/replay. Color, audio, mandala, particles continue to track instantaneous stillness — moment-to-moment feedback runs alongside long-arc round score. Replay derives the accumulator from recorded samples + saved capacity. localStorage schema migrated; old saves reset to fresh defaults.
- [x] v1.51 — Collapsed three values back to one. Replaced the asymmetric EMA (α=0.25 falling / 0.05 rising) with a symmetric exponential pursuit (α=0.10 both directions), and removed the CSS transition on `stroke-dashoffset`. Game logic, audio, ring fill, and the displayed integer all now read the same eased `stillness`. The ring's old "anti-zero-drop" property — that the user explicitly liked — is now a property of the score itself, not just the rendered ring. Tradeoff: rising is faster than the prior design (α 0.05 → 0.10), so high stillness builds slightly faster than before; the curve in `stillnessScore()` (linear fall 0.80→1.00) still gates 100% behind genuinely sub-noise motion.
- [x] v1.50 — Display-only smoothing on the score number (`displayedStillness` lerps toward `stillness` per frame, α≈0.10). Superseded by v1.51 which lifted the smoothing into the underlying score itself.
- [x] v1.32 — Temple-atmosphere paradigm: harmonic drone fades at peak stillness; ambient pentatonic bell strikes at 6–14s intervals replace the sustained sound. Dissonance floor reduced to 0.01.
- [x] v1.31 — More milestone bells in high range (added 97% → 792 Hz, 99% → 864 Hz). Five total milestones now.
- [x] v1.30 — Per-layer fade-in / fade-out windows along the stillness axis. Anchor layers (sub-bass + fundamental) replaced point-density at peak with sparse two-tone texture.
- [x] v1.29 — Mobile responsive layout (single 640px breakpoint).
- [x] v1.28 — Detection sensitivity (`MOTION_SCALE` 0.03 → 0.018), rise-alpha decays with stillness (climb hardens past 90%), audio gain curve plateaus at eff=0.75.
- [x] v1.27 — Dissonance gain scales with motion (silent at unison, louder when moving).
- [x] v1.26 — Audio pleasantness pass: reduced sub-bass and upper-fifth peaks, dissonance max detune 90→55 cents, low-pass filter on dissonance.
- [x] v1.25 — Error boundary around the loop (S3 fix). Try/catch/finally protects every other safety mechanism from silent failure.
- [x] v1.24 — Onboarding clarity pass: factual corrections, removed unverified "3–5%" stat, dual-mode score language, dynamic welcome text.
- [x] v1.23 — Six-page onboarding modal on first run; about-this-practice link to re-open.
- [x] v1.22 — Threshold staircase (in addition to duration). Both knobs adapt 2-up/1-down. Dynamic threshold tick on the ring.
- [x] v1.21 — Idle-pause trance prevention (5 min in idle/won/lost dims ring + mutes audio; tap to resume).
- [x] v1.20 — Stillness % stays in centre during active rounds; countdown moves to side timer beside the ring.
- [x] v1.19 — Renamed Hill Combing → Hill Climbing throughout user-facing text. localStorage keys preserved for state continuity.
- [x] v1.18 — Report link visible at all tiers (capture always on; backend POST tier-gated)
- [x] v1.17 — Modal CSS bug fix (report modal hidden by default; Cancel works)
- [x] v1.16 — Audio trajectory save & replay
- [x] v1.15 — Minimum 10s break between rounds with countdown
- [x] v1.14 — Cap pill in round-info; start-screen mentions 5-min ceiling
- [x] v1.13 — Smoothness-round mode (alternates after each interlude)
- [x] v1.12 — Tier system (TIER 0–3); local adverse-event capture; responsibility-copy variant
- [x] v1.11 — Personalised welcome; ambient bells idle-only; settling timeout; camera-disconnect detection
- [x] v1.10 — Failure grace window; round-start audio cue; re-frame link
- [x] v1.9 — Debug overlay toggle (d key); positioning step with silhouette guide; today-stat tracking
- [x] v1.8 — Motion interlude every 3 wins (untimed, breathing pacer)
- [x] v1.7 — Safety modal; hard duration cap; Esc abort; reframed loss copy; reminders
- [x] v1.6 — Renamed to Hill Combing
- [x] v1.5 — Game with 2-up/1-down staircase
- [x] v1.4 — Switched detection to live `cameraPreview` element (frame-pause bug fix)
- [x] v1.3 — Threshold-based pixel-counting detection; debug overlay
- [x] v1.2 — Adaptive motion calibration (replaced in v1.3 with simpler approach)
- [x] v1.1 — Initial fundamental-tone audio + ring + game scaffolding
- [x] v1.0 — Camera-based stillness measurement with ambient audio feedback

---

## How to triage

When you sit down to triage:

1. Empty the **Inbox** by sorting items into the right section.
2. Scan **Reported Concerns** — read each note, decide if it's a bug, a tuning issue, a design question, or noise. Move it.
3. Pick at most **3 items from anywhere** to mark `[~]` for the next session. Resist the urge to take more.
4. Move completed items to **Done** (or archive to a changelog when this section gets long).
5. If an item has been `[?]` for more than two triage sessions without action, either commit to investigating it or delete it. Indecision compounds.

The backlog is most useful when it's slightly *embarrassing* to look at — full of things you haven't done yet. A clean backlog usually means you've stopped noticing.
