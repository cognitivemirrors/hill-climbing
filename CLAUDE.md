# Hill Climbing — Agent Handoff

**For the next AI instance.** Read this file first, in full, before acting. Then read the files listed in §3 in the order given. Then ask the user §6's confirmation question before making any change.

This is a working solo-developer project. The user is building a suite of contemplative/practice web apps under the "Hill Climbing" name (it began as a single stillness-meditation app). The project has **pivoted into a public-data art project titled "Governance, the Game"** (see §1): all user data is now treated as **public donated seed data**, the product-era privacy *guarantees* ("nothing leaves your device", zero-knowledge-as-promise) and wellness *disclaimers* ("not therapy") are stripped, and there is **no live `TIER` constant** anymore — the tier-gating scaffolding was product-era framing that the art frame supersedes. `STATEMENT.md` is the art statement that supersedes product-era privacy framing; point to it rather than re-litigating. Care and safety remain the art's *foundation* (Govern's coded floor is literally "care for the vulnerable") — only the privacy guarantees and wellness disclaimers are gone, not the values. The suite is now **eight practice apps (meditate, breathe, reflect, nourish [unified cook+taste], levity, foresee, climb, train) + the companion (companion+council) + the kin + govern world/game layer + two games (echo, garden) + the Trace body-painting toy + the anime experiment + the Remind utility**, behind a hub (ERP was removed; Savor folded into Nourish and its standalone page deleted) (see §1 and §2). Note: §§4–5, 7–8, 10 below were largely written during the single-app era. The *collaboration norms, values, and decision history* they record are still current and load-bearing — but their file references and mechanics often describe `meditate.html` in its earlier camera-game form (see §1; that mode has since been **deleted**, not just hidden). Read those sections for norms-and-history; read §§1–3, 6, 9, 11 for the live factual state, which is what drifts.

---

## 1. Project state

> **⚠️ STRUCTURE — read before editing files.** The project is a **hub + eight practice apps + the companion + the kin/govern world-game layer + two games + one experiment**, all single-file, no build, no backend of our own. `index.html` is the static **hub/landing page** (practice cards + a "next step" suggestion panel — companion-written via BYOK when the opt-in toggle is on, deterministic otherwise — + weekly-usage dashboard + PWA install banner + opt-in Web Push reminder toggle). **When older prose below says "the app" or "index.html" as if it were the meditation app, it means `meditate.html`** — the meditation app was `index.html` until the multi-app split; each app links back to the hub via a top-left home link.

- **The project has pivoted into a public-data art project — "Governance, the Game" (see the header and `STATEMENT.md`).** Hill Climbing began as a solo, on-device practice suite and is being redrawn toward a **values-constrained digital world where donated app data is public seed data**: the practices become the world's geography and verbs (pages → "rooms"), and each person can have an **agent-character grounded in their own profile**. `kin.html` (character lineage) and `govern.html` (the governance game — the project's namesake) are the **first built slices** of that world/game layer. The `window.HC` parity work is its foundation — a character acts in the world indistinguishably from a human. **Turn-based first; autonomous play is the eventual destination, not day one.** The fuller design — a phased roadmap (Phase 0 foundations → 4 guarded autonomy), drafted binding-doc changes, and the two hard gates (security + relational-safety reviews) before partner-shared content — lives in `WORLD_ARCHITECTURE.md` and `WORLD_ROADMAP.md`; §9 tracks it as the largest open thread.
- **Single-file HTML apps**, one per practice, plus the hub and shared JS modules (`hc-sync.js`, `hc-usage.js`, `hc-sync-chip.js`, `sw.js`). No build system, no bundled dependencies, no backend of our own. Open any file in a browser to run. (Opt-in sync uses Supabase; opt-in reminders use Web Push via a GitHub Actions sender — see below.)
- **Versioning is now per-app, and there are no git tags.** Each app carries its own in-product label (`v1.80` in meditate — the old `· t0` tier tag is gone, `v0.11 · breathe`, `v0.15 · companion`, …). There is **no global `vX.Y` suite line and no annotated tags** — the old tag-per-version convention has lapsed (§4). A version lives only in the in-product label + the commit message.
- **No tier-gating code (still solo-stage).** The old `const TIER = 0` at the top of `meditate.html`'s JS, which gated escalating safety scaffolding (0 solo → 1 friends & family → 2 open beta → 3 public), has been **deleted**, along with the tier-gated safety modal, multi-page onboarding, and adverse-event reporter. The tier-gating *machinery* in code is gone; the project is still solo-stage, which `STATEMENT.md` labels "Tier 0" — that's the project *phase*, not a code gate (see the header and `STATEMENT.md`). Care and safety remain the art's foundation — they are just no longer expressed as tier-gated product scaffolding. Anywhere §§4–8 speak of "Tier 0" / "tier-gating" as live mechanics, read them as **historical**.
- **Meditate's camera "stillness" game is deleted (v1.80).** The camera-guided Stillness mode (webcam motion detection → 2-up/1-down staircase over duration + threshold → stillness-reactive synth) and all its safety scaffolding have been **removed entirely** — the dead code, the settling/won/lost state machine, the staircase and its store, trajectory record/replay (`hill-combing-last-trajectory`), the drone audio stack, the mode picker, the safety modal, multi-page onboarding, the adverse-event reporter (`hill-combing-reports`/submitReport), and the `TIER` constant are all gone. **Timed mode** (a quiet countdown sit with a bell-bookended ambient noise bed) is now the whole app. Much of §7's audio/staircase history describes that **now-deleted** camera mode — accurate as decision-history, not for what the app is today.
- **The 2-up/1-down staircase (Levitt 1971, ~71% success) is now the suite's signature**, reused beyond meditation: Nourish (cooking) and Levity (comedy) both climb a 10-level ladder with self-reported outcomes.
- **Data is public donated seed data; the off-device flows are disclosed, not privacy-guaranteed.** All user data in this project is treated as **public seed data** — there is no "nothing leaves your device" / "zero-knowledge" *promise* anymore (see the header and `STATEMENT.md`). The flows themselves are unchanged and honestly disclosed: five **bring-your-own-Anthropic-key** surfaces (Council, Companion, Nourish's chef, the hub's next-step suggestion, and Reflect's photo describe — the last sends journal *photos*, the suite's first image egress, L42) call the Anthropic API directly on the *user's own key*; **Companion additionally has live web search/fetch**; **cross-device sync** (Supabase; the sync server sees blob sizes/timestamps/counts — a factual disclosure, not a privacy claim) covers every app with user data; and **Web Push reminders**. **Two carve-outs survive the privacy strip and must be preserved:** (1) **credentials are secrets, not data** — the Anthropic API key stays out of HC read/export; (2) **only your own data is yours to donate** — nothing makes another person's data public. The data policy is reframed in **REQUIREMENTS** and `STATEMENT.md` — read them before touching anything data-related.
- **The suite is agent-interoperable (founder-directed, 2026-07).** Every page defines `window.HC` (shared `hc-agent.js`): `describe()`/`read()`/`export()`/`import({confirm:true})`/`invoke()`. The binding standard is **REQUIREMENTS §1.4**: human- and LLM-produced on-device snapshots must be **indistinguishable** — every action calls the same internal function the UI handler calls (inline handlers were extracted into named functions shared by both), export/import is byte-faithful (ids/timestamps preserved, IDB records incl. tombstones), **no provenance is recorded anywhere, and the interface adds zero egress**. `llms.txt` at the site root documents it for agents, with binding ground rules. The one carve-out: `hill-climbing-api-key` never appears in read/export (actions only). When you change any app's mutations or stores, **update its `HC.register` manifest and `llms.txt` in the same change** — a stale manifest misleads agents (KNOWN_RISKS L45(f)).

---

## 2. Files in the repo

**Practice apps + hub** (single-file HTML; line counts approximate):

| File | Purpose | Read first? |
|---|---|---|
| `index.html` | **Hub/landing page** (~1,850 lines) — fourteen cards: twelve practice/experiment cards plus the two games (Council is reachable but not carded; the games sit between Train and Anime and are deliberately absent from the dashboard and both suggestion layers), a **"A next step" suggestion panel**: an opt-in **companion-written recommendation** (BYOK on the shared `hill-climbing-api-key`, `claude-opus-4-8`, structured output, re-fetched on every return to the hub, throttled; opt-in flag `hill-climbing-hub-suggest` — the hub's only egress and the suite's first automatic-on-arrival BYOK send) layered over a **deterministic engine** (rest / resume-in-progress / Climb focus step / time-of-day invitation) that is the instant first paint and offline/error fallback. Companion suggestions carry a **review + follow-up loop**: every fetch snapshots (digest/model/reply) to a device-local rolling log (`hill-climbing-hub-suggest-log`, cap 10, never synced); **"didn't fit?"** saves snapshot + note into `hill-climbing-requests` (the hub registers that blob to push it); **"ask about this"** seeds a new Companion conversation with the snapshot as hidden context via consumed-on-read `hill-climbing-hub-suggest-seed`. Plus weekly-usage dashboard, PWA install banner, opt-in Web Push reminder toggle. | Yes (quick skim) |
| `meditate.html` | **Meditation app**, in-product `v1.80`. **Timed mode is the whole app** — the camera Stillness mode, its staircase/audio/state-machine + store, all safety+onboarding scaffolding, and the `TIER` constant were **deleted** in v1.80 (no longer holds any tier constant). HC declares **one** store (`hill-climbing-timed-minutes`) + **three** actions (`setTimedMinutes`/`beginTimedSit`/`endTimedSit`). Older prose calls this "the app" / "index.html". | Yes (skim) |
| `breathe.html` | Breathwork + nervous-system training (`v0.11`; v0.11 stripped the privacy/secrecy + wellness copy — stores/actions unchanged) — coherence / physiological sigh / box / 4-7-8, plus a stress-then-recover training loop. | If relevant |
| `reflect.html` | Journal (`v0.11`; v0.11 stripped the on-device-privacy/secrecy + wellness copy — stores/actions unchanged), IndexedDB-backed (`journal`/`entries`): free-text + optional mood/satisfaction + optional **photos** (≤4/entry, compressed on-device to JPEG data URLs stored inside the entry record, so they ride the per-entry encrypted sync/export/delete unchanged — KNOWN_RISKS L41). Opt-in **"describe photos"** (off by default; `hill-climbing-reflect-photo-ai`) makes Reflect the **fifth BYOK surface**: each newly attached photo is sent once to Anthropic on the shared key for a description + verbatim text transcription, stored beside the photo (`desc`/`ocr`) — the suite's first image egress, L42. The consent is decided **once, account-wide**: the flag syncs as its own encrypted blob (`ls:hill-climbing-reflect-photo-ai`, v0.9), so enable/disable carries to every synced device. **The most sensitive store in the suite** (still true, and now donated as public seed data — see `STATEMENT.md`). | If relevant |
| `nourish.html` | **Unified Cook + Taste** app (`v0.17`) — two tabs. **Cook:** learn-to-cook via the staircase (10-level ladder, self-reported outcomes; General + Sushi tracks; optional BYOK "chef" mode writes recipes from your pantry). **Taste:** the former standalone Savor app, now **consolidated in** — a ten-episode season on the palate (salt · acid · fat · five tastes · aroma · "what does this need?") + writeable guest lessons. Cook challenges deep-link to the paired Taste episode (`#taste-cN`). **Three stores:** `hill-climbing-nourish`, `hill-climbing-nourish-chef`, and `hill-climbing-savor` (the taste store — **preserved**, now written from inside Nourish). Its HC manifest declares **19 actions** across the two tabs (cook: serveChallenge/skipChallenge/tooEasy/finishCooking/recordOutcome/leaveCooking/switchTrack/toggleHave/addExtra/toggleExtra/removeExtra/setServings/logChefOutcome/removeChefKey; taste: openEpisode/setStage/finishEpisode/deleteNote/addLesson). Taste usage folds into the Nourish dashboard row on the hub. | If relevant |
| `levity.html` | Learn-to-be-funny via the staircase (`v0.3`). Comedy-craft ladder + a bit notebook. State: `hill-climbing-levity`. | If relevant |
| `foresee.html` | **Calibration training** via the staircase (`v0.2`) — the metacognition practice. Write one-line predictions about your own life with stated confidence (5–95%; exactly 50% is refused), resolve them against reality (happened / didn’t / can’t say — void never moves the ladder), Brier-score each call (**beat a coin flip = success**) into the 2-up/1-down ladder (10 levels climbing horizon *and* interiority: today-and-concrete → a-month-out inner weather). Calibration view shows a folded said-vs-happened curve — deliberately a curve, never a score (L44). First ladder graded by external reality (resolution still self-reported). Fully on-device, no egress. State: `hill-climbing-foresee`. | If relevant |
| `climb.html` | Goals + steps tracker (`v0.6`) with an append-only IndexedDB event log (`climb`/`events`). No due dates/points/streaks by design. State: `hill-climbing-climb`. | If relevant |
| `train.html` | Workout logger (`v0.3`) — exercises, sets, progressive-overload defaults. State: `hill-climbing-train`. | If relevant |
| `remind.html` | **Reminders utility** (`v0.1`) — log reminders to yourself: text + an optional local due datetime, grouped Overdue / Today / Upcoming / Someday / Done, with done-toggle / snooze / edit / delete. No streaks, no nagging. Synced; due items surface in-app and on the hub's next-step. **No egress** — no BYOK surface, no phone notifications yet (a noted follow-up). Deliberately **outside the usage dashboard** (a utility, not a tracked practice — like the games). HC exposes state mutations only (addReminder/editReminder/setDue/toggleDone/snooze/deleteReminder/clearDone). State: `hill-climbing-remind`. | If relevant |
| `echo.html` | **Game** (`v0.2`) — a listening memory game: five stones voice a growing pentatonic-bell phrase (the suite's signature sound) that the player taps back. No fail state — a slip shortens the phrase by one; a single quiet all-time record ("the farthest phrase you've held"). On-device only, **deliberately outside sync + the usage dashboard** (games are play, not tracked practice). State: `hill-climbing-echo`. | If relevant |
| `garden.html` | **Game** (`v0.3`) — a zen stone-garden Sokoban: twelve hand-made, machine-verified-solvable levels of nudging stones onto moss beds. Unlimited undo, no clock, no move-count records, no level locks. On-device only, outside sync + usage like Echo. State: `hill-climbing-garden`. | If relevant |
| `trace.html` | **Body-painting toy** (`v0.1`) — paint animations with your body: webcam motion (per-frame luma differencing, kept spatial) deposits paint onto a persistent canvas; free/mirror/mandala symmetry, camera-ghost toggle, clear, snapshot (PNG), record-the-making (WebM). No stored state, no sync, no usage hook, no egress — play, like the games. HC actions: startCamera/setSymmetry/toggleGhost/clearPainting/snapshot/toggleRecording. | If relevant |
| `companion.html` | **BYOK AI** (`v0.15`) — **two modes of one app**, chosen by a top-centre switch that re-themes the page (teal ↔ indigo via `html[data-mode]`). Accepts the hub's `?ask=suggestion` hand-off (v0.12): consumes `hill-climbing-hub-suggest-seed` into a new conversation whose hidden, deliberately-unanswered opening turn (`awaiting: true` — refreshTail skips the retry control for it) carries the hub suggestion + digest, so follow-up questions have context; v0.13 adds **visible continuity** — the suggestion is stored on the conversation as `hubRef` and rendered as a dashed hand-off card at the top of the thread (now and on every History reopen), so the person keeps seeing what they came to ask about. **Companion**: reads your current goals + recent journal (incl., since v0.14, photo entries — the stored AI description/transcription ride the digest, never the photos; tombstones excluded) + activity (incl. summed practice durations & session counts for meditate/breathe, from the **cross-device** synced usage aggregate — v0.10/v0.11) into each reply and can search/fetch the web (**broadest personal-content egress** — L29/L30); carries a cross-conversation memory (v0.8, L37); and can **write up requests about the suite to an on-device Requests list** (a `{ heading, prompt }` brief — what to build / questions to answer) via a client-side `add_request` tool, confirm-first, reviewable/copyable from menu → Requests (v0.9, L38 — on-device, no new egress; a server-side feedback service was considered and declined). **Council**: an LLM "board of directors" (4 directors + a Chair) for a decision (off-device — L24). Mode persists in `hill-climbing-cc-mode`; `?mode=council` deep-links. Three synced blobs: `hill-climbing-companion` + `hill-climbing-council` + `hill-climbing-requests`. | If relevant |
| `council.html` | **Redirect** → `companion.html?mode=council` (hash preserved). Council merged into Companion as a mode (companion v0.7). Kept as a stub so old bookmarks/precache entries still land in Council mode. | If relevant |
| `anime.html` | **Experiment** (`v0.2`) — a webcam cel-shading/rotoscope toy (draws you as a cel-shaded character; snapshot/record). No persistent state; linked from the hub but not in the PWA precache/manifest. | If relevant |
| `kin.html` | **Character lineage** (`v0.1`) — the MVP first step of the world direction, built single-user first (World Phase 1). Spawn a character from your history: it inherits a frozen snapshot of *everything* at birth, then diverges into its own person (family name = your name; per-instance given name it proposes and you approve/return-with-feedback). BYOK on the shared key; conversation + name-proposal are **UI-only** (network), so the HC surface exposes only state mutations (setFamilyName/spawnCharacter/approveName/returnName/deleteCharacter). Characters write only their own store (§1.4 intact). State: `hill-climbing-kin` (synced). **All app data treated as public seed data** (the public-data art pivot — see §1 and `STATEMENT.md`). The **family backlog** (embodied practices a character can't do, filed for the human to fulfil) is the designed-for next layer. See `KIN.md`. | If working on the world/kin/game direction |
| `govern.html` | **Governance, the Game** (`v0.2`) — the governance layer over Kin's families, and the project's namesake. Rules authored at levels (charter > family > character); an **append-only ledger** of character actions (a real coded invariant — history is never rewritten); an **asynchronous magistrate** (BYOK) that audits the ledger *after the fact* and writes **public verdicts** (allow/refuse-with-reason/hold) — never a synchronous gate, offline/keyless leaves entries `unreviewed`. The **charter floor** is code-backed (protected-term-redefinition rejection; append-only) — its literal floor is "care for the vulnerable". **v0.2 adds a token-budget layer:** families share a token **surplus** above a coded **subsistence floor** no vote can strip (the care value made material); surplus is distributed by **one-family-one-vote** proposals; **metering is a hard code invariant** (no spending what you don't have); discretionary budgets **reset on a per-world cadence** (a jubilee that leaves the ledger + verdict record untouched). The magistrate (network) stays **UI-only**; HC exposes state mutations only (authorRule/deleteRule/logAction + v0.2's setPool/setSubsistence/setCadence/addFamily/removeFamily/proposeDistribution/voteOnProposal/resolveProposal/spendTokens/resetBudgets). State: `hill-climbing-govern` (synced). The multi-family exhibition is a later layer. See `GOVERNANCE.md`. | If working on the game/governance |

**Shared infra & config:**

| File | Purpose | Read first? |
|---|---|---|
| `hc-sync.js` / `hc-sync-chip.js` | Shared opt-in **client-side-encrypted sync** engine (Supabase holds ciphertext plus blob metadata — sizes/timestamps/counts; an **architecture fact, not a privacy promise** now that data is public seed data) + the floating status chip. Loaded by every app holding user data. | If touching sync |
| `hc-agent.js` | Shared **agent interface** (`window.HC` — describe/read/export/import/invoke; REQUIREMENTS §1.4, KNOWN_RISKS L45). Loaded by **every** page (hub, all practices, both games, anime); each app calls `HC.register({...})` at the end of its main script. Actions must call the same functions the UI calls; keep manifests current with every mutation/store change. | If touching any app's mutations, stores, or the interface itself |
| `llms.txt` | Static agent-facing suite documentation at the site root (llms.txt convention): the interoperability contract, ground rules, app directory with state keys + actions. Hand-maintained — update alongside app changes (L45(f)). | If touching apps or the agent surface |
| `hc-usage.js` | Shared **usage log** (per-app day-flags + per-session practice durations). Records to a `hc-usage` IndexedDB store, keeps the legacy `hill-climbing-usage` localStorage aggregate for offline/companion, and registers a per-record `usage` sync store so totals **sum** across devices. Loaded by the seven practice apps that log usage (meditate · breathe · nourish [cook+taste] · levity · foresee · climb · train) + the hub (added v1.78; Foresee joined v0.1 — marks on commit and on verdict) + **companion** (read-only, joined companion v0.11 — reads `aggregate()` so its context digest sees cross-device durations). (Savor's usage now folds into the Nourish row; ERP is removed.) | If touching usage/dashboard |
| `sw.js` / `manifest.webmanifest` | PWA service worker (`CACHE_VERSION` — currently `hc-v2.31`, **bump on every deploy**; `/erp.html` and `/savor.html` removed from precache — both apps deleted) + manifest. | If touching PWA |
| `supabase-schema.sql` | Backend schema for sync (`sync_docs`, `sync_keybundle`, RLS). Founder provisions the Supabase project. | If touching sync |
| `push/` + `.github/workflows/notify.yml` | Self-owned Web Push **sender** (Node + `web-push`, VAPID) run on cron via GitHub Actions. Receiving half is in `sw.js`. | If touching reminders |
| `README.md` | User-facing repo readme. | Skim |

**Governance docs:**

| File | Purpose | Read first? |
|---|---|---|
| `STATEMENT.md` | **The art statement** for "Governance, the Game" — establishes the public-data frame that **supersedes product-era privacy framing** (all app data is public donated seed data; the two carve-outs; care/safety kept as the art's foundation). Point here rather than re-litigating privacy in code copy. | Yes |
| `CONSTRAINTS.md` | Founding principles: care, safety, balanced power distribution. `[DECISION]` markers for unresolved values. | Yes |
| `REQUIREMENTS.md` | Auditable specifics: **data-practices standard (v0.2, standard-first)** + full inventory in Appendix A, adverse-event runbook, tier criteria, verification, decision register. | Yes |
| `BACKLOG.md` | Work tracking: Inbox / Bugs / Features / Tuning / Experiments / Design Questions / Done. | Yes |
| `KNOWN_RISKS.md` | Self-flagged uncertainties, ranked by user-safety severity (S = safety, L = low). | Yes |
| `WORLD_ARCHITECTURE.md` | **World direction** (draft for founder ratification) — the shape and why of the shift from solo practice suite to a private, two-person digital world: the layer stack over `window.HC` (identity/rooms/authorization/sharing/characters/autonomy), two-sided continuous consent, and the drafted binding-doc changes (§9). Precedence unchanged: subordinate to CONSTRAINTS/REQUIREMENTS. Not shipped; nothing world-related is built. | If working on the world direction |
| `WORLD_ROADMAP.md` | **World direction** (draft) — the phased plan (Phase 0 foundations → 1 vertical slice → 2 partner joins → 3 world of rooms → 4 guarded autonomy), turn-based first; names the two hard gates (security + relational-safety reviews) that block Phase 2. Companion to `WORLD_ARCHITECTURE.md`. | If working on the world direction |
| `KIN.md` | **Kin spec** — the character-lineage model behind `kin.html` (v0.1): copy-on-spawn-then-diverge, family/given names, the naming handshake, the deferred family backlog, and how it is World Phase 1 built single-user first. All app data treated as public seed data. | If working on kin/the world direction |
| `GOVERNANCE.md` | **Govern spec** — the governance-game model behind `govern.html` (v0.2): the level hierarchy (charter > family > character), the append-only ledger invariant, the asynchronous magistrate + public verdicts, the code-backed charter floor ("care for the vulnerable"), and v0.2's token-budget layer (subsistence floor, one-family-one-vote surplus distribution, hard metering, per-world reset/jubilee). | If working on the game/governance |
| `CLAUDE.md` | This file. | (You're here) |

**Document precedence** in case of conflict: CONSTRAINTS > REQUIREMENTS > BACKLOG. KNOWN_RISKS doesn't bind — it captures uncertainty.

---

## 3. Recommended reading order for next instance

1. `CLAUDE.md` (this file) — collaboration norms and history
2. `CONSTRAINTS.md` — the principles
3. `REQUIREMENTS.md` — what's auditable/binding
4. `KNOWN_RISKS.md` — what's known-broken or suspect, ordered by user-safety
5. `BACKLOG.md` — what's open
6. The app you're working on (e.g. `meditate.html`) — skim CSS, skim the JS state machine, deep-read whatever's relevant to the task. Add `hc-sync.js` if the task touches persistence/sync.

---

## 4. Collaboration norms (inferred from the user)

These are how the user has consistently operated. Default to these unless told otherwise.

- **Auto mode is on.** Execute autonomously, make reasonable assumptions, prefer action over planning. The user provides course corrections rather than approving every step.
- **User pushes back when wrong.** Take corrections seriously and re-evaluate framing, not just the specific item. Example from session: I labelled "tab-switching exploit" as HIGH severity; user correctly flagged that this was gameplay-integrity, not safety, and insisted *user safety is the primary axis* for all risk assessment. That reframing should propagate to all future severity calls.
- **When the user says "completely different," believe them.** Across the v1.26–v1.31 audio iterations I kept tuning the same architecture even though the user was reporting the same complaint each round. They had to explicitly say "we may have maxed out on these knobs" before I shifted paradigm. **Heuristic: if the same user complaint persists after 2+ rounds of incremental fixes, the issue is paradigm-level, not parameter-level.** Stop tuning, propose alternative paradigms, ask the user to choose before implementing.
- **User prefers concrete over theoretical.** When asked exploratory questions, give a recommendation and the main tradeoff (2–3 sentences for small questions, longer for substantive ones), not a survey of possibilities.
- **Bias toward shipping but pause-for-inspection default.** After a substantive change, the default is to wait for user verification. The user overrides with "keep going" when they want continuation. Bigger refactors and aesthetic changes especially deserve a pause.
- **One user-stated outcome per iteration when iteration is risky.** The user explicitly noted "you ideate and ship faster than I can inspect" — slow down for things touching state machine, persistence, audio engine, or safety mechanisms. Note: "one outcome" allows bundling multiple coordinated code changes that all address the same user complaint (e.g., three coordinated knob tweaks for a single "audio is harsh" fix). The rule is about the unit of verification, not the unit of code change.
- **Each app self-versions in-product; formats vary** (`v1.80` in meditate — the old `· t0` tier suffix is gone, `v0.11 · breathe`, `v0.15 · companion`). Bump on user-visible changes, and update **both** the `#version-label` HTML literal **and** the JS line that sets its `textContent`. There is no global suite version and no git tags (see the git-workflow bullet).
- **localStorage keys keep the `hill-combing-*` prefix for v1 stability** even though the app was renamed. New keys may use `hill-climbing-*`. Do not rename existing keys without a migration shim — user state would be lost.
- **The IDE / preview panel auto-publishes after each edit, with a hook reminder to mention it.** When you edit `index.html`, the system reminder says "is now visible in the preview panel" — your response should briefly acknowledge that.
- **The `TodoWrite` tool reminder appears periodically.** It's a gentle nudge — ignore unless tracking work would actually help. Never mention the reminder itself to the user.
- **No documentation files unless asked.** Markdown files like CONSTRAINTS.md, REQUIREMENTS.md, BACKLOG.md, KNOWN_RISKS.md, and this CLAUDE.md were each created at explicit user request. Don't proliferate docs unprompted.
- **Keep documentation current by default — including the binding docs.** Standing founder instruction: *always assume you should keep documentation up to date.* When your change makes a doc factually stale — a new app, a new localStorage key, a version bump, a data flow, a roster/count — update the doc as part of the same change, in every doc it touches (CLAUDE.md §§1–2, REQUIREMENTS Appendix A, KNOWN_RISKS, BACKLOG). Do **not** defer factual freshness or leave it "flagged for later." The one carve-out: a **substantive policy amendment** to CONSTRAINTS.md or REQUIREMENTS.md — changing a *commitment, standard, or value* (not inventorying a fact) — still warrants surfacing it to the founder, because those docs bind and the precedence ordering (CONSTRAINTS > REQUIREMENTS > BACKLOG > KNOWN_RISKS) means their substance carries weight. Rule of thumb: **adding a true row to Appendix A = just do it; rewriting P1 or a test = confirm first.** BACKLOG.md, KNOWN_RISKS.md, and this file always grow freely.
- **Reuse existing infrastructure before building new.** When extending the app, ask *"can the existing code be extended to handle this?"* before writing new structure. Default to extension; build new only when conflation would actually hurt readability or correctness. The bias toward new is one of my consistent failure modes — example: when adding ambient bells in v1.32, I considered both a new scheduler and extending the milestone-bell code; the new scheduler happened to be the right call but the deliberation should be the default, not the exception.
- **Git workflow — PR-based; "shipped" means merged to `main`.** The repo is `github.com/cognitivemirrors/hill-climbing`, deploying to GitHub Pages at `https://cognitivemirrors.github.io/hill-climbing/` on every push to `main` (~1–2 min redeploy; the legacy Pages builder occasionally drops a trigger — KNOWN_RISKS L19). Work now happens on **`claude/<slug>` feature branches**, one change per branch, merged to `main` via **pull request** (see `git log` — PRs #13–#19). This replaced the old commit-tag-push-to-main flow. **A local edit is not a ship**; a "shipped" claim must be backed by a merged PR (or at least a pushed branch the user can review).

  **Per-change checklist:**
  1. Bump the touched app's in-product version (the `#version-label` HTML literal **and** the JS `textContent` line).
  2. If any app shell (HTML/JS) changed, bump `CACHE_VERSION` in `sw.js` (e.g. `hc-v2.06` → `hc-v2.07`) so clients pull fresh HTML; add the file to the precache list if it's a new app.
  3. Add a terse `Done` (or Inbox `[x]`) entry to `BACKLOG.md`.
  4. If you touched a data practice, reflect it in `REQUIREMENTS.md` (binding — see the confirmation norm above) and `KNOWN_RISKS.md`.
  5. **If the change alters the app roster, the tier, the versioning/workflow, or a data flow, update `CLAUDE.md` §§1–2 in the same commit.** The handoff rots fastest there — that's why §11 is a freshness *check*, not a snapshot to trust. (Run `./scripts/state.sh` to see the live state.)
  6. Commit on a `claude/<slug>` branch; `git push -u origin <branch>`. Open a PR **only when the user asks** — do not open PRs unprompted.

  **No git tags.** The old annotated-tag convention has lapsed — don't reintroduce tags without asking. Revert is `git revert` of the offending commit/PR (or `git checkout <commit>` locally); there are no version tags to check out.
  - **Concurrent branches collide on version numbers.** Several in-flight `claude/*` branches have collided on `main` (Climb/Train, Council/Levity — see BACKLOG). On collision, renumber to the next free number at merge rather than rewriting history.
  - Never run destructive operations (`reset --hard`, force-push to `main`, branch deletion) without explicit user instruction.

---

## 5. The user's values, as established in this session

The user has been thoughtful and explicit about what matters. Carry these forward.

- **Care, safety, balanced power distribution** are the three founding constraints. Care is active beneficence; safety is harm prevention; power distribution is resisting concentration (over user attention, user data, user behavior, market position, internal company power).
- **User safety is the primary axis** for risk assessment. Gameplay integrity, code quality, accuracy, and feature completeness are secondary. A bug that makes the game less fun is LOW severity; a bug that defeats a safety mechanism is MEDIUM severity even if it requires deliberate user action to trigger.
- **Engagement metrics are explicitly rejected.** Don't optimise for time-spent, daily active use, streak length, or retention. The CONSTRAINTS doc names these as anti-patterns.
- **"Not therapy or medical care" alone is a cop-out** — the user pushed back when I used it as a disclaimer: take responsibility for the harm the practice might cause, don't disclaim it away. This value *aligns with* the public-data art pivot, which strips wellness disclaimers as product-era framing. (The tier-gated "safety modal" this historically pointed to has since been deleted — the value survives, the mechanism doesn't.)
- **Audit-readiness matters.** REQUIREMENTS.md was written specifically because the user wanted documented commitments that an external auditor (privacy, safety, governance) could verify. Don't drift from that doc's specifics; if you change a binding constraint, update REQUIREMENTS.md.
- **Tier-gating was real (now superseded — historical).** In the product era a `TIER` constant at the top of `meditate.html`'s JS controlled escalating safety, and the user explicitly drew the distinction between always-on capture and tier-gated backend submission. That framing — and the `TIER` constant — have been **deleted** under the public-data art pivot; there is no live tier gate in code anymore (the solo "Tier 0" stage that `STATEMENT.md`/`REQUIREMENTS.md` still name is the project *phase*, not a code gate). Preserved here as decision-history: the *instinct* (some safeguards always-on, some scaled to audience) is still a useful lens even though the tier mechanism is gone.

---

## 6. The first question to ask the user

Before making any change, ask:

> "I've read CLAUDE.md, CONSTRAINTS.md, REQUIREMENTS.md, KNOWN_RISKS.md, BACKLOG.md, and STATEMENT.md. My read of the live state: the project has pivoted into a **public-data art project ("Governance, the Game")** — all app data is public donated seed data, the product-era privacy *guarantees* and wellness *disclaimers* are stripped (the two carve-outs — credentials-are-secrets and only-your-own-data — survive), and there is **no live `TIER` constant** anymore. The suite is **eight practice apps + the companion + the kin/govern world-game layer + two games + the anime experiment** (ERP was removed; Savor folded into Nourish and its standalone page deleted), versioned per-app with no git tags. Before I do anything, can you confirm: (a) is the public-data art frame the current top-level intention; (b) which open item is the priority — the watch-items on the off-device flows (KNOWN_RISKS L28–L31), the world/game layer (kin/govern), or something new; and (c) any context the prior session missed?"

Wait for the answer. Then proceed.

---

## 7. Critical decisions made in the prior session that don't fully appear in other docs

These came up in conversation and informed code, but the reasoning isn't fully captured in CONSTRAINTS or REQUIREMENTS.

- **The motion-interlude (light-version alternation) was chosen over a full smoothness-scored mode initially, then the smoothness-scored mode was added later in v1.13.** The interlude is now followed by exactly one smoothness round per cycle.
- **The 2-up/1-down staircase was chosen because it converges to ~71% success rate** (Levitt 1971 — the engagement-zone result from psychophysics). Both duration AND threshold staircase together as of v1.22.
- **Nourish (v1.77) reuses the staircase with a self-reported success signal.** The cooking app has no sensor, so the user reports the outcome themselves (Nailed it / Came together / Struggled): two clears step the level up, one struggle steps it down, over a 10-level ladder of 25 cooking challenges. State is one localStorage blob (`hill-climbing-nourish`: level, streak, cleared dishes, history, in-progress cook). Usage logs `markUsage('nourish')` when the user commits to cook — the analog of meditate's `setPhase('settling')`. Watch the gamification line: levels/progression are exactly what CONSTRAINTS §3.1/§5 are wary of — kept honest by free skips, non-shaming outcome copy, no points/badges, and the weekly (not daily) hub streak. Flagged for review in KNOWN_RISKS L23.
- **Audio is parameter-replay, not signal-replay.** Trajectory recording captures `(stillness, audioMotion)` samples at 10 Hz; replay feeds these back to the live audio engine. Faithful to "what was sent to the audio engine," not a microphone capture. The audio engine's smoothing (~0.5s time constants) means replay is approximate.
- **Onboarding modal shows on first run regardless of tier.** This is universal orientation. Safety modal is tier-gated (≥1). They're separate.
- **The 5-minute idle-pause is a coarse heuristic.** The user accepted it as the trance-prevention safeguard at Tier 0; at higher tiers, more sophisticated detection is on the roadmap.
- **Smoothness rounds and stillness rounds share `game.threshold`.** This is intentional but flagged as L14 in KNOWN_RISKS — at very high thresholds, smoothness rounds become hard.
- **The error boundary in v1.25 was the highest-leverage safety fix** because it protects every other safety mechanism from silent failure via JS errors in the loop. Subsumes the loopRunning recovery item.
- **The audio went through a multi-iteration paradigm shift (v1.26 → v1.32).** The user said the high-stillness sound was unpleasant across multiple iterations of incremental volume reduction. After exhausting "tune the same knobs" (peaks, curves, plateau, fade-out windows), the design moved to a paradigm where peak stillness has *no sustained drone* — just occasional pentatonic bell strikes. Trigger frequencies, scale, volume, and interval are all configurable. The user explicitly likes the bell character; sustained sine-tone drones do not work for them.
- **Detection sensitivity tightened in v1.28.** `MOTION_SCALE: 0.03 → 0.018`. User said it was too easy to settle into a high stillness score. Combined with the rise-alpha-decays-with-stillness change, climbing past 90% now takes real sustained stillness rather than a few seconds.
- **The reward at peak stillness is space, not sound.** This is the v1.30/v1.32 design philosophy. The "rich" zone is around 75–82% (where the harmonic stack peaks); 100% is sparser. Approaching is rewarded, arriving is gentle.
- **Mobile responsive in v1.29.** Single breakpoint at 640px. Round-info wraps; ring shrinks to 240px; timer moves below ring; corner buttons tighten. Not a full mobile-first redesign; it's a "doesn't break" pass.
- **Audio aesthetic preference: sparse + bell-like over sustained-tonal.** Established across the v1.26–v1.32 arc with multiple feedback rounds. When in doubt about audio (a new mode, a new effect, a tuning question), lean toward fewer simultaneous tones with natural decay rather than dense sustained synthesis. The temple-atmosphere paradigm at v1.32 is the touchstone — sub-bass + fundamental fade out at peak stillness, leaving room for occasional pentatonic bell strikes. Deviations from this principle should be deliberate experiments with explicit user ask, not assumed.
- **Hub usage dashboard (v1.64) — data model.** Usage tracking uses a new localStorage key `hill-climbing-usage` written by `meditate.html` and `breathe.html`; the hub reads that log plus the journal's IndexedDB directly (same origin). `reflect.html` is not modified — its full entry history counts retroactively for free. The hybrid approach was confirmed with the user over merging onto IndexedDB: localStorage is right-sized for tiny day-flags; IDB would require cross-page version coordination (a `VersionError`/`blocked` footgun). Migration trigger: if usage tracking ever grows into per-session records with timestamps, move to a dedicated IDB `usage` store (separate from the journal DB to avoid schema coupling).
- **Hub usage dashboard (v1.64) — canonical day key.** `hcDayKey()` in every writer file and the hub uses zero-padded `YYYY-MM-DD` (e.g. `"2026-05-30"`). This is **deliberately different** from meditate's `todayKey()` (unpadded, `getMonth()+1`) and reflect's internal `dayKey()` (0-indexed month). Do not mix these formats — only `hcDayKey` feeds the usage log. Future maintainers: never "reuse" either app's existing date helper for the dashboard.
- **Hub usage dashboard (v1.64) — hook points.** meditate: `setPhase('settling')` — the moment a sit begins (not page-open, not first win). Guarded by `hcLoggedToday` flag per page-load to avoid redundant writes across rounds. breathe: `startBreathwork()` and `startRecovery()` — the two session-start functions. Do **not** hook `tick()`, `enterPhase()`, `advanceRecovery()`, or `startExercise()` — those run per-frame/per-cycle.
- **Hub usage dashboard (v1.64) — IndexedDB gotcha.** The hub opens the journal's IDB with **no version number** and **no `onupgradeneeded`**. If you open it at `version: 1` with a store-creating `onupgradeneeded`, and the user visits the hub before ever opening reflect.html, a storeless `journal` DB gets created — reflect's later `open('journal', 1)` won't fire `onupgradeneeded` (same version, already exists) and the `entries` store is **never created**, silently breaking the journal. If our versionless open creates the DB (detectable: `onupgradeneeded` fired), delete it immediately and return an empty set.
- **Weekly streak rule (v1.64).** ≥1 active day/week (any app). Changed from ≥3 at user request. Isolated in `STREAK_MIN_DAYS` constant and `weekIsActive()` predicate — easy to change. Weekly not daily to avoid daily-streak shame mechanics (CONSTRAINTS §5). Current week never breaks the streak; it only adds +1 if already active.
- **"No audio on iPhone" debugging order.** Always ask in this order before shipping code: (1) is the iPhone hardware silent switch off? Safari respects it for web audio. (2) is the volume up? (3) is the device in a low-power mode that might suspend audio? Only after the user has confirmed all three should you start changing AudioContext / unlock code. v1.33 and v1.34 shipped iOS-specific Web Audio unlock changes when the actual cause was the silent switch — a real-world physical toggle that no code change could have fixed. Lesson: physical-world causes are simpler and more common than they look. Ask first.

---

## 8. Discussion threads worth preserving (not in any file)

These shaped the project's direction. If a future user wants to revisit any of these, the next instance should be aware they happened.

- **AI-lab funding & governance** (anthropological / political-science / economics lenses): how to fund a tech company under care + safety + power-distribution constraints. The user is genuinely building something analogous and asked for serious treatment. Layered structure recommendation: perpetual purpose trust + worker cooperative + capped non-voting outside capital + user council + external accountability board. Concrete funder categories suggested. This conversation is the source of CONSTRAINTS.md §3 (power distribution).
- **AI safety as organisational culture** (~60–70% of practical safety outcome): hiring filter, founder embodiment, costly signals, pre-commitment mechanisms, ritualised dissent, demographic diversity, comp tied to outcomes, welcomed whistleblowers, external accountability, near-miss documentation. The user accepted these as binding for their own org.
- **Trance-state risk and the ring's role.** The user asked specifically about hypnotic risk; the answer was the 5-minute idle-pause. KNOWN_RISKS S6 captures the residual coarseness.
- **What a north-star metric would look like for this app.** Conclusion: not engagement, not capability — qualitative monthly self-reflection + pre/post-session 1-question rating + one external canary metric. Implementation deferred; user was deciding whether to build the in-app rating or just keep a journal.

---

## 9. Active open questions (live as of the REQUIREMENTS v0.2 reframe, 2026-07)

- **The world direction is now the largest open thread.** Hill Climbing is drafted (direction, not decided) to become a private, two-person, values-constrained digital world — practices as rooms, profile-grounded agent-characters, turn-based first (see §1, `WORLD_ARCHITECTURE.md`, `WORLD_ROADMAP.md`). It carries **binding-doc changes drafted for founder ratification**: a new CONSTRAINTS §2 relational/psychological-harm safety commitment (a character wounding in a partner's voice, parasocial attachment, a portrayal that feels like a violation — binding under T1, not a KNOWN_RISKS note), a new §5 anti-engagement amendment scoped to the world (the intimate-partner turn loop is the *strongest* pull — no turn-timers, no "partner is waiting" prompts, no completion pressure; not a games-carve-out widening), and a REQUIREMENTS world-data section (room artifact store, shared-room keys, autonomy guardrails, plus a relational adverse-event path). Nothing is adopted until the founder ratifies via the amendment process; `kin.html` and `govern.html` are the **first built slices** of the world/game layer, and there is **no live TIER** anymore (the public-data art frame supersedes tiers; some of the drafts' "T1"/"Tier" references are now anachronistic and should be re-read in the art frame). Two hard gates — a **security review** (cross-user shared-room crypto: MITM-without-fingerprints, metadata linkage, forward-secrecy) and a **relational-safety review** (two-sided continuous consent, approval queue, rupture/exit, bilateral veto) — **both block Phase 2**, before any partner-shared content.
- **Watch-items on the off-device flows** (KNOWN_RISKS L28–L31) are the live cluster now. BYOK egress disclosure (esp. Companion sending the journal by default on every reply), the *soft* prompt-level web-query guardrail (model-obedience, not a hard filter), sync-metadata leakage (email/sizes/counts/timestamps — now a factual disclosure rather than a privacy failing, since data is public seed data, but still worth surfacing honestly), whole-blob LWW concurrent-edit loss across the synced stores, and adding full **account deletion**. Under the public-data art frame these are about honesty and craft, not privacy guarantees — but they still deserve a look before any partner-shared world content.
- **The data policy is now the public-data art frame (supersedes the earlier product-era reframes).** All app data is public donated seed data (`STATEMENT.md`), with the two carve-outs intact (credentials-are-secrets; only-your-own-data). This further supersedes the prior REQUIREMENTS v0.2 "standard-first" reframe (value → consent-scaled-to-audience → controls-proportional-to-risk; inventory in Appendix A) — the recurring "nothing leaves the device" contradiction is now resolved not by scoping consent but by dropping the privacy *guarantee* entirely: the data is public. Honest disclosures of what each flow sends are kept; privacy *promises* are gone.
- **Meditation safety items S1/S2/S4/S5** (KNOWN_RISKS) are now **moot** — the camera stillness mode they described was **deleted** in meditate v1.80 (mode + staircase + safety modal + adverse-event reporter all gone). They would only matter again if a camera/sensor practice were rebuilt from scratch. Likewise any ERP-specific open item is moot — `erp.html` is deleted.
- **Gamification watch across the ladder apps** — Nourish / Levity / Climb / Train levels & progression sit near the CONSTRAINTS §3.1/§5 anti-gamification line (KNOWN_RISKS L23/L25). Kept honest by free skips, non-shaming copy, no points/badges, weekly-not-daily hub streak.
- **The 12 [DECISION] markers in CONSTRAINTS.md** have proposed defaults in REQUIREMENTS.md §7 but await explicit founder ratification. Don't unilaterally treat them as resolved.
- **`anime.html` is un-integrated** — linked from the hub but the only app not in the `sw.js` precache, no usage hook, no doc beyond §2 here. Decide whether it's a real practice or a scratch experiment. (Related: the manifest's install shortcuts still cover only 5 apps — BACKLOG Inbox. Run `./scripts/state.sh` to see current coverage.)
- **The journal-vs-in-app-rating question** for measuring the app's value in the user's life — user was leaning toward journal first; nothing built. The hub's Reflect dot-strip surfaces usage but no ratings.
- **Agent-interface watch-items (KNOWN_RISKS L45).** Sensitive stores are enumerable by anything in the page context; llms.txt/manifest drift is a doc-freshness surface (watch it especially after the ERP removal + Savor→Nourish consolidation + meditate v1.80 store changes — the manifests and `llms.txt` must match); REQUIREMENTS §1.4's wording awaits explicit founder read-through (the decision itself was founder-stated verbatim in-session). (The former ERP-write-actions clinical-boundary item is moot — `erp.html` is deleted.)

---

## 10. My (prior session's) known weaknesses

For the benefit of the next instance, knowing what to be cautious about:

- **I tend to ship too fast for solo-dev verification.** Default to one feature per iteration with a pause for the user to inspect.
- **I conflated feature-integrity with user-safety in earlier risk assessments.** User explicitly corrected this — *user safety is the primary axis*, not feature correctness.
- **I sometimes propose features the user didn't ask for.** Default to small polish and explicit asks, not roadmap-style additions.
- **I over-cited a specific "3–5%" stat for adverse meditation events** that I couldn't actually verify. The honest framing is "documented across studies of regular meditation" without a specific number. (Already corrected in onboarding copy in v1.24.)
- **I used a hacky `setPhase` reassignment pattern** in the replay code that should be cleaned up to call `updateReplayButton()` inline. Logged as H2 / L-rank in KNOWN_RISKS.

---

## 11. How to verify this handoff is current

Before relying on anything in this file, verify (these drift fastest):

1. **App inventory (§2).** `ls *.html` shows **no `erp.html`** and **no `savor.html`** (both deleted) and 18 files: the hub + eight practice apps (meditate · breathe · reflect · nourish · levity · foresee · climb · train) + the Remind utility (`remind.html`) + companion + council (redirect) + kin + govern + two games (`echo.html` · `garden.html`) + `trace.html` (body-painting toy) + `anime.html`. Savor folded into Nourish; ERP removed. In-product labels roughly match (`grep -h version-label *.html` / the `VERSION` constants) — meditate `v1.80`, nourish `v0.17`, govern `v0.2`, companion `v0.15`, reflect/breathe `v0.11`, remind `v0.1`.
2. **No tier-gating code (still solo-stage).** `grep 'const TIER' meditate.html` returns **nothing** — the constant was deleted in v1.80. The project is still solo-stage (`STATEMENT.md`: "Still Tier 0"); what's gone is the tier-gating *machinery*, not the solo posture. If a `TIER` constant reappears, treat tier-gating as revived and re-sync this file.
3. **Meditation mode.** Meditate is **Timed-only** — the camera Stillness mode + its staircase/audio/state-machine + safety scaffolding were **deleted** in v1.80. `grep -i 'stillness\|getUserMedia' meditate.html` should be empty. If any of that returns, the mode was rebuilt and this file is stale.
4. **Workflow.** `git tag` is still empty and history still shows PR merges (`git log --oneline | grep 'Merge pull request'`) — the PR-based, no-tag flow still holds.
5. **Data policy.** The live frame is the **public-data art project**: `STATEMENT.md` exists and treats all app data as public donated seed data, with the two carve-outs (credentials-are-secrets, only-your-own-data) intact; product-era privacy *guarantees* ("nothing leaves your device", zero-knowledge-as-promise) and wellness *disclaimers* ("not therapy") are stripped from app copy and `REQUIREMENTS.md`. If `STATEMENT.md` is gone or the privacy-guarantee framing has returned, re-sync this file.
6. **Open questions (§9)** — none answered without this doc being updated.

If any of those are out of date, treat this file as stale and update it (this file grows freely — no confirmation needed) or ask the user to re-handoff.

**Automation (so this snapshot self-corrects):** a `SessionStart` hook (`.claude/settings.json` → `.claude/hooks/session-start.sh`) prints `scripts/state.sh` into context at the start of every session and arms an advisory doc-freshness pre-commit guard (`scripts/git-hooks/pre-commit`, warns when app code is committed without a doc/version touch — never blocks). The hook takes effect for all sessions once merged to the default branch; the guard is armed per-session via `git config core.hooksPath scripts/git-hooks` (run it once yourself for a local checkout).

---

## 12. Final note from the prior instance

The user is building this as both a personal practice tool and the seed of a values-constrained organisation. They're thoughtful, push back well, and care about safety as a binding constraint rather than a marketing claim. Treat the work seriously. When in doubt, prioritise their welfare over feature velocity. Read the docs. Match the existing voice in CONSTRAINTS.md and REQUIREMENTS.md (specific, slightly uncomfortable, refuses to disclaim away responsibility) when adding to those documents.

The hardest thing is keeping the structure honest as it grows. Most of my best work in this session came from being explicit about what's binding versus what's malleable — that distinction is the load-bearing one. Carry it forward.
